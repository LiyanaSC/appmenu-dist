import { importShared } from './__federation_fn_import-BMMB1g9W.js';

const plusWhite = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M352%20128C352%20110.3%20337.7%2096%20320%2096C302.3%2096%20288%20110.3%20288%20128L288%20288L128%20288C110.3%20288%2096%20302.3%2096%20320C96%20337.7%20110.3%20352%20128%20352L288%20352L288%20512C288%20529.7%20302.3%20544%20320%20544C337.7%20544%20352%20529.7%20352%20512L352%20352L512%20352C529.7%20352%20544%20337.7%20544%20320C544%20302.3%20529.7%20288%20512%20288L352%20288L352%20128z'/%3e%3c/svg%3e";

const plusBlack = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20d='M352%20128C352%20110.3%20337.7%2096%20320%2096C302.3%2096%20288%20110.3%20288%20128L288%20288L128%20288C110.3%20288%2096%20302.3%2096%20320C96%20337.7%20110.3%20352%20128%20352L288%20352L288%20512C288%20529.7%20302.3%20544%20320%20544C337.7%20544%20352%20529.7%20352%20512L352%20352L512%20352C529.7%20352%20544%20337.7%20544%20320C544%20302.3%20529.7%20288%20512%20288L352%20288L352%20128z'/%3e%3c/svg%3e";

const _export_sfc = (sfc, props) => {
  const target = sfc.__vccOpts || sfc;
  for (const [key, val] of props) {
    target[key] = val;
  }
  return target;
};

const _sfc_main$8 = {  };
const {createElementVNode:_createElementVNode$a,openBlock:_openBlock$a,createElementBlock:_createElementBlock$a} = await importShared('vue');


const _hoisted_1$8 = { class: "starbar" };

function _sfc_render(_ctx, _cache) {
  return (_openBlock$a(), _createElementBlock$a("div", _hoisted_1$8, _cache[0] || (_cache[0] = [
    _createElementVNode$a("div", { class: "left-bar" }, null, -1),
    _createElementVNode$a("p", null, "⭐️", -1),
    _createElementVNode$a("div", { class: "right-bar" }, null, -1)
  ])))
}
const StarBar = /*#__PURE__*/_export_sfc(_sfc_main$8, [['render',_sfc_render],['__scopeId',"data-v-3600db60"]]);

// tableau de types de listes disponibles

const typeMenu = [

  {
    value: 'vegetarian',
    label: 'Végétarien',
    description: 'Sans viande ni poisson.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%201024%201024'%20class='icon'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M767.018667%20171.648a53.418667%2053.418667%200%200%200-68.138667-41.386667%2052.48%2052.48%200%200%200-35.477333%2036.117334%2054.613333%2054.613333%200%200%200-1.642667%2021.589333%2080.106667%2080.106667%200%200%201-22.976%2066.666667l-117.397333%20117.397333%2045.248%2045.248%20117.397333-117.397333a80.192%2080.192%200%200%201%2066.688-22.976c6.848%200.874667%2014.101333%200.405333%2021.546667-1.642667a52.48%2052.48%200%200%200%2036.138666-35.477333%2053.418667%2053.418667%200%200%200-41.386666-68.138667zM937.322667%20342.272c2.709333-12.202667%202.048-25.642667-8.832-41.002667-9.578667-13.504-24.725333-23.338667-41.301334-23.914666A53.333333%2053.333333%200%200%200%20832%20330.666667v0.384c0.170667%2022.549333-10.474667%2043.754667-30.656%2053.845333l-154.325333%2077.162667%2028.629333%2057.258666%20167.296-83.648c18.496-9.258667%2039.786667-6.272%2057.408%204.565334a53.589333%2053.589333%200%200%200%2043.733333%205.44%2052.16%2052.16%200%200%200%2034.922667-34.965334%2053.418667%2053.418667%200%200%200-41.685333-68.437333z'%20fill='%23D7CCC8'%20/%3e%3cpath%20d='M661.333333%20405.333333c0-67.306667%2022.698667-105.450667%2022.698667-105.450666l-45.248-45.248C581.162667%20312.256%20384%20298.666667%20384%20298.666667l277.333333%20106.666666z'%20fill='%23BF360C'%20/%3e%3cpath%20d='M42.666667%20768s147.093333%20128%20384%20128%20426.666667-137.344%20426.666666-256-170.666667-234.666667-170.666666-234.666667-173.76-106.666667-298.666667-106.666666C42.645333%20298.666667%2042.666667%20768%2042.666667%20768z'%20fill='%23E64A19'%20/%3e%3cpath%20d='M839.189333%20578.389333c-14.954667-34.56-39.850667-66.944-65.621333-94.229333C608.405333%20504.32%20320%20597.333333%20320%20597.333333s53.333333%20128%20213.333333%20128c142.485333%200%20208.448-34.048%20305.856-146.944z'%20fill='%23D84315'%20/%3e%3cpath%20d='M384%20298.666667c165.930667%200%20243.050667%2085.333333%20341.333333%2085.333333h76.010667l27.989333%2059.328S687.808%20682.666667%20512%20682.666667c-128%200-192-85.333333-192-85.333334s-24.896-64-106.666667-64c-106.666667%200-170.666667%20234.666667-170.666666%20234.666667s0-469.333333%20341.333333-469.333333z'%20fill='%23FF7043'%20/%3e%3cpath%20d='M277.333333%20426.666667a21.333333%2021.333333%200%201%201-42.666666%200%2021.333333%2021.333333%200%200%201%2042.666666%200z%20m64-64a21.333333%2021.333333%200%201%200%200%2042.666666%2021.333333%2021.333333%200%200%200%200-42.666666z%20m0%2085.333333a21.333333%2021.333333%200%201%200%200%2042.666667%2021.333333%2021.333333%200%200%200%200-42.666667z'%20fill='%23F4511E'%20/%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'vegan',
    label: 'Végétalien',
    description: 'Sans produit d’origine animale.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%201024%201024'%20class='icon'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M524.8%20605.866667s-19.2-27.733333-38.4-46.933334c-19.2-19.2-44.8-36.266667-46.933333-38.4-110.933333-83.2-206.933333-53.333333-256%200-38.4%2042.666667-55.466667%2085.333333-55.466667%20128%200%2032%2012.8%2064%2017.066667%2093.866667%2012.8%2059.733333%2017.066667%2098.133333-6.4%20132.266667l-25.6%2025.6c-8.533333%208.533333-8.533333%2021.333333%200%2029.866666%204.266667%206.4%208.533333%208.533333%2014.933333%208.533334s10.666667-2.133333%2014.933333-6.4l34.133334-34.133334c27.733333-23.466667%2072.533333-12.8%20123.733333-2.133333%2034.133333%206.4%2070.4%2021.333333%20108.8%2021.333333%2040.533333%200%2083.2-19.2%20119.466667-53.333333%2055.466667-51.2%2070.4-162.133333-4.266667-258.133333z'%20fill='%23FF4081'%20/%3e%3cpath%20d='M531.2%20701.866667c12.8-12.8%2021.333333-29.866667%2023.466667-49.066667-6.4-14.933333-17.066667-29.866667-27.733334-46.933333%200%200-19.2-27.733333-38.4-46.933334-19.2-19.2-44.8-36.266667-46.933333-38.4-17.066667-12.8-32-21.333333-46.933333-29.866666-19.2%202.133333-36.266667%208.533333-49.066667%2021.333333-38.4%2038.4-27.733333%20113.066667%2023.466667%20164.266667s123.733333%2064%20162.133333%2025.6z'%20fill='%23F8BBD0'%20/%3e%3cpath%20d='M640%20426.666667s100.266667-85.333333%20174.933333-85.333334c49.066667%200%20102.4%2032%20102.4%2089.6%200%2046.933333-12.8%20102.4-70.4%20102.4-55.466667%200-55.466667-42.666667-59.733333-59.733333-12.8%2014.933333-72.533333%2034.133333-89.6-14.933333%200%200-91.733333%2027.733333-157.866667%2076.8L640%20426.666667z%20m-46.933333%2021.333333c66.133333-76.8%2010.666667-256-89.6-256C426.666667%20192%20405.333333%20230.4%20405.333333%20264.533333c0%2029.866667%2029.866667%2066.133333%2072.533334%2059.733334-8.533333%2053.333333%2040.533333%2076.8%2076.8%2049.066666%2021.333333%206.4-4.266667%20121.6-61.866667%20174.933334l100.266667-100.266667z'%20fill='%234CAF50'%20/%3e%3cpath%20d='M706.133333%2085.333333C659.2%2085.333333%20618.666667%20110.933333%20618.666667%20149.333333c0%2051.2%2057.6%2044.8%2057.6%2044.8s-49.066667%2059.733333%2036.266666%2076.8c-4.266667%2021.333333-36.266667%2061.866667-81.066666%20110.933334l-198.4%20198.4c-8.533333%208.533333-8.533333%2021.333333%200%2029.866666%204.266667%204.266667%2010.666667%206.4%2014.933333%206.4s10.666667-2.133333%2014.933333-6.4l53.333334-53.333333c46.933333-42.666667%20104.533333-102.4%20172.8-172.8%200%200%2061.866667-64%2076.8-81.066667C870.4%20198.4%20853.333333%2085.333333%20706.133333%2085.333333z'%20fill='%23388E3C'%20/%3e%3c/svg%3e", import.meta.url).href,
    without: false
  },

  {
    value: 'pork-free',
    label: 'Sans porc',
    description: 'Exclut le porc et les aliments qui en contiennent.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%2064%2064'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--emojione'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20d='M20.6%2014.8c-2.4-4.5-18.2-3.1-18.2-3.1s-1.9%2016%202.7%2018.5c3.5%201.9%2017.4-11.8%2015.5-15.4'%20fill='%23fc97b2'%3e%3c/path%3e%3cpath%20d='M17.6%2017.2c-1.5-2.8-11.3-1.9-11.3-1.9S5.2%2025.3%208%2026.8c2.2%201.2%2010.8-7.3%209.6-9.6'%20fill='%23ffd7e4'%3e%3c/path%3e%3cpath%20d='M58.7%2030.1c4.5-2.4%203.1-18.1%203.1-18.1s-16.1-1.9-18.6%202.7C41.3%2018.2%2055%2032%2058.7%2030.1z'%20fill='%23fc97b2'%3e%3c/path%3e%3cpath%20d='M56.2%2027.1c2.8-1.5%201.9-11.2%201.9-11.2s-10-1.2-11.6%201.7c-1.1%202.2%207.5%2010.7%209.7%209.5'%20fill='%23ffd7e4'%3e%3c/path%3e%3cpath%20d='M58.8%2039.4C58.8%2056.8%2046.8%2060%2032%2060S5.2%2056.8%205.2%2039.4S21.7%209.5%2032%209.5S58.8%2022%2058.8%2039.4z'%20fill='%23fc97b2'%3e%3c/path%3e%3cpath%20d='M46%2049.8c0%208.1-6.3%207.2-14%207.2s-14%20.9-14-7.2s6.3-12.1%2014-12.1s14%204%2014%2012.1'%20fill='%23ffd7e4'%3e%3c/path%3e%3cg%20fill='%23bf7b90'%3e%3cellipse%20cx='25.7'%20cy='48'%20rx='3.4'%20ry='6.3'%3e%3c/ellipse%3e%3cellipse%20cx='38.3'%20cy='48'%20rx='3.4'%20ry='6.3'%3e%3c/ellipse%3e%3cpath%20d='M32%2022.6s-3-10-9.7-11.9c0%200%203-1.7%205.2-.9c0%200%20.1-1.8-1.8-3.1c0%200%204.3-.9%206.5%202.1c0%200%20.3-3.2-.5-4.8c0%200%207.1%201.7%209%207.8c0%200-7.3%202.1-8.7%2010.8'%3e%3c/path%3e%3c/g%3e%3cg%20fill='%23493b30'%3e%3cellipse%20cx='15.6'%20cy='34.6'%20rx='3.1'%20ry='5.6'%3e%3c/ellipse%3e%3cellipse%20cx='48.4'%20cy='34.4'%20rx='3.1'%20ry='5.6'%3e%3c/ellipse%3e%3c/g%3e%3c/svg%3e", import.meta.url).href,
    without: true

  },

  {
    value: 'gluten-free',
    label: 'Sans gluten',
    description: 'Adapté aux personnes évitant le gluten.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='iso-8859-1'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20height='800px'%20width='800px'%20version='1.1'%20id='Layer_1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20viewBox='0%200%20504.123%20504.123'%20xml:space='preserve'%3e%3cpath%20style='fill:%23FF912E;'%20d='M248.103,401.353c0,4.017-3.261,7.278-7.278,7.278l0,0c-4.017,0-7.263-3.261-7.263-7.278V105.527%20c0-4.009,3.245-7.263,7.263-7.263l0,0c4.017,0,7.278,3.253,7.278,7.263V401.353z'/%3e%3cg%3e%3cpath%20style='fill:%23FFB92C;'%20d='M233.563,105.527c0-4.009,3.245-7.263,7.263-7.263l0,0c4.017,0,7.278,3.253,7.278,7.263v295.826'/%3e%3c/g%3e%3cpath%20style='fill:%23559B0B;'%20d='M248.103,496.845c0,4.017-3.261,7.278-7.278,7.278l0,0c-4.017,0-7.263-3.261-7.263-7.278v-93.412%20c0-4.017,3.245-7.263,7.263-7.263l0,0c4.017,0,7.278,3.245,7.278,7.263V496.845z'/%3e%3cpath%20style='fill:%23FFB92C;'%20d='M240.825,0v110.726c0,0-36.092-8.42-36.092-39.715C204.733,39.723,240.825,0,240.825,0z'/%3e%3cpath%20style='fill:%23FF912E;'%20d='M240.825,0v110.726c0,0,36.108-8.42,36.108-39.715C276.933,39.723,240.825,0,240.825,0z'/%3e%3cpath%20style='fill:%23FFB92C;'%20d='M163.119,92.885l67.537,67.513c0,0-27.167,16.888-46.245-2.19%20C165.317,139.122,163.119,92.885,163.119,92.885z'/%3e%3cg%3e%3cpath%20style='fill:%23FF912E;'%20d='M163.119,92.885l67.537,67.513c0,0,16.88-27.144-2.213-46.238%20C209.365,95.082,163.119,92.885,163.119,92.885z'/%3e%3cpath%20style='fill:%23FF912E;'%20d='M319.87,92.885l-67.521,67.513c0,0,27.168,16.888,46.245-2.19%20C317.672,139.122,319.87,92.885,319.87,92.885z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FFB92C;'%20d='M319.87,92.885l-67.521,67.513c0,0-16.88-27.144,2.198-46.238%20C273.64,95.082,319.87,92.885,319.87,92.885z'/%3e%3cpath%20style='fill:%23FFB92C;'%20d='M163.119,163.47l67.537,67.513c0,0-27.167,16.888-46.245-2.198%20C165.317,209.707,163.119,163.47,163.119,163.47z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FF912E;'%20d='M163.119,163.47l67.537,67.513c0,0,16.88-27.144-2.213-46.238%20C209.365,165.66,163.119,163.47,163.119,163.47z'/%3e%3cpath%20style='fill:%23FF912E;'%20d='M319.87,163.47l-67.521,67.513c0,0,27.168,16.888,46.245-2.198%20C317.672,209.707,319.87,163.47,319.87,163.47z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FFB92C;'%20d='M319.87,163.47l-67.521,67.513c0,0-16.88-27.144,2.198-46.238%20C273.64,165.66,319.87,163.47,319.87,163.47z'/%3e%3cpath%20style='fill:%23FFB92C;'%20d='M163.119,234.055l67.537,67.513c0,0-27.167,16.88-46.245-2.198%20C165.317,280.292,163.119,234.055,163.119,234.055z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FF912E;'%20d='M163.119,234.055l67.537,67.513c0,0,16.88-27.152-2.213-46.245%20C209.365,236.245,163.119,234.055,163.119,234.055z'/%3e%3cpath%20style='fill:%23FF912E;'%20d='M319.87,234.055l-67.521,67.513c0,0,27.168,16.88,46.245-2.198%20C317.672,280.292,319.87,234.055,319.87,234.055z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FFB92C;'%20d='M319.87,234.055l-67.521,67.513c0,0-16.88-27.152,2.198-46.245%20C273.64,236.245,319.87,234.055,319.87,234.055z'/%3e%3cpath%20style='fill:%23FFB92C;'%20d='M163.119,304.632l67.537,67.521c0,0-27.167,16.88-46.245-2.198%20C165.317,350.878,163.119,304.632,163.119,304.632z'/%3e%3c/g%3e%3cg%3e%3cpath%20style='fill:%23FF912E;'%20d='M163.119,304.632l67.537,67.521c0,0,16.88-27.152-2.213-46.245%20C209.365,306.83,163.119,304.632,163.119,304.632z'/%3e%3cpath%20style='fill:%23FF912E;'%20d='M319.87,304.632l-67.521,67.521c0,0,27.168,16.88,46.245-2.198S319.87,304.632,319.87,304.632z'/%3e%3c/g%3e%3cpath%20style='fill:%23FFB92C;'%20d='M319.87,304.632l-67.521,67.521c0,0-16.88-27.152,2.198-46.245%20C273.64,306.83,319.87,304.632,319.87,304.632z'/%3e%3cpath%20style='fill:%23559B0B;'%20d='M247.584,465.203c0,0,9.342-62.803,49.31-57.612c39.952,5.183,44.111-43.599,44.111-43.599%20s0,45.678-38.92,65.402s-40.999,55.54-54.493,55.012C234.098,483.879,247.584,465.203,247.584,465.203z'/%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'lactose-free',
    label: 'Sans lactose',
    description: 'Sans lait ni produitsaitiers contenant du lactose.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%201.1//EN'%20'http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd'%3e%3csvg%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20version='1.1'%20id='Layer_1'%20width='512'%20height='512'%20viewBox='0%200%20512%20512'%20enable-background='new%200%200%20512%20512'%20xml:space='preserve'%3e%3cg%20id='milk_2'%3e%3cg%3e%3cpath%20fill='%23E5E8EC'%20d='M203.06,85.331V0.005H96.397v85.326c0,0-53.335,106.67-53.335,127.998c0,21.327,0,287.994,0,287.994%20c0,5.891,4.781,10.672,10.672,10.672H245.73c5.891,0,10.664-4.781,10.664-10.672c0,0,0-266.667,0-287.994%20C256.395,192.001,203.06,85.331,203.06,85.331z'/%3e%3c/g%3e%3cg%3e%3crect%20x='43.062'%20y='234.656'%20fill='%234FC0E8'%20width='213.332'%20height='191.996'/%3e%3c/g%3e%3cpath%20fill='%23CBD0D8'%20d='M213.731,501.323c-5.891,0-10.672,4.781-10.672,10.672h21.335%20C224.395,506.104,219.622,501.323,213.731,501.323z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M171.06,501.323c-5.89,0-10.664,4.781-10.664,10.672h21.335%20C181.732,506.104,176.951,501.323,171.06,501.323z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M128.397,501.323c-5.891,0-10.664,4.781-10.664,10.672h21.328%20C139.061,506.104,134.288,501.323,128.397,501.323z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M85.734,501.323c-5.891,0-10.672,4.781-10.672,10.672h21.335C96.397,506.104,91.624,501.323,85.734,501.323%20z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M203.06,54.004V32.66c-5.891,0-10.664,4.781-10.664,10.672S197.169,54.004,203.06,54.004z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M107.062,43.332c0-5.891-4.773-10.672-10.664-10.672v21.344C102.288,54.004,107.062,49.223,107.062,43.332z%20'/%3e%3cg%3e%3cpath%20fill='%23E5E8EC'%20d='M190.771,344.342c0,22.672-18.375,41.047-41.039,41.047c-22.671,0-41.046-18.375-41.046-41.047%20s31.398-68.404,41.046-68.404C159.381,275.938,190.771,321.67,190.771,344.342z'/%3e%3c/g%3e%3cg%3e%3cpolygon%20fill='%23CBD0D8'%20points='447.626,511.995%20298.284,511.995%20276.956,287.999%20468.938,287.999%20'/%3e%3c/g%3e%3cpolygon%20fill='%23E5E8EC'%20points='280.003,319.998%20298.284,511.995%20447.626,511.995%20465.906,319.998%20'/%3e%3cg%3e%3cpath%20fill='%23CBD0D8'%20d='M373.72,362.654c-5.874,0-10.655,4.781-10.655,10.672v85.326c0,5.891,4.781,10.672,10.655,10.672%20c5.906,0,10.672-4.781,10.672-10.672v-85.326C384.392,367.436,379.626,362.654,373.72,362.654z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M349.689,457.668l-0.031-0.094v-0.016l-7.968-85.232h-0.016c-0.5-5.422-5.062-9.672-10.609-9.672%20c-5.906,0-10.672,4.781-10.672,10.672c0,0.344,0.016,0.672,0.047,1l0,0v0.078v0.016l8,85.232l0,0%20c0.5,5.422,5.062,9.672,10.625,9.672c5.875,0,10.655-4.781,10.655-10.672c0-0.328-0.016-0.656-0.047-0.984H349.689z'/%3e%3cpath%20fill='%23CBD0D8'%20d='M427.001,374.326L427.001,374.326c0.031-0.328,0.062-0.656,0.062-1c0-5.891-4.781-10.672-10.672-10.672%20c-5.547,0-10.109,4.25-10.609,9.672l0,0l-8,85.232v0.016v0.094l0,0c-0.031,0.328-0.062,0.656-0.062,0.984%20c0,5.891,4.781,10.672,10.672,10.672c5.547,0,10.109-4.25,10.609-9.672l0,0l8-85.232v-0.016V374.326z'/%3e%3c/g%3e%3cg%20opacity='0.2'%3e%3cpath%20fill='%23FFFFFF'%20d='M203.06,85.331V0.005h-21.328v85.326c0,0,53.327,106.67,53.327,127.998c0,21.327,0,287.994,0,287.994%20c0,5.891-4.773,10.672-10.664,10.672h21.335c5.891,0,10.664-4.781,10.664-10.672c0,0,0-266.667,0-287.994%20C256.395,192.001,203.06,85.331,203.06,85.331z'/%3e%3c/g%3e%3cpath%20fill='%23CBD0D8'%20d='M213.731,10.661c0-5.891-4.781-10.656-10.672-10.656l0,0H96.397l0,0l0,0%20c-5.891,0-10.664,4.766-10.664,10.656c0,5.89,4.773,10.671,10.664,10.671l0,0l0,0H203.06l0,0%20C208.95,21.333,213.731,16.551,213.731,10.661z'/%3e%3cg%20opacity='0.2'%3e%3cpolygon%20fill='%23FFFFFF'%20points='447.626,287.999%20426.282,511.995%20447.626,511.995%20468.938,287.999%20'/%3e%3c/g%3e%3c/g%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'nut-free',
    label: 'Sans fruits à coque',
    description: 'Évite les noix, noisettes, amandes et autres fruits à coque.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%201024%201024'%20class='icon'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M477.717333%20165.098667c-73.834667%2020.352-147.285333%2061.568-209.728%20122.965333-130.538667%20128.384-172.416%20304.042667-114.538666%20431.594667%200.085333%200.256%200.042667%200.448%200.149333%200.704%2022.592%2058.176-7.36%20106.432%207.872%20121.386666%2015.210667%2014.954667%2076.074667-14.954667%20121.898667%207.061334%200.490667%200.256%200.832%200.170667%201.322666%200.362666%20129.770667%2057.557333%20308.885333%2016.533333%20439.722667-112.149333%2062.421333-61.461333%20104.32-133.717333%20124.992-206.314667L477.717333%20165.098667z'%20fill='%23E65100'%20/%3e%3cpath%20d='M548.458667%20461.269333l-0.170667-0.192c-117.290667-115.328-176.32-201.301333-175.509333-255.424%200.32-19.605333%208.448-29.205333%2011.946666-32.448%2091.2-89.706667%20215.872-70.570667%20341.568%2053.056a503.765333%20503.765333%200%200%201%2028.8%2030.698667c6.549333%204.8%2025.088%2018.837333%2046.677334%2040.064%20109.696%20107.904%20124.309333%20226.090667%2040.170666%20324.245333-3.349333%203.776-12.586667%2011.882667-31.68%2012.544-53.653333%201.898667-141.696-56.149333-261.76-172.544h-0.042666z'%20fill='%23FFA726'%20/%3e%3cpath%20d='M779.370667%20673.152c9.365333-12.885333%2017.770667-26.112%2025.621333-39.488-54.037333-1.792-140.181333-59.626667-256.490667-172.352l-0.042666-0.042667-0.170667-0.192c-115.072-113.173333-173.888-197.952-175.338667-252.224-13.568%207.722667-27.008%2015.978667-40.106666%2025.173334%2013.525333%2064.32%2073.237333%20147.072%20185.002666%20257.024%20137.536%20133.290667%20214.250667%20172.288%20261.525334%20182.101333z'%20fill='%23AA3304'%20/%3e%3cpath%20d='M252.778667%20751.978667c-39.189333-38.549333-80.874667-66.901333-116.117334-81.344%204.138667%2016.938667%209.685333%2033.365333%2016.789334%2049.024%200.085333%200.256%200.042667%200.448%200.149333%200.704%2022.592%2058.176-7.36%20106.432%207.872%20121.386666%2015.210667%2014.954667%2076.074667-14.954667%20121.898667%207.061334%200.490667%200.256%200.832%200.170667%201.322666%200.362666%2016.234667%207.189333%2033.216%2012.864%2050.773334%2016.981334-14.72-34.602667-43.52-75.626667-82.688-114.176z'%20fill='%23FB8C00'%20/%3e%3cpath%20d='M823.317333%20115.968a32.704%2032.704%200%200%200-45.653333%200%2031.402667%2031.402667%200%200%200%200%2044.885333c11.52%2011.349333%205.888%2044.373333-4.672%2059.584-19.306667%2023.594667-46.4%2042.218667-59.050667%2049.152a21.013333%2021.013333%200%200%200-4.736%2033.472c6.613333%206.506667%2017.066667%208.213333%2025.642667%203.498667%202.56-1.386667%2048.725333-27.008%2078.272-67.84h0.021333c40.362667-43.477333%2022.784-110.357333%2010.176-122.752z'%20fill='%23BF360C'%20/%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'egg-free',
    label: 'Sans œufs',
    description: 'Exclut les œufs et les préparations qui en contiennent.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%20128%20128'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--noto'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20d='M63.43%204.51c31.68-.07%2051.67%2051.46%2043.78%2084.12c-6.98%2028.92-27.03%2034.91-43.08%2034.91s-35.54-7.78-43.08-33.79C12.33%2059.63%2033.87%204.58%2063.43%204.51z'%20fill='%23f2a05b'%3e%3c/path%3e%3cpath%20d='M39.26%2029.03c-3.8%206.07-7.63%2013.71-8.45%2020.08c-1.5%2011.73%207.13%2013.05%2010.14%207.23s6.09-11.71%2010.98-19.52c6.76-10.79%2017.83-18.02%2012.39-22.71s-17.08%202.16-25.06%2014.92z'%20fill='%23fecc88'%3e%3c/path%3e%3cellipse%20cx='91.4'%20cy='43.58'%20rx='2.02'%20ry='2.06'%20fill='%23d47858'%3e%3c/ellipse%3e%3cellipse%20transform='rotate(-67.642%2091.663%2094.057)'%20cx='91.66'%20cy='94.05'%20rx='2.19'%20ry='2.18'%20fill='%23d47858'%3e%3c/ellipse%3e%3ccircle%20transform='rotate(-67.642%2032.913%20102.875)'%20cx='32.91'%20cy='102.87'%20fill='%23d47858'%20r='2.04'%3e%3c/circle%3e%3cellipse%20transform='rotate(-27.393%2097.64%2062.565)'%20cx='97.65'%20cy='62.57'%20rx='3.07'%20ry='2.7'%20fill='%23d47858'%3e%3c/ellipse%3e%3cellipse%20transform='rotate(-27.393%2068.515%2089.405)'%20cx='68.52'%20cy='89.41'%20rx='3.07'%20ry='2.7'%20fill='%23d47858'%3e%3c/ellipse%3e%3cellipse%20transform='rotate(-51.428%2064.796%20104.32)'%20cx='64.79'%20cy='104.32'%20rx='3.23'%20ry='2.7'%20fill='%23d47858'%3e%3c/ellipse%3e%3cellipse%20transform='rotate(-48.356%2052.395%20107.012)'%20cx='52.39'%20cy='107.01'%20rx='3.58'%20ry='3.46'%20fill='%23d47858'%3e%3c/ellipse%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'fish-free',
    label: 'Sans poisson',
    description: 'Exclut les poissons et produits dérivés.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='iso-8859-1'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20height='800px'%20width='800px'%20version='1.1'%20id='Layer_1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20viewBox='0%200%20496.158%20496.158'%20xml:space='preserve'%3e%3cpath%20style='fill:%2358D1DD;'%20d='M248.082,0.003C111.07,0.003,0,111.063,0,248.085c0,137.001,111.07,248.07,248.082,248.07%20c137.006,0,248.076-111.069,248.076-248.07C496.158,111.062,385.088,0.003,248.082,0.003z'/%3e%3cg%3e%3cpath%20style='fill:%23C14E00;'%20d='M298.486,105.655c-1.959-0.735-50.152-18.669-79.666-17.209%20c-18.338,0.888-31.063,20.819-31.599,21.667c-0.501,0.8-0.573,1.796-0.192,2.659c0.383,0.862,1.168,1.479,2.1,1.645l105.969,18.924%20c0.166,0.03,0.333,0.044,0.499,0.044c0.621,0,1.229-0.203,1.727-0.586c0.635-0.486,1.034-1.217,1.101-2.011l1.894-22.234%20C300.426,107.285,299.676,106.1,298.486,105.655z'/%3e%3cpath%20style='fill:%23C14E00;'%20d='M283.058,233.673l-13.998-31.527c-0.566-1.281-2.048-1.891-3.348-1.39l-50.24,19.25%20c-1.002,0.384-1.672,1.335-1.692,2.407c-0.021,1.071,0.608,2.049,1.592,2.473c1.312,0.563,32.471,13.788,65.386,12.488%20c0.107-0.005,0.211-0.016,0.316-0.033c0.746-0.121,1.41-0.558,1.814-1.208C283.35,235.392,283.414,234.471,283.058,233.673z'/%3e%3c/g%3e%3cpath%20style='fill:%23E85E00;'%20d='M397.451,226.358l-10.256-61.663l10.256-62.25c0.184-1.105-0.52-2.174-1.686-2.564%20s-2.475,0.006-3.141,0.949c-13.955,19.732-39.832,49.62-49.864,49.62c-8.78,0-29.989-11.148-39.31-18.193%20c-19.637-14.841-46.532-26.955-78.326-26.955c-57.056,0-123.479,31.723-126.447,58.422l38.072,14.726l-37.086-6.98%20c8.959,24.991,71.413,54.073,125.461,54.073c35.691,0,67.545-17.124,86.577-32.429c6.176-4.967,22.864-14.757,31.059-14.757%20c9.61,0,35.69,29.896,49.872,49.635c0.498,0.691,1.342,1.083,2.214,1.083c0.311,0,0.626-0.049,0.93-0.152%20C396.937,228.528,397.635,227.46,397.451,226.358z'/%3e%3ccircle%20style='fill:%2342423F;'%20cx='166.159'%20cy='142.019'%20r='11.852'/%3e%3ccircle%20style='fill:%23FFFFFF;'%20cx='169.229'%20cy='137.189'%20r='4.829'/%3e%3cg%3e%3cpath%20style='fill:%23C14E00;'%20d='M308.936,280.446c-0.535-0.848-13.261-20.779-31.599-21.667%20c-29.514-1.46-77.707,16.474-79.666,17.209c-1.189,0.445-1.939,1.63-1.831,2.898l1.894,22.234c0.066,0.794,0.466,1.524,1.101,2.011%20c0.497,0.383,1.105,0.586,1.727,0.586c0.166,0,0.333-0.014,0.499-0.044l105.968-18.924c0.932-0.165,1.717-0.782,2.1-1.645%20C309.51,282.242,309.437,281.246,308.936,280.446z'/%3e%3cpath%20style='fill:%23C14E00;'%20d='M280.685,390.34l-50.24-19.25c-1.3-0.501-2.781,0.108-3.348,1.39l-13.998,31.527%20c-0.355,0.798-0.291,1.719,0.17,2.46c0.404,0.65,1.068,1.087,1.814,1.208c0.105,0.017,0.209,0.028,0.316,0.033%20c32.915,1.3,64.074-11.924,65.386-12.488c0.983-0.423,1.612-1.402,1.592-2.473C282.357,391.675,281.687,390.724,280.685,390.34z'/%3e%3c/g%3e%3cpath%20style='fill:%23E85E00;'%20d='M397.48,334.056c-2.968-26.699-69.392-58.422-126.447-58.422c-31.794,0-58.689,12.114-78.326,26.955%20c-9.32,7.045-30.529,18.193-39.31,18.193c-10.032,0-35.909-29.888-49.864-49.62c-0.665-0.943-1.975-1.34-3.141-0.949%20s-1.869,1.459-1.686,2.564l10.256,62.25L98.706,396.69c-0.184,1.103,0.514,2.17,1.675,2.563c0.304,0.104,0.619,0.152,0.93,0.152%20c0.872,0,1.716-0.392,2.214-1.083c14.182-19.738,40.262-49.635,49.872-49.635c8.194,0,24.883,9.79,31.06,14.757%20c19.031,15.305,50.885,32.429,86.576,32.429c54.049,0,116.502-29.082,125.461-54.073l-37.086,6.98L397.48,334.056z'/%3e%3ccircle%20style='fill:%2342423F;'%20cx='329.999'%20cy='312.349'%20r='11.852'/%3e%3ccircle%20style='fill:%23FFFFFF;'%20cx='326.929'%20cy='307.519'%20r='4.829'/%3e%3c/svg%3e", import.meta.url).href,
    without: true
  },

  {
    value: 'shellfish-free',
    label: 'Sans fruits de mer',
    description: 'Exclut crustacés, mollusques et fruits de mer.',
    src: new URL("/appmenu-dist/assets/shellfish-free-B1Pmy8mP.svg", import.meta.url).href,
    without: true
  },
  {
    value: 'kid-friendly',
    label: 'Adapté aux enfants',
    description: 'Recettes simples et généralement appréciées des plus jeunes.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%2036%2036'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--twemoji'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20fill='%23C1694F'%20d='M25%2022h-.253c.16-.798.253-1.633.253-2.5c0-5.247-3.134-9.5-7-9.5s-7%204.253-7%209.5c0%20.867.093%201.702.253%202.5H11c-2%203-1.502%208.056.122%209C18%2035%2014%2029%2018%2029s0%206%206.878%202c1.624-.944%202.122-6%20.122-9z'%3e%3c/path%3e%3cpath%20fill='%23C1694F'%20d='M23.667%2020.458c-.177-1.544.317-3.562.255-5.25C22.535%2016.292%2019.947%2017%2018%2017s-4.51-.708-5.897-1.792c-.062%201.688.407%203.706.23%205.25c-.458%204%202.353%207.184%205.667%207.184s6.125-3.184%205.667-7.184z'%3e%3c/path%3e%3cpath%20fill='%23C1694F'%20d='M12.373%2014s-5%201-6%207s3.419%206.581%205%205c2-2%200-4%200-4s1-5%201-8zm11.254%200s5%201%206%207s-3.419%206.581-5%205c-2-2%200-4%200-4s-1-5-1-8z'%3e%3c/path%3e%3cpath%20fill='%23A0041E'%20d='M13%2013c-2%200-1%201-1%202s1%203%203%202c0%200-1%202-1%205c0%200%202%200%203%202c0%200%201-7%201-9s-4-2-5-2zm10%200c2%200%201%201%201%202s-1%203-3%202c0%200%201%202%201%205c0%200-2%200-3%202c0%200-1-7-1-9s4-2%205-2z'%3e%3c/path%3e%3ccircle%20fill='%23C1694F'%20cx='11'%20cy='4'%20r='4'%3e%3c/circle%3e%3ccircle%20fill='%23662113'%20cx='11'%20cy='4'%20r='2'%3e%3c/circle%3e%3ccircle%20fill='%23C1694F'%20cx='25'%20cy='4'%20r='4'%3e%3c/circle%3e%3ccircle%20fill='%23662113'%20cx='25'%20cy='4'%20r='2'%3e%3c/circle%3e%3cellipse%20fill='%23C1694F'%20cx='18'%20cy='8.5'%20rx='8'%20ry='7.5'%3e%3c/ellipse%3e%3ccircle%20fill='%23292F33'%20cx='15'%20cy='8'%20r='1'%3e%3c/circle%3e%3ccircle%20fill='%23292F33'%20cx='21'%20cy='8'%20r='1'%3e%3c/circle%3e%3cpath%20fill='%23D99E82'%20d='M18.058%209.563c-6.808%200-4.612%205.562-.147%205.562c4.464%200%206.955-5.562.147-5.562z'%3e%3c/path%3e%3cpath%20fill='%23292F33'%20d='M16.737%2011.065l.526.911a.851.851%200%200%200%201.474%200l.526-.911a.851.851%200%200%200-.737-1.277h-1.052a.851.851%200%200%200-.737%201.277z'%3e%3c/path%3e%3cpath%20fill='%23934035'%20d='M11.265%2027.002a.499.499%200%200%201-.269-.921a2.417%202.417%200%200%200%20.997-2.022c-.023-.991-.933-1.641-.942-1.646a.507.507%200%200%201-.213-.368c-.205-2.36.65-4.709.687-4.809a.5.5%200%200%201%20.938.346c-.008.021-.761%202.099-.644%204.165c.375.322%201.146%201.124%201.174%202.289c.044%201.91-1.398%202.851-1.459%202.89a.523.523%200%200%201-.269.076zm13.471%200a.5.5%200%200%201-.269-.078c-.062-.039-1.504-.979-1.46-2.89c.027-1.165.799-1.967%201.174-2.289c.118-2.072-.636-4.143-.644-4.164a.5.5%200%201%201%20.938-.347c.037.099.893%202.448.688%204.809a.502.502%200%200%201-.215.369c-.008.005-.918.654-.94%201.646a2.416%202.416%200%200%200%20.997%202.022a.5.5%200%200%201-.269.922zM24.665%2022h.01h-.01z'%3e%3c/path%3e%3cellipse%20transform='rotate(-45.001%2011.121%2030.5)'%20fill='%23C1694F'%20cx='11.122'%20cy='30.5'%20rx='4.5'%20ry='5'%3e%3c/ellipse%3e%3cpath%20fill='%23D99E82'%20d='M13.349%2033.227c-1.054%201.054-2.906.912-4.137-.318c-1.23-1.23-1.373-3.082-.318-4.137c1.054-1.054%202.906-.912%204.137.318c1.23%201.231%201.372%203.083.318%204.137z'%3e%3c/path%3e%3cpath%20fill='%23D99E82'%20d='M12.889%2032.768c-.781.781-2.206.623-3.182-.354c-.976-.976-1.135-2.401-.354-3.182c.781-.781%202.206-.623%203.182.354s1.135%202.401.354%203.182z'%3e%3c/path%3e%3cellipse%20transform='rotate(-45.001%2024.878%2030.5)'%20fill='%23C1694F'%20cx='24.878'%20cy='30.5'%20rx='5'%20ry='4.5'%3e%3c/ellipse%3e%3cpath%20fill='%23D99E82'%20d='M22.651%2033.227c1.054%201.054%202.906.912%204.137-.318c1.23-1.23%201.373-3.082.318-4.137c-1.054-1.054-2.906-.912-4.137.318c-1.23%201.231-1.372%203.083-.318%204.137z'%3e%3c/path%3e%3cellipse%20transform='rotate(-45.001%2024.878%2031)'%20fill='%23D99E82'%20cx='24.878'%20cy='31'%20rx='2.5'%20ry='2'%3e%3c/ellipse%3e%3cpath%20fill='%23292F33'%20d='M18.087%2014.138c-1.28%200-2.249-.947-2.264-.961a.25.25%200%200%201%20.353-.354c.075.074%201.849%201.797%203.647%200a.25.25%200%201%201%20.354.354c-.721.721-1.445.961-2.09.961z'%3e%3c/path%3e%3cpath%20fill='%23934035'%20d='M15.95%2029.727a.5.5%200%200%201-.449-.28c-2.274-4.635-6.795-3.15-6.986-3.086a.5.5%200%200%201-.326-.946c.055-.02%205.543-1.845%208.21%203.592a.5.5%200%200%201-.449.72zm4.101%200a.499.499%200%200%201-.46-.695c2.255-5.301%208.141-3.641%208.198-3.623a.499.499%200%200%201%20.339.619a.505.505%200%200%201-.619.341c-.207-.061-5.096-1.42-6.998%203.054a.502.502%200%200%201-.46.304z'%3e%3c/path%3e%3c/svg%3e", import.meta.url).href,
    without: false
  },
  {
    value: 'baby-friendly',
    label: 'Adapté aux bébés',
    description: 'Recettes simples et généralement appréciées des plus jeunes.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%2064%2064'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--emojione'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20d='M57%2027C57%2010%2045%202%2032%202S7%2010%207%2027c-3.5%200-5%202.5-5%205s1.5%205%205%205c0%206.7%207%2016%2025%2016s25-9.3%2025-16c3.5%200%205-2.5%205-5s-1.5-5-5-5'%20fill='%23d6a57c'%3e%3c/path%3e%3cpath%20d='M33%2015c-4-3-2-7-2-7s8%205%2015%203c0%200-4-1-5-4c0%200%204%201%207%200c-4-3-9-5-16-5S20%204%2016%207c4%205%208%208%2017%208'%20fill='%23594640'%3e%3c/path%3e%3cg%20fill='%23ff717f'%3e%3cpath%20d='M56%2037.4c-.4-2.4-3.3-3.9-6.6-3.2c-3.3.6-5.7%203.1-5.3%205.5c.4%202.4%203.3%203.9%206.6%203.2c3.2-.7%205.6-3.1%205.3-5.5'%20opacity='.5'%3e%3c/path%3e%3cpath%20d='M14.6%2034.1c-3.2-.6-6.2.9-6.6%203.3c-.4%202.4%202%204.9%205.3%205.5c3.3.6%206.3-.8%206.6-3.2c.4-2.5-2-4.9-5.3-5.6'%20opacity='.5'%3e%3c/path%3e%3c/g%3e%3cpath%20d='M26%2027.5c0%203.6-2.9%206.5-6.5%206.5S13%2031.1%2013%2027.5s2.9-6.5%206.5-6.5s6.5%202.9%206.5%206.5'%20fill='%23ffffff'%3e%3c/path%3e%3ccircle%20cx='19.5'%20cy='27.5'%20r='4.5'%20fill='%23664e27'%3e%3c/circle%3e%3cpath%20d='M21%2027.5c0%20.8-.7%201.5-1.5%201.5s-1.5-.7-1.5-1.5s.7-1.5%201.5-1.5s1.5.7%201.5%201.5'%20fill='%23231f20'%3e%3c/path%3e%3cpath%20d='M51%2027.5c0%203.6-2.9%206.5-6.5%206.5S38%2031.1%2038%2027.5s2.9-6.5%206.5-6.5s6.5%202.9%206.5%206.5'%20fill='%23ffffff'%3e%3c/path%3e%3ccircle%20cx='44.5'%20cy='27.5'%20r='4.5'%20fill='%23664e27'%3e%3c/circle%3e%3ccircle%20cx='44.5'%20cy='27.5'%20r='1.5'%20fill='%23231f20'%3e%3c/circle%3e%3cpath%20d='M47%2043.8C47%2052.1%2040.3%2056%2032%2056s-15-3.9-15-12.2c0-8.4%206.8-4.8%2015-4.8c8.3%200%2015-3.6%2015%204.8'%20fill='%2361c2ff'%3e%3c/path%3e%3cellipse%20cx='32'%20cy='47'%20rx='6'%20ry='5'%20fill='%236235a8'%3e%3c/ellipse%3e%3cpath%20d='M32%2046c-6.1%200-11-.7-11%205.2c0%206%204.9%2010.8%2011%2010.8s11-4.8%2011-10.7c0-6-4.9-5.3-11-5.3m0%2012.4c-4.3%200-8.3-2.9-8.3-7.1c0-4.2%204-3.2%208.3-3.2c4.3%200%208.3-1%208.3%203.2s-4%207.1-8.3%207.1'%20fill='%23fbbf67'%3e%3c/path%3e%3cpath%20d='M36%2048.8c0%20.6-.5%201.2-1.1%201.2h-5.8c-.6%200-1.1-.5-1.1-1.2v-2.7c0-.6.5-1.2%201.1-1.2h5.8c.6%200%201.1.5%201.1%201.2v2.7'%20fill='%2342ade2'%3e%3c/path%3e%3cpath%20d='M32%2038c-4.2%200-6.3-3-4.2-3h8.4c2.1%200%200%203-4.2%203'%20fill='%23b58360'%3e%3c/path%3e%3c/svg%3e", import.meta.url).href,
    without: false
  },
  {
    value: 'budget-friendly',
    label: 'Petit budget',
    description: 'Repas économiques avec des ingrédients accessibles.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='iso-8859-1'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20height='800px'%20width='800px'%20version='1.1'%20id='Layer_1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20viewBox='0%200%20512%20512'%20xml:space='preserve'%3e%3cpath%20style='fill:%23FF5023;'%20d='M497.308,213.927L298.407,14.915C288.946,5.454,276.369,0,263.012,0H50.532%20C36.73,0,23.93,5.788,14.915,14.915C5.788,24.042,0,36.73,0,50.532v212.48c0,13.357,5.454,25.934,14.915,35.395l198.901,198.901%20c9.461,9.461,22.094,14.692,35.45,14.692c13.468,0,26.073-5.231,35.534-14.692l212.493-212.48%20c9.461-9.461,14.699-22.038,14.699-35.506C511.993,235.965,506.769,223.388,497.308,213.927z%20M111.75,156.271%20c-11.91,0-23.04-4.675-31.499-13.023c-8.348-8.459-13.023-19.59-13.023-31.499c0-11.91,4.675-23.04,13.023-31.499%20c8.459-8.348,19.59-13.023,31.499-13.023s23.04,4.675,31.499,13.023c8.348,8.459,13.023,19.59,13.023,31.499%20s-4.675,23.04-13.023,31.499C134.79,151.597,123.659,156.271,111.75,156.271z'/%3e%3cpath%20style='fill:%23CD2A00;'%20d='M512,249.322c0,13.468-5.231,26.045-14.692,35.506L391.012,391.012L143.249,143.249%20c8.348-8.459,13.023-19.59,13.023-31.499c0-11.91-4.675-23.04-13.023-31.499c-8.459-8.348-19.59-13.023-31.499-13.023%20S88.71,71.903,80.25,80.25L15.137,14.915C24.153,5.788,36.73,0,50.532,0h212.48c13.357,0,25.934,5.454,35.395,14.915%20l198.901,198.901C506.769,223.277,512,235.965,512,249.322z'/%3e%3cpath%20style='fill:%23D1E7F8;'%20d='M362.168,312.086c-12.826,0-25.652-4.881-35.413-14.647c-19.527-19.522-19.527-51.299-0.006-70.832%20c19.533-19.533,51.316-19.533,70.837,0c19.533,19.527,19.533,51.305,0,70.832C387.826,307.206,375,312.086,362.168,312.086z%20M362.168,245.336c-4.272,0-8.549,1.625-11.804,4.881c-6.506,6.511-6.506,17.103,0,23.614c6.516,6.511,17.109,6.511,23.614,0%20c6.511-6.511,6.511-17.103,0-23.614C370.723,246.961,366.445,245.336,362.168,245.336z'/%3e%3cpath%20style='fill:%23FFFFFF;'%20d='M261.565,378.88c-9.223,0-16.696-7.473-16.696-16.696V161.837c0-9.223,7.473-16.696,16.696-16.696%20c9.223,0,16.696,7.473,16.696,16.696v200.348C278.261,371.407,270.788,378.88,261.565,378.88z'/%3e%3cpath%20style='fill:%23D1E7F8;'%20d='M261.565,145.141c-9.223,0-16.696,7.473-16.696,16.696v83.478l33.391,33.391v-116.87%20C278.261,152.614,270.788,145.141,261.565,145.141z'/%3e%3cpath%20style='fill:%23FFFFFF;'%20d='M161.82,312.086c-12.826,0-25.652-4.881-35.413-14.647c-19.527-19.522-19.527-51.299-0.006-70.832%20c19.533-19.533,51.316-19.533,70.837,0c19.533,19.527,19.533,51.305,0,70.832C187.478,307.206,174.652,312.086,161.82,312.086z%20M161.82,245.336c-4.272,0-8.549,1.625-11.804,4.881c-6.506,6.511-6.506,17.103,0,23.614c6.511,6.511,17.103,6.511,23.614,0%20c6.511-6.511,6.511-17.103,0-23.614C170.375,246.961,166.097,245.336,161.82,245.336z'/%3e%3c/svg%3e", import.meta.url).href,
    without: false
  },
  {
    value: 'batch-cooking',
    label: 'Batch cooking',
    description: 'Repas pouvant être préparés à l’avance pour plusieurs jours.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%2036%2036'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--twemoji'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20fill='%23292F33'%20d='M31%200H5a4%204%200%200%200-4%204v28a4%204%200%200%200%204%204h26a4%204%200%200%200%204-4V4a4%204%200%200%200-4-4z'%3e%3c/path%3e%3cpath%20fill='%23292F33'%20d='M14%2011a4%204%200%200%201-4%204H9a4%204%200%200%201-4-4V9a4%204%200%200%201%204-4h1a4%204%200%200%201%204%204v2z'%3e%3c/path%3e%3cpath%20fill='%23BE1931'%20d='M22%2027a2%202%200%200%201-2%202H5a2%202%200%200%201-2-2v-8a2%202%200%200%201%202-2h15a2%202%200%200%201%202%202v8zm11-8a2%202%200%200%200-2-2h-5a2%202%200%200%200-2%202v8a2%202%200%200%200%202%202h5a2%202%200%200%200%202-2v-8zM16%204a2%202%200%200%201%202-2h13a2%202%200%200%201%202%202v9a2%202%200%200%201-2%202H18a2%202%200%200%201-2-2V4zm-2%209a2%202%200%200%201-2%202H5a2%202%200%200%201-2-2V4a2%202%200%200%201%202-2h7a2%202%200%200%201%202%202v9z'%3e%3c/path%3e%3cpath%20fill='%23DD2E44'%20d='M14%2013a2%202%200%200%201-2%202H5a2%202%200%200%201-2-2V7a2%202%200%200%201%202-2h7a2%202%200%200%201%202%202v6zm19%200a2%202%200%200%201-2%202H18a2%202%200%200%201-2-2V7a2%202%200%200%201%202-2h13a2%202%200%200%201%202%202v6z'%3e%3c/path%3e%3cpath%20fill='%23E1E8ED'%20d='M22%2027a2%202%200%200%201-2%202H5a2%202%200%200%201-2-2v-5a2%202%200%200%201%202-2h15a2%202%200%200%201%202%202v5z'%3e%3c/path%3e%3cpath%20fill='%23DD2E44'%20d='M33%2027a2%202%200%200%201-2%202h-5a2%202%200%200%201-2-2v-5a2%202%200%200%201%202-2h5a2%202%200%200%201%202%202v5z'%3e%3c/path%3e%3cpath%20fill='%23FFAC33'%20d='M27.015%209.24c1.315%201.478%201.908%203.098%201.322%203.619l-1.059.941c-.585.522-2.126-.255-3.44-1.733l-3.571-4.013c-1.314-1.479-1.907-3.099-1.321-3.62l1.059-.941c.586-.521%202.124.255%203.438%201.733l3.572%204.014z'%3e%3c/path%3e%3cpath%20fill='%23FFD983'%20d='M22.28%2011.076c-.598%201.539-1.477%202.633-1.961%202.444l-.877-.341c-.485-.188-.393-1.588.205-3.126l1.625-4.178c.599-1.538%201.477-2.632%201.962-2.444l.876.341c.485.188.394%201.588-.205%203.126l-1.625%204.178z'%3e%3c/path%3e%3cpath%20fill='%23FFD983'%20d='M23.113%2011.536c-1.562.535-2.965.568-3.133.076l-.305-.891c-.169-.492.962-1.324%202.522-1.858l4.242-1.45c1.562-.534%202.964-.567%203.133-.075l.304.89c.169.493-.961%201.325-2.523%201.858l-4.24%201.45z'%3e%3c/path%3e%3cpath%20fill='%235C913B'%20d='M6.442%207.873c.373-4.106%204.907-.961%207.135%201.049c-1.34%201.485-7.586%203.93-7.135-1.049z'%3e%3c/path%3e%3cpath%20fill='%235C913B'%20d='M6.584%207.784c3.739-1.737%203.293%203.763%202.672%206.698c-1.956-.414-7.206-4.59-2.672-6.698z'%3e%3c/path%3e%3cpath%20fill='%2377B255'%20d='M5%208c-1.2-1.6%201-5%206-5c1%201-3%209-6%205z'%3e%3c/path%3e%3cpath%20fill='%23B2C2CC'%20d='M6%2027a.999.999%200%200%201-.707-1.707l1-1a.999.999%200%201%201%201.414%201.414l-1%201A.997.997%200%200%201%206%2027zm12.417.499a.997.997%200%200%201-.707-.293l-1-1a1%201%200%201%201%201.415-1.414l1%201a.999.999%200%200%201-.708%201.707zm-6.167-4.291c-.255%200-.511-.098-.707-.292l-.459-.458a1%201%200%201%201%201.413-1.416l.459.458a1%201%200%200%201-.706%201.708z'%3e%3c/path%3e%3cpath%20fill='%23B26304'%20d='M32.149%2023.412a1.999%201.999%200%200%201-.257%202.816l-2.305%201.921a1.999%201.999%200%200%201-2.816-.257l-1.921-2.305a1.999%201.999%200%200%201%20.257-2.816l2.305-1.921a1.999%201.999%200%200%201%202.816.257l1.921%202.305z'%3e%3c/path%3e%3cpath%20fill='%23F4900C'%20d='M32.149%2021.412a1.999%201.999%200%200%201-.257%202.816l-2.305%201.921a1.999%201.999%200%200%201-2.816-.257l-1.921-2.305a1.999%201.999%200%200%201%20.257-2.816l2.305-1.921a1.999%201.999%200%200%201%202.816.257l1.921%202.305z'%3e%3c/path%3e%3ccircle%20fill='%23CCD6DD'%20cx='12'%20cy='26'%20r='1'%3e%3c/circle%3e%3ccircle%20fill='%23CCD6DD'%20cx='20'%20cy='22'%20r='1'%3e%3c/circle%3e%3c/svg%3e", import.meta.url).href,
    without: false
  },
  {
    value: 'quick-meals',
    label: 'Repas rapides',
    description: 'Recettes prêtes en moins de 30 minutes.',
    src: new URL("data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Uploaded%20to:%20SVG%20Repo,%20www.svgrepo.com,%20Generator:%20SVG%20Repo%20Mixer%20Tools%20--%3e%3csvg%20width='800px'%20height='800px'%20viewBox='0%200%20128%20128'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20aria-hidden='true'%20role='img'%20class='iconify%20iconify--noto'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cpath%20d='M116.46%204.14h-104c-4.42%200-8%203.58-8%208v104c0%204.42%203.58%208%208%208h104c4.42%200%208-3.58%208-8v-104c0-4.42-3.58-8-8-8z'%20fill='%23f77e00'%3e%3c/path%3e%3cpath%20d='M110.16%204.14h-98.2a7.555%207.555%200%200%200-7.5%207.5v97.9c-.01%204.14%203.34%207.49%207.48%207.5H110.06c4.14.01%207.49-3.34%207.5-7.48V11.64c.09-4.05-3.13-7.41-7.18-7.5h-.22z'%20fill='%23ff9800'%3e%3c/path%3e%3cpath%20d='M40.16%2013.04c0-2.3-1.6-3-10.8-2.7c-7.7.3-11.5%201.2-13.8%204s-2.9%208.5-3%2015.3c0%204.8%200%209.3%202.5%209.3c3.4%200%203.4-7.9%206.2-12.3c5.4-8.7%2018.9-10.6%2018.9-13.6z'%20opacity='.75'%20fill='%23ffbd52'%3e%3c/path%3e%3cpath%20d='M107.46%2062.14l-39.9-25.7c-.98-.58-2.24-.26-2.82.72c-.23.39-.33.84-.28%201.28v22.3l-37-24.3c-1.4-.9-3%20.2-3%202v51.4c0%201.8%201.7%202.9%203%202l37-24.4v22.3c-.13%201.13.69%202.15%201.82%202.28c.45.05.9-.05%201.28-.28l39.9-25.6c1.1-.76%201.38-2.28.62-3.38c-.17-.25-.38-.46-.62-.62z'%20fill='%23fafafa'%3e%3c/path%3e%3cpath%20d='M107.36%2066.14l-39.8%2025.7c-.35.24-.78.34-1.2.3a.632.632%200%200%201-.4-.1c-.1%200-.2-.1-.4-.2c-.24-.11-.45-.28-.6-.5c-.12-.1-.2-.24-.2-.4c-.2-.33-.31-.71-.3-1.1v-22.4l-3%202l-34%2022.4c-.35.24-.78.34-1.2.3a.632.632%200%200%201-.4-.1c-.1%200-.2-.1-.4-.2c-.7-.46-1.12-1.26-1.1-2.1v-51.3c-.08-1%20.54-1.91%201.5-2.2c.1%200%20.2-.1.4-.1c.42-.04.85.06%201.2.3l34%2022.4l3%202v-22.4c0-.42.11-.83.3-1.2c.1-.1.1-.2.2-.4c.17-.2.38-.36.6-.5c.1-.1.2-.1.4-.2s.3-.1.4-.1c.42-.04.85.06%201.2.3l39.8%2025.8c1.1.76%201.38%202.28.62%203.38c-.17.24-.38.45-.62.62z'%20opacity='.2'%20fill='%23ffffff'%3e%3c/path%3e%3c/svg%3e", import.meta.url).href,
    without: false
  }
  


];

const MenuList = [ 
        { "name": "lasagna", 
          "label": "Lasagnes à la bolognaise", 
          "source": new URL("/appmenu-dist/assets/lasagnes-Dz9peVO3.jpg", import.meta.url), 
          "creationType": "suggestion",
          "servings": 4,
          "category": "Plats",
          "diets": [
            "pork-free",
            "nut-free",
            "egg-free",
            "fish-free",
            "shellfish-free",
            "kid-friendly",
            "batch-cooking"
          ],
          "ingredients": [
            {
              "name": "Sauce tomate",
              "quantity": 600,
              "unit": "g",
              "storePackageQuantity": 200,
              "storePackageUnit": "g",
              "storePackagePrice": 1.80,
              "price": 5.40
            },
            {
              "name": "Pâtes à lasagne",
              "quantity": 500,
              "unit": "g",
              "storePackageQuantity": 500,
              "storePackageUnit": "g",
              "storePackagePrice": 1.80,
              "price": 1.80
            },
            {
              "name": "Emmental râpé",
              "quantity": 200,
              "unit": "g",
              "storePackageQuantity": 1000,
              "storePackageUnit": "g",
              "storePackagePrice": 2.0,
              "price": 0.40
            },
            {
              "name": "Viande hachée",
              "quantity": 500,
              "unit": "g",
              "storePackageQuantity": 1000,
              "storePackageUnit": "g",
              "storePackagePrice": 5.0,
              "price": 2.50
            },
            {
              "name": "Oignon",
              "quantity": 1,
              "unit": "pièce",
              "storePackageQuantity": 1,
              "storePackageUnit": "pièce",
              "storePackagePrice": 0.5,
              "price": 0.5
            },
            {
              "name": "Farine",
              "quantity": 50,
              "unit": "g",
              "storePackageQuantity": 1000,
              "storePackageUnit": "g",
              "storePackagePrice": 0.2,
              "price": 0.01
            },
            {
              "name": "Beurre",
              "quantity": 50,
              "unit": "g",
              "storePackageQuantity": 1000,
              "storePackageUnit": "g",
              "storePackagePrice": 0.3,
              "price": 0.015
            },
            {
              "name": "Lait",
              "quantity": 500,
              "unit": "ml",
              "storePackageQuantity": 1000,
              "storePackageUnit": "ml",
              "storePackagePrice": 0.8,
              "price": 0.4
            },
          ],
        },
        {
          "name": "salade-sardines",
          "label": "Salade de sardines",
          "source": new URL("/appmenu-dist/assets/salade-sardines-DKto7Ebe.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",          
          "diets": [
            "pork-free",
            "gluten-free",
            "lactose-free",
            "nut-free",
            "egg-free",
            "budget-friendly",
            "shellfish-free",
          ],
          "ingredients": [],
        },
                {
          "name": "Aucune-selection",
          "label": "Aucune sélection",
          "source": new URL("/appmenu-dist/assets/aucune-selection-CT10A5Cq.png", import.meta.url),
          "creationType": "none",
          "servings": 0,
          "category": "none",          
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "pates-bolognaise",
          "label": "Pâtes à la bolognaise",
          "source": new URL("/appmenu-dist/assets/pates-bolognaise-B6jI5SD2.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
            "pork-free",
            "shellfish-free",

          ],
          "ingredients": [],
        },
        {
          "name": "raclette-porc",
          "label": "Raclette de pommes de terre et porc",
          "source": new URL("/appmenu-dist/assets/raclette-porc-Bt4OqzMP.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "riz-lentilles-porc",
          "label": "Riz aux lentilles et au porc",
          "source": new URL("/appmenu-dist/assets/riz-lentilles-porc-xoySOZda.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "pâtes-jambon-fromage",
          "label": "Pâtes au jambon et au fromage",
          "source": new URL("/appmenu-dist/assets/pates-jambon-fromage-BBRlbqWG.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "semoule-legumes-merguez",
          "label": "Semoule aux légumes et merguez",
          "source": new URL("/appmenu-dist/assets/semoule-legumes-merguez-BY4hgSYY.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "frites-nuggets",
          "label": "Frites et nuggets",
          "source": new URL("/appmenu-dist/assets/frites-nuggets-p7gHFcyO.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "salade-carottes-thon",
          "label": "Salade de carottes et thon",
          "source": new URL("/appmenu-dist/assets/salade-carottes-thon-Bv68pJ7U.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "chile",
          "label": "Chili con carne",
          "source": new URL("/appmenu-dist/assets/chile-By13-t7w.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "gratin-pommes-terre-brocolis",
          "label": "Gratin de pommes de terre et brocolis",
          "source": new URL("/appmenu-dist/assets/gratin-pommes-terre-brocolis-BctOZA0z.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "rougail-saucisses",
          "label": "Rougail de saucisses",
          "source": new URL("/appmenu-dist/assets/rougail-saucisses-jQAWXDUG.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "nouilles-sautees-poulet",
          "label": "Nouilles sautées au poulet",
          "source": new URL("/appmenu-dist/assets/nouilles-sautees-poulet-DBDar8UT.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "epinards-accras-riz",
          "label": "Épinards, accras et riz",
          "source": new URL("/appmenu-dist/assets/epinards-accras-riz-CdpTIGRH.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "sandwich-jambon-fromage",
          "label": "Sandwich au jambon et au fromage",
          "source": new URL("/appmenu-dist/assets/sandwich-jambon-fromage-bHZ8Ab43.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "salade-cesar",
          "label": "Salade César",
          "source": new URL("/appmenu-dist/assets/salade-cesar-BYQ-fbFX.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "pizza-jambon-fromage",
          "label": "Pizza au jambon et au fromage",
          "source": new URL("/appmenu-dist/assets/pizza-jambon-fromage-Ddv6TNYy.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "pizza-bolognaise",
          "label": "Pizza Bolognaise",
          "source": new URL("/appmenu-dist/assets/pizza-bolognaise-CSODe3RY.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "gratin-chou-fleur",
          "label": "Gratin de chou-fleur",
          "source": new URL("/appmenu-dist/assets/gratin-chou-fleur-DeeXv_hI.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",  
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "riz-haricots-rouges-poulet",
          "label": "Riz aux haricots rouges et poulet",
          "source": new URL("/appmenu-dist/assets/riz-haricots-rouges-poulet-BIUObSIi.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "haricots-verts-poulet-riz",
          "label": "Haricots verts, poulet et riz",
          "source": new URL("/appmenu-dist/assets/haricots-verts-poulet-riz-Cl4NxR8Z.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "burger-frites",
          "label": "Burger et frites",
          "source": new URL("/appmenu-dist/assets/burger-frites-dlMDArva.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "salade-de-pommes-de-terre",
          "label": "Salade de pommes de terre",
          "source": new URL("/appmenu-dist/assets/salade-de-pommes-de-terre-tfEl8nrv.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "pizza-raclette",
          "label": "Pizza à la raclette",
          "source": new URL("/appmenu-dist/assets/pizza-raclette-Czvp8CGs.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "hot-dog",
          "label": "Hot Dog",
          "source": new URL("/appmenu-dist/assets/hot-dog-6LYECkot.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Plats",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "omelette-pain-beurre",
          "label": "Omelette et pain au beurre",
          "source": new URL("/appmenu-dist/assets/omelette-pain-beurre-BBC6qvjj.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Petits-déjeuners",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "biscuit-smarties-pommes",
          "label": "Biscuit Smarties et pommes",
          "source": new URL("/appmenu-dist/assets/biscuit-smarties-pommes-CFLwpgKF.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Collations",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "glace-caramel-beurre-sale",
          "label": "Glace caramel au beurre salé",
          "source": new URL("/appmenu-dist/assets/glace-caramel-beurre-sale-BIG6ZXMr.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Desserts",
          "diets": [
          ],
          "ingredients": [],
        },
        {
          "name": "milkshake-oreo",
          "label": "Milkshake Oreo",
          "source": new URL("/appmenu-dist/assets/milkshake-oreo-BuIa1xSj.jpg", import.meta.url),
          "creationType": "suggestion",
          "servings": 2,
          "category": "Desserts",
          "diets": [
          ],
          "ingredients": [],
        },

      ];

const API_URL = "https://app-simplifie-backend.onrender.com/api";
async function http(path, options = {}) {
  const token = localStorage.getItem("token");
  const res = await fetch(`${API_URL}${path}`, {
    // Ajoute les autres options de la requête (méthode, corps, etc.) en écrasant les valeurs par défaut si nécessaire
    ...options,
    headers: {
      "Content-Type": "application/json",
      //boolean && objet retourne objet donc ici => Si un token est présent, l'ajoute à l'en-tête Authorization (les points: ...(null) => renvoit rien si token est null)
      ...token && { Authorization: `Bearer ${token}` },
      // Ajoute les en-têtes supplémentaires fournis dans les options, en écrasant les en-têtes par défaut si nécessaire
      ...options.headers || {}
    }
  });
  if (!res.ok) {
    const error = await res.json().catch(() => ({}));
    throw new Error(error.message || "API Error");
  }
  return res.json();
}

// GET /menus
const getMenusList = () => {
  return http('/menus') // utilise uniquement le paramètre path de la fonction http
};

// POST /menus
const createMenusList = (data) => {
  return http('/menus', { // utilise le paramètre path 
    //et options de la fonction http pour spécifier la méthode et le corps de la requête
    method: 'POST', 
    body: JSON.stringify(data)
  })
};

// PATCH /menus/:id
const updateMenuList = (id, data) => {
  return http(`/menus/${id}`, { // utilise le paramètre path pour spécifier l'ID du menu à mettre à jour
    //et options de la fonction http pour spécifier la méthode et le corps de la requête
    method: 'PATCH',
    body: JSON.stringify(data)
  })
};

// DELETE /menus/:id
const deleteMenusList = (id) => {
  return http(`/menus/${id}`, { // utilise le paramètre path pour spécifier l'ID du menu à supprimer
    //et options de la fonction http pour spécifier la méthode
    method: 'DELETE'
  })
};

const {defineStore} = await importShared('pinia');

const {ref: ref$8,computed: computed$8} = await importShared('vue');


const useMenuStore = defineStore('menu', () => {
    const suggestedMenus = ref$8(MenuList); // Stocke les suggestions de menus basées sur les préférences de l'utilisateur
    const menusLists = ref$8([]); // Stocke les menu de l'utilisateur
    const selectedMenusList = ref$8(null); // Stocke du menu actuellement sélectionnée pour affichage ou modification
    const creatingMenusList = ref$8(false); // Indique si l'utilisateur est en train de créer une nouvelle menu
    const title = ref$8(''); // Stocke le titre du menu en cours de création ou de modification
    const dietTypes = ref$8([]); // Stocke les types de régimes choisis par l'utilisateur pour le menu en cours de création ou de modification
    const showMenusListResults = ref$8(false); // Indique si les résultats du menu sélectionnée doivent être affichés (pour mobile)
    const showMenusListSelection = ref$8(false); // Indique si la sélection du menu doit être affichée (pour mobile)
    const hiddenCategories = ref$8(selectedMenusList.value?.displayCategory ?? ['Petits-déjeuners', 'Entrées', 'Plats', 'Desserts', 'Collations']); // Stocke les catégories de menus que l'utilisateur a choisi de masquer dans les résultats du menu sélectionnée
    const showDeleteConfirmationMenusList = ref$8(false); // Indique si la confirmation de suppression d'un menu doit être affichée
    const showDeleteConfirmationMenu = ref$8(false); // Indique si la confirmation de suppression d'un menu doit être affichée
    const toDeleteId = ref$8(null); // Stocke l'ID du menu à supprimer
    const toDeleteName = ref$8(''); // Stocke le nom du menu à supprimer
    const toDeleteImg = ref$8(''); // Stocke l'image du menu à supprimer
    const countMenuByCategory = ref$8({});
    const selectedDay = ref$8(null); // Stocke le jour sélectionné pour l'affichage du menu (pour mobile)   
    const selectedCategory = ref$8(null); // Stocke la catégorie sélectionnée pour l'ajout de menu (pour mobile)
//----------------------------- Les computed ---------------------------------------------------  
    const ingredientSuggestions = computed$8(() => {
        const ingredients = new Map();// Utilise une Map pour éviter les doublons d'ingrédients

        menusLists.value.forEach(menu => {
            menu.items.forEach(item => {
            if (item.ingredientOverwritten && item.ingredientOverwritten.length > 0) {
                item.ingredientOverwritten.forEach(ingredient => {
                    ingredients.set(
                        ingredient.name,//je récupère le nom de l'ingrédient pour l'utiliser comme clé dans la Map afin d'éviter les doublons
                        ingredient //j'ajoute ces propiétés
                    );
                });
            }
            });
        });
        // Convertit la Map en tableau pour l'affichage des suggestions d'ingrédients classé par ordre alphabétique
        return Array.from(ingredients.values()).sort((a, b) => a.name.localeCompare(b.name)
        )
    });
    const weeksInMonth = computed$8(() => {
    const currentDate = new Date();// Récupère la date actuelle
    const currentMonth = currentDate.getMonth();// Récupère le mois en cours (0-11)
    const currentYear = currentDate.getFullYear();// Récupère l'année en cours
    const firstDayOfMonth = new Date(currentYear, currentMonth, 1);// Récupère le premier jour du mois
    const lastDayOfMonth = new Date(currentYear, currentMonth + 1, 0);// Récupère le dernier jour du mois
    const weeks = [];// Initialise un tableau pour stocker les semaines du mois
    
    let currentWeek = [];// Initialise un tableau pour stocker les jours de la semaine en cours
    for (let day = firstDayOfMonth; day <= lastDayOfMonth; day.setDate(day.getDate() + 1)) {
        currentWeek.push(new Date(day)); // Ajoute une copie de l'objet Date pour éviter les problèmes de référence
        if (day.getDay() === 0) { // Si c'est dimanche, termine la semaine
        weeks.push(currentWeek);// Ajoute la semaine complète au tableau des semaines
        currentWeek = [];// Réinitialise le tableau pour la semaine suivante
        }
    }
    if (currentWeek.length > 0) { // Ajoute la dernière semaine si elle n'est pas vide
        weeks.push(currentWeek);// Ajoute la semaine incomplète au tableau des semaines
    }
    return weeks;
    });
    const shoppingList = computed$8(() => {
        const ingredientsMap = new Map();// Utilise une Map pour éviter les doublons d'ingrédients et pour agréger les quantités
        selectedMenusList.value.items.forEach(menu => {//pour chaque menu du menuList sélectionnée
            menu.ingredientOverwritten?.forEach(ingredient => {//pour chaque ingrédient personnalisé du menu
                const existing = ingredientsMap.get(// Vérifie si l'ingrédient existe déjà dans la Map en utilisant son nom comme clé
                    ingredient.name
                );
                if (existing) {
                    existing.quantity += Number(// Si l'ingrédient existe déjà, ajoute la quantité à celle existante
                    ingredient.quantity
                    );
                } else {
                    ingredientsMap.set(
                    ingredient.name,
                    {
                        ...ingredient
                    }
                    );
                }
             });
        });
        return Array.from(ingredientsMap.values())
    });
    const shoppingListWithPackages = computed$8(() => {
    return shoppingList.value.map(// Pour chaque ingrédient de la liste de courses
        ingredient => {
            const packagesNeeded = Math.ceil(ingredient.quantity /ingredient.storePackageQuantity); // Calcule le nombre de paquets nécessaires en arrondissant à l'entier supérieur
            const totalPrice = packagesNeeded * ingredient.price; // Calcule le prix total en multipliant le nombre de paquets nécessaires par le prix unitaire de l'ingrédient
        return {
            ...ingredient,
            packagesNeeded,
            totalPrice: totalPrice.toFixed(2) // Ajoute les propriétés packagesNeeded et totalPrice à l'objet ingrédient, en arrondissant le prix total à 2 décimales
        }
        }
    )
    });
    const shoppingByWeek = computed$8(() => {

    return weeksInMonth.value.map(// Pour chaque semaine du mois
        (week, weekIndex) => {

            const startDay = week[0].getDate();// Récupère le jour du mois du premier jour de la semaine
            const endDay = week[week.length - 1].getDate();//Récupère le jour du mois du dernier jour de la semaine
            // Filtre les menus du menuList sélectionnée pour ne garder que ceux qui sont compris entre le premier et le dernier jour de la semaine
            const menusInWeek =
            selectedMenusList.value.items.filter(
                menu =>
                menu.dayIndex >= startDay &&
                menu.dayIndex <= endDay
            );
            
            const ingredientsMap = new Map();// Utilise une Map pour éviter les doublons d'ingrédients et pour agréger les quantités

            menusInWeek.forEach(menu => {//pour chaque menu de la semaine

                menu.ingredientOverwritten?.forEach(//pour chaque ingrédient personnalisé du menu
                    ingredient => {
                        const existing = ingredientsMap.get(ingredient.name); // Vérifie si l'ingrédient existe déjà dans la Map en utilisant son nom comme clé

                        if (existing) {// Si l'ingrédient existe déjà, ajoute la quantité à celle existante
                            existing.quantity += Number(ingredient.quantity);
                        } else {
                            ingredientsMap.set(ingredient.name,{...ingredient});// Si l'ingrédient n'existe pas encore, ajoute-le à la Map avec son nom comme clé et toutes ses propriétés
                        }
                    }
                );
            });

        const ingredients =
            Array.from(
            ingredientsMap.values()
                ).map(
                ingredient => {

                    const packagesNeeded =
                    Math.ceil(
                        ingredient.quantity /
                        ingredient.storePackageQuantity
                    );

                    const totalPrice =
                    packagesNeeded *
                    ingredient.price;

                    return {
                    ...ingredient,
                    packagesNeeded,
                    totalPrice: totalPrice.toFixed(2)
                    }

                }
                );

        return {

            week: weekIndex + 1,

            ingredients,

            articles:
            ingredients.reduce(
                (total, ingredient) =>
                total +
                ingredient.packagesNeeded,
                0
            ),

            totalPrice:
            Number(
                ingredients.reduce(
                (total, ingredient) =>
                    total +
                    ingredient.totalPrice,
                0
                ).toFixed(2)
            )

        }

        }
    )

    });
    //----------------------------- Les fonctions ---------------------------------------------------   

    
    //fonction pour intialiser les menus
    const initializeMenusList = async () => {
        try {
            const res = await getMenusList(); // Appelle la fonction de l'API pour récupérer les menus depuis la base de données
            menusLists.value = res.data; // Stocke les menus récupérées dans la variable reactive
          console.log('Menus initialisées:', res.data); // Affiche les menus initialisées dans la console pour vérification
        } catch (error) {
            console.error('Erreur lors de l\'initialisation des menus:', error);
        }
    };
 
    // Fonction pour créer une nouveau menu
    const createMenusList$1 = async () => {
        if (!title.value.trim()) return // Ne pas créer de menu si le titre est vide
        try {
            const newMenu = { 
                title: title.value,
                diets: dietTypes.value,
                items: [],
                displayCategory: ['Petits-déjeuners', 'Entrées', 'Plats', 'Desserts', 'Collations'], // Par défaut, toutes les catégories sont affichées
                typeView: 'multi'
                }; // Crée un objet menu
            title.value = ''; // Réinitialiser le champ de titre
            dietTypes.value = []; // Réinitialiser les types de régimes
            creatingMenusList.value = false; // Sortir du mode création
            //j'envois à la bdd
            const res = await createMenusList(newMenu); // Appelle la fonction de l'API pour créer une nouvelle menu dans la base de données
            menusLists.value.push(res.data); // Ajouter la nouvelle menu à la liste des menus
           console.log('Nouveau menu créé:', res.data); // Affiche la nouvelle menu dans la console pour vérification
        } catch (error) {
            console.error('Erreur lors de la création du menu:', error);
        }
    };

    const selectMenusList = (menuId) => {
        console.log('la', menuId);
        selectedMenusList.value = menusLists.value.find(t => t._id === menuId) || null; // Trouve du menu par son ID et la stocke dans selectedMenusList
        showMenusListResults.value = true; // Affiche les résultats du menu sélectionnée (pour mobile)
        hiddenCategories.value = selectedMenusList.value?.displayCategory || ['Petits-déjeuners', 'Entrées', 'Plats', 'Desserts', 'Collations']; // Met à jour les catégories masquées en fonction du menu sélectionnée
    };   

    const editMenusListTitle = async (event, menuId, newTitle) => {
        event.stopPropagation(); // Empêche la propagation de l'événement de clic pour éviter de sélectionner du menu avant de la modifier
        const menu = menusLists.value.find(t => t._id === menuId); // Trouve du menu à modifier par son ID
        if (menu) {
            menu.title = newTitle; // Met à jour le titre du menu
            try {
                await updateMenuList(menuId, { title: newTitle }); // Appelle la fonction de l'API pour mettre à jour le titre du menu dans la base de données
                console.log('Titre du menu mis à jour:', newTitle); // Affiche le nouveau titre dans la console pour vérification
            } catch (error) {
                console.error('Erreur lors de la mise à jour du titre du menu:', error);
            }
        }
    };  

    const updateMenusListTypeView = async (menuId, newTypeView) => {
        const menu = menusLists.value.find(t => t._id === menuId); // Trouve du menu à modifier par son ID
        if (menu) {
            menu.typeView = newTypeView; // Met à jour le type d'affichage du menu
            try {
                const res = await updateMenuList(menuId, { typeView: newTypeView }); // Appelle la fonction de l'API pour mettre à jour le type d'affichage du menu dans la base de données
                console.log('Type d\'affichage du menu mis à jour:', res.data); // Affiche le nouveau type d'affichage dans la console pour vérification
            } catch (error) {
                console.error('Erreur lors de la mise à jour du type d\'affichage du menu:', error);
            }
        }
    };

     const updateMenusListcategorySelected = async (menuId, newCategorySelected) => {
        const menu = menusLists.value.find(t => t._id === menuId); // Trouve du menu à modifier par son ID
        if (menu) {
            menu.displayCategory = newCategorySelected; // Met à jour la catégorie sélectionnée du menu
            console.log('la',newCategorySelected, menu);
            try {
                const res = await updateMenuList(menuId, { displayCategory: newCategorySelected }); // Appelle la fonction de l'API pour mettre à jour la catégorie sélectionnée du menu dans la base de données
                console.log('Catégorie sélectionnée du menu mise à jour:', res.data); // Affiche la nouvelle catégorie sélectionnée dans la console pour vérification
            } catch (error) {
                console.error('Erreur lors de la mise à jour de la catégorie sélectionnée du menu:', error);
            }
        }
    };

    const deleteMenusList$1 = async(MenuListId) => {
        menusLists.value = menusLists.value.filter(t => t._id !== MenuListId); // Supprime du menu de la liste des menus
        try {
            await deleteMenusList(MenuListId); // Appelle la fonction de l'API pour supprimer du menu de la base de données
        } catch (error) {
            console.error('Erreur lors de la suppression du menu:', error);
        }
        showDeleteConfirmationMenusList.value = false; // Ferme la confirmation de suppression
        toDeleteId.value = null;
        toDeleteName.value = '';
        toDeleteImg.value = '';
        if (selectedMenusList.value && selectedMenusList.value._id === MenuListId) {
            selectedMenusList.value = null; // Si du menu supprimée était sélectionnée, réinitialise la sélection
            showMenusListResults.value = false; // Masque les résultats du menu sélectionnée (pour mobile)
        }

    }; 


// fonctions pour les menus à l'intérieur d'un menuList

    //ajouter un menu à du menuList sélectionnée
    const addMenu = async (newMenu) => {
        if (selectedMenusList.value) {
            const menuToAdd = {
                ...newMenu,
                done: false,  // Ajoute une propriété pour indiquer si du menu est complétée ou non
                order: selectedMenusList.value.items.length // Ajoute une propriété pour indiquer l'ordre du menu
            };
            try {
            const newMenusList = selectedMenusList.value.items.filter(item => item.dayIndex !== newMenu.dayIndex || item.category !== newMenu.category);//penser à filtrer avec l'id à termer
            selectedMenusList.value.items = newMenusList;

            selectedMenusList.value.items.push(menuToAdd); // Ajoute la nouvelle menu à du menuList sélectionnée
            const res = await updateMenuList(selectedMenusList.value._id, { items: selectedMenusList.value.items }); // Appelle la fonction de l'API pour mettre à jour du menuList dans la base de données avec la nouvelle menu
            selectedMenusList.value.items = res.data.items; // Ajoute la nouvelle menu à du menuList sélectionnée
            console.log('menu ajouté!', res.data.items);
        } catch (error) {
                console.error('Erreur lors de l\'ajout du menu à du menuList:', error);
            }
        }
    };
    //mettre à jour l'état des menus
    const updateMenuStatus = async (dayIndex, category, done) => {
        if (selectedMenusList.value) {
            const menu = selectedMenusList.value.items.find(item => item.dayIndex === dayIndex && item.category === category); // penser à mettre l'id
            if (menu) {
                menu.done = done; // Met à jour l'état du menu
                menu.lastCompletedAt = done ? new Date() : null; // Met à jour la date de dernière complétion si du menu est marquée comme faite, sinon réinitialise
                const res = await updateMenuList(selectedMenusList.value._id, { items: selectedMenusList.value.items }); // Appelle la fonction de l'API pour mettre à jour du menuList dans la base de données avec le nouvel état du menu
                selectedMenusList.value.items = res.data.items; // Met à jour du menuList sélectionnée avec les données retournées par l'API
                console.log('État du menu mis à jour!', res.data.items);
            }
        }
    };
    const updateMenuIngredients= async (name, ingredients) => {
        if (selectedMenusList.value) {
            selectedMenusList.value.items.forEach(item => {
                if(item.name === name){
                    item.ingredientOverwritten = ingredients.filter(ingredient => ingredient.name !== ''); // Personalise les ingrédients du menu avec ceux fournis
                } 
            });
            const res = await updateMenuList(selectedMenusList.value._id, { items: selectedMenusList.value.items }); // Appelle la fonction de l'API pour mettre à jour du menuList dans la base de données avec le nouvel état du menu
            selectedMenusList.value.items = res.data.items; // Met à jour du menuList sélectionnée avec les données retournées par l'API
            console.log('Ingrédients du menu mis à jour!', res.data.items);
        }
    };

    //supprimer un menu du menuList sélectionnée
    const deleteMenu = async (dayIndex, category) => {//penser à mettre l'id
        if (selectedMenusList.value) {
            selectedMenusList.value.items = selectedMenusList.value.items.filter(item => item.dayIndex !== dayIndex || item.category !== category); // //penser à mettre l'id
            localStorage.setItem('menusLists', JSON.stringify(menusLists.value)); // Met à jour le localStorage avec le menu supprimé
            //await menuApi.updateMenuList(selectedMenusList.value._id, { items: selectedMenusList.value.items }) // Appelle la fonction de l'API pour mettre à jour du menuList dans la base de données avec du menu supprimée
            showDeleteConfirmationMenu.value = false; // Ferme la confirmation de suppression
            toDeleteId.value = null;
            toDeleteName.value = '';
            toDeleteImg.value = '';
        }
    };
 
    // Retourne les données et les fonctions pour être utilisées dans les composants
    return {
        suggestedMenus,
        menusLists,
        selectedMenusList,
        creatingMenusList,
        title,
        dietTypes,
        showMenusListResults,
        showMenusListSelection,
        hiddenCategories,
        toDeleteId,
        toDeleteName,
        toDeleteImg,
        countMenuByCategory,
        selectedDay,
        selectedCategory,
        ingredientSuggestions,
        weeksInMonth,
        shoppingByWeek,
        shoppingList,
        shoppingListWithPackages,
        showDeleteConfirmationMenusList,
        showDeleteConfirmationMenu,
        initializeMenusList,
        createMenusList: createMenusList$1,
        selectMenusList,
        editMenusListTitle,
        updateMenusListTypeView,
        updateMenusListcategorySelected,
        deleteMenusList: deleteMenusList$1,
        addMenu,
        updateMenuStatus,
        updateMenuIngredients,
        deleteMenu
    }       
});

const {createElementVNode:_createElementVNode$9,unref:_unref$8,vModelText:_vModelText$1,withDirectives:_withDirectives$1,renderList:_renderList$5,Fragment:_Fragment$5,openBlock:_openBlock$9,createElementBlock:_createElementBlock$9,createCommentVNode:_createCommentVNode$6,toDisplayString:_toDisplayString$7,vShow:_vShow,normalizeClass:_normalizeClass$2,withModifiers:_withModifiers$4} = await importShared('vue');


const _hoisted_1$7 = { class: "choose-menu-title-form" };
const _hoisted_2$7 = { class: "input-group" };
const _hoisted_3$6 = {
  key: 0,
  class: "input-group"
};
const _hoisted_4$6 = { class: "type-select" };
const _hoisted_5$6 = ["onClick"];
const _hoisted_6$4 = { class: "type-info" };
const _hoisted_7$4 = ["id", "name", "onChange"];
const _hoisted_8$4 = ["src", "alt"];
const _hoisted_9$4 = ["src"];
const _hoisted_10$2 = ["for"];
const _hoisted_11$2 = { class: "action-buttons" };

const {computed: computed$7,ref: ref$7} = await importShared('vue');
//import Without from '../../assets/without.svg';

const _sfc_main$7 = {
  __name: 'chooseMenuTitle',
  setup(__props) {


const without = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(0,%200,%200)'%20d='M431.2%20476.5L163.5%20208.8C141.1%20240.2%20128%20278.6%20128%20320C128%20426%20214%20512%20320%20512C361.5%20512%20399.9%20498.9%20431.2%20476.5zM476.5%20431.2C498.9%20399.8%20512%20361.4%20512%20320C512%20214%20426%20128%20320%20128C278.5%20128%20240.1%20141.1%20208.8%20163.5L476.5%20431.2zM64%20320C64%20178.6%20178.6%2064%20320%2064C461.4%2064%20576%20178.6%20576%20320C576%20461.4%20461.4%20576%20320%20576C178.6%20576%2064%20461.4%2064%20320z'/%3e%3c/svg%3e", import.meta.url).href;
const menuStore = useMenuStore(); // Utilisation du store pour les menus avec Pinia
const selectedType = ref$7(null); // Variable pour stocker le type de budget sélectionné, initialisée au premier type disponible
const showDescription = ref$7(null); // Variable pour contrôler l'affichage de la description du type de budget
//Fermeture du formulaire de création de menu
const closeForm = () => {
  menuStore.title = '';
  menuStore.dietTypes = []; // Réinitialise les types de régimes sélectionnés
  menuStore.creatingMenusList = false; // Ferme le formulaire de création de menu
};
//j'ajoute ou retire les types de régimes choisis par l'utilisateur dans le tableau dietTypes en fonction de la sélection
const toggleDietType = (type) => {
  const index = menuStore.dietTypes.indexOf(type);
  if (index === -1) {
    menuStore.dietTypes.push(type); // Ajoute le type de régime au tableau s'il n'est pas déjà présent
  } else {
    menuStore.dietTypes.splice(index, 1); // Retire le type de régime du tableau s'il est déjà présent
  }
 // menuStore.dietTypes = dietTypes.value // Met à jour le store avec les types de régimes sélectionnés
console.log(menuStore.dietTypes);
};


return (_ctx, _cache) => {
  return (_openBlock$9(), _createElementBlock$9("div", _hoisted_1$7, [
    _cache[6] || (_cache[6] = _createElementVNode$9("div", { class: "form-header" }, [
      _createElementVNode$9("h3", { class: "form-title" }, "Créer une nouvelle liste de menus ✨")
    ], -1)),
    _cache[7] || (_cache[7] = _createElementVNode$9("p", { class: "form-subtitle" }, "Donnez un nom à votre liste de menus pour mieux vous organiser ✨", -1)),
    _createElementVNode$9("form", {
      onSubmit: _cache[2] || (_cache[2] = _withModifiers$4($event => (_unref$8(menuStore).createMenusList()), ["prevent"]))
    }, [
      _createElementVNode$9("div", _hoisted_2$7, [
        _cache[3] || (_cache[3] = _createElementVNode$9("label", {
          class: "subtitle",
          for: "title"
        }, "Nom de la liste de menus :", -1)),
        _withDirectives$1(_createElementVNode$9("input", {
          class: "title-input",
          "onUpdate:modelValue": _cache[0] || (_cache[0] = $event => ((_unref$8(menuStore).title) = $event)),
          type: "text",
          placeholder: "Ma super liste de menus"
        }, null, 512), [
          [_vModelText$1, _unref$8(menuStore).title]
        ])
      ]),
      (_unref$8(typeMenu).length > 1)
        ? (_openBlock$9(), _createElementBlock$9("div", _hoisted_3$6, [
            _cache[4] || (_cache[4] = _createElementVNode$9("p", { class: "subtitle" }, "Régimes (si nécessaire) :", -1)),
            _createElementVNode$9("div", _hoisted_4$6, [
              (_openBlock$9(true), _createElementBlock$9(_Fragment$5, null, _renderList$5(_unref$8(typeMenu), (item) => {
                return (_openBlock$9(), _createElementBlock$9("div", {
                  class: _normalizeClass$2(["type-option", { selected: selectedType.value === item.value }]),
                  key: item.value,
                  onClick: $event => (selectedType.value = item.value, showDescription.value = item.value, _unref$8(menuStore).type = item.value)
                }, [
                  _createElementVNode$9("div", _hoisted_6$4, [
                    _createElementVNode$9("input", {
                      type: "checkbox",
                      id: item.value,
                      name: item.value,
                      onChange: $event => (toggleDietType(item.value))
                    }, null, 40, _hoisted_7$4),
                    _createElementVNode$9("img", {
                      src: item.src,
                      alt: `sélectionner ${item.label}`,
                      class: "type-icon"
                    }, null, 8, _hoisted_8$4),
                    (item.without)
                      ? (_openBlock$9(), _createElementBlock$9("img", {
                          key: 0,
                          src: _unref$8(without),
                          alt: "sans",
                          class: "without-icon"
                        }, null, 8, _hoisted_9$4))
                      : _createCommentVNode$6("", true),
                    _createElementVNode$9("label", {
                      class: "type-name",
                      for: item.value
                    }, _toDisplayString$7(item.label), 9, _hoisted_10$2)
                  ]),
                  _withDirectives$1(_createElementVNode$9("p", { class: "type-description" }, _toDisplayString$7(item.description), 513), [
                    [_vShow, showDescription.value = selectedType.value === item.value]
                  ])
                ], 10, _hoisted_5$6))
              }), 128))
            ])
          ]))
        : _createCommentVNode$6("", true),
      _createElementVNode$9("div", _hoisted_11$2, [
        _createElementVNode$9("button", {
          class: "secondary-button",
          type: "button",
          onClick: _cache[1] || (_cache[1] = $event => (closeForm()))
        }, " Annuler "),
        _cache[5] || (_cache[5] = _createElementVNode$9("button", {
          class: "super-action-button submit-button",
          type: "submit"
        }, " Créer ", -1))
      ])
    ], 32)
  ]))
}
}

};
const ChooseMenuTitle = /*#__PURE__*/_export_sfc(_sfc_main$7, [['__scopeId',"data-v-f302a909"]]);

const {toDisplayString:_toDisplayString$6,openBlock:_openBlock$8,createElementBlock:_createElementBlock$8,createCommentVNode:_createCommentVNode$5,vModelText:_vModelText,unref:_unref$7,withKeys:_withKeys,withDirectives:_withDirectives,createElementVNode:_createElementVNode$8} = await importShared('vue');


const _hoisted_1$6 = { class: "menu-title-button" };
const _hoisted_2$6 = { class: "menu-title-content" };
const _hoisted_3$5 = {
  key: 0,
  class: "button-text"
};
const _hoisted_4$5 = {
  key: 0,
  class: "action-icons"
};
const _hoisted_5$5 = ["src"];
const _hoisted_6$3 = ["src"];
const _hoisted_7$3 = {
  key: 1,
  class: "action-icons"
};
const _hoisted_8$3 = ["src"];
const _hoisted_9$3 = ["src"];

const {computed: computed$6,ref: ref$6,onMounted: onMounted$7} = await importShared('vue');


const _sfc_main$6 = {
  __name: 'MenuTitle',
  props: {
  title: {
    type: String,
    required: true,
      },
  menuId: {
    type: [String, Number],
    required: true
      }
},
  setup(__props) {

const menuStore = useMenuStore();

//--------------------------Base du template-------------------------
const pencil = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M100.4%20417.2C104.5%20402.6%20112.2%20389.3%20123%20378.5L304.2%20197.3L338.1%20163.4C354.7%20180%20389.4%20214.7%20442.1%20267.4L476%20301.3L442.1%20335.2L260.9%20516.4C250.2%20527.1%20236.8%20534.9%20222.2%20539L94.4%20574.6C86.1%20576.9%2077.1%20574.6%2071%20568.4C64.9%20562.2%2062.6%20553.3%2064.9%20545L100.4%20417.2zM156%20413.5C151.6%20418.2%20148.4%20423.9%20146.7%20430.1L122.6%20517L209.5%20492.9C215.9%20491.1%20221.7%20487.8%20226.5%20483.2L155.9%20413.5zM510%20267.4C493.4%20250.8%20458.7%20216.1%20406%20163.4L372%20129.5C398.5%20103%20413.4%2088.1%20416.9%2084.6C430.4%2071%20448.8%2063.4%20468%2063.4C487.2%2063.4%20505.6%2071%20519.1%2084.6L554.8%20120.3C568.4%20133.9%20576%20152.3%20576%20171.4C576%20190.5%20568.4%20209%20554.8%20222.5C551.3%20226%20536.4%20240.9%20509.9%20267.4z'/%3e%3c/svg%3e", import.meta.url).href;
const trash = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M232.7%2069.9L224%2096L128%2096C110.3%2096%2096%20110.3%2096%20128C96%20145.7%20110.3%20160%20128%20160L512%20160C529.7%20160%20544%20145.7%20544%20128C544%20110.3%20529.7%2096%20512%2096L416%2096L407.3%2069.9C402.9%2056.8%20390.7%2048%20376.9%2048L263.1%2048C249.3%2048%20237.1%2056.8%20232.7%2069.9zM512%20208L128%20208L149.1%20531.1C150.7%20556.4%20171.7%20576%20197%20576L443%20576C468.3%20576%20489.3%20556.4%20490.9%20531.1L512%20208z'/%3e%3c/svg%3e", import.meta.url).href;
const check = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='%234caf8d'%20d='M530.8%20134.1C545.1%20144.5%20548.3%20164.5%20537.9%20178.8L281.9%20530.8C276.4%20538.4%20267.9%20543.1%20258.5%20543.9C249.1%20544.7%20240%20541.2%20233.4%20534.6L105.4%20406.6C92.9%20394.1%2092.9%20373.8%20105.4%20361.3C117.9%20348.8%20138.2%20348.8%20150.7%20361.3L252.2%20462.8L486.2%20141.1C496.6%20126.8%20516.6%20123.6%20530.9%20134z'/%3e%3c/svg%3e", import.meta.url).href;
const cross = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='%23e35d6a'%20d='M183.1%20137.4C170.6%20124.9%20150.3%20124.9%20137.8%20137.4C125.3%20149.9%20125.3%20170.2%20137.8%20182.7L275.2%20320L137.9%20457.4C125.4%20469.9%20125.4%20490.2%20137.9%20502.7C150.4%20515.2%20170.7%20515.2%20183.2%20502.7L320.5%20365.3L457.9%20502.6C470.4%20515.1%20490.7%20515.1%20503.2%20502.6C515.7%20490.1%20515.7%20469.8%20503.2%20457.3L365.8%20320L503.1%20182.6C515.6%20170.1%20515.6%20149.8%20503.1%20137.3C490.6%20124.8%20470.3%20124.8%20457.8%20137.3L320.5%20274.7L183.1%20137.4z'/%3e%3c/svg%3e", import.meta.url).href;

const props = __props;

//---------------------------GESTION DE L'ÉDITION ET DE LA SUPPRESSION DE Liste de tache-------------------------

computed$6(() => props.title); // Computed pour le titre de la budget
const newTitle = ref$6(props.title); // Variable réactive pour stocker le nouveau titre lors de l'édition


onMounted$7(() => {
});
const isEditing = ref$6(false); // Variable réactive pour contrôler le mode édition
const stopPropagation = (event) => {
  event.stopPropagation(); // Empêche la propagation de l'événement de clic pour éviter de sélectionner la budget avant de la supprimer
};
// Emit pour les actions d'édition et de suppression

const updatemode = (event) => {
  event.stopPropagation(); // Empêche la propagation de l'événement de clic pour éviter de sélectionner la budget avant de la supprimer
  isEditing.value = true; // Bascule le mode édition
};

const endUpdatemode = (event) => {
  event.stopPropagation(); // Empêche la propagation de l'événement de clic pour éviter de sélectionner la budget avant de la supprimer
  isEditing.value = false; // Désactive le mode édition
  newTitle.value = props.title; // Réinitialise le titre à l'original en cas d'annulation
};
const closeEditMode = () => {
  isEditing.value = false; // Désactive le mode édition
};


return (_ctx, _cache) => {
  return (_openBlock$8(), _createElementBlock$8("button", _hoisted_1$6, [
    _createElementVNode$8("div", _hoisted_2$6, [
      (!isEditing.value)
        ? (_openBlock$8(), _createElementBlock$8("p", _hoisted_3$5, _toDisplayString$6(newTitle.value), 1))
        : _withDirectives((_openBlock$8(), _createElementBlock$8("input", {
            key: 1,
            class: "input-edit",
            type: "text",
            "onUpdate:modelValue": _cache[0] || (_cache[0] = $event => ((newTitle).value = $event)),
            onKeyup: _cache[1] || (_cache[1] = _withKeys($event => {_unref$7(menuStore).editMenusListTitle($event, __props.menuId, newTitle.value); closeEditMode();}, ["enter"])),
            onClick: _cache[2] || (_cache[2] = $event => (stopPropagation($event)))
          }, null, 544)), [
            [_vModelText, newTitle.value]
          ])
    ]),
    (!isEditing.value)
      ? (_openBlock$8(), _createElementBlock$8("div", _hoisted_4$5, [
          _createElementVNode$8("img", {
            class: "icon-edit",
            src: _unref$7(pencil),
            alt: "modifier",
            onClick: updatemode
          }, null, 8, _hoisted_5$5),
          _createElementVNode$8("img", {
            class: "icon-edit",
            src: _unref$7(trash),
            alt: "supprimer",
            onClick: _cache[3] || (_cache[3] = $event => {_unref$7(menuStore).showDeleteConfirmationMenusList = true; _unref$7(menuStore).toDeleteId = __props.menuId; _unref$7(menuStore).toDeleteName = newTitle.value;})
          }, null, 8, _hoisted_6$3)
        ]))
      : (_openBlock$8(), _createElementBlock$8("div", _hoisted_7$3, [
          _createElementVNode$8("img", {
            class: "icon-edit",
            src: _unref$7(check),
            alt: "valider",
            onClick: _cache[4] || (_cache[4] = $event => {closeEditMode(); _unref$7(menuStore).editMenusListTitle($event, __props.menuId, newTitle.value);})
          }, null, 8, _hoisted_8$3),
          _createElementVNode$8("img", {
            class: "icon-edit",
            src: _unref$7(cross),
            alt: "annuler",
            onClick: endUpdatemode
          }, null, 8, _hoisted_9$3)
        ]))
  ]))
}
}

};
const MenuTitle = /*#__PURE__*/_export_sfc(_sfc_main$6, [['__scopeId',"data-v-6fcd94c3"]]);

const {createElementVNode:_createElementVNode$7,createVNode:_createVNode$3,unref:_unref$6,openBlock:_openBlock$7,createElementBlock:_createElementBlock$7,createCommentVNode:_createCommentVNode$4,createBlock:_createBlock$2,renderList:_renderList$4,Fragment:_Fragment$4,withModifiers:_withModifiers$3} = await importShared('vue');


const _hoisted_1$5 = { class: "menu-sidebar" };
const _hoisted_2$5 = ["src"];
const {ref: ref$5,computed: computed$5,onMounted: onMounted$6,onUnmounted: onUnmounted$1} = await importShared('vue');

const _sfc_main$5 = {
  __name: 'Sidebar',
  setup(__props) {

const menuStore = useMenuStore(); // Utilisation du store pour accéder aux données et fonctions

// -------------------------GESTION DU THÈME (SOMBRE/CLAIR)-------------------------
const isDark = ref$5(false); // Variable réactive pour stocker le thème (false = clair, true = sombre)

onMounted$6(() => { // Exécuté quand le composant est monté
    // Observe le thème système
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)'); 
    // Initialise isDark selon le thème actuel
    isDark.value = mediaQuery.matches; 
    // Fonction appelée quand le thème change
    const handler = (e) => { 
      isDark.value = e.matches; // Met à jour la valeur en fonction du nouveau thème
    };
    // Écoute les changements du thème système
    mediaQuery.addEventListener('change', handler); 

    menuStore.initializeMenusList();

  onUnmounted$1(() => { // Exécuté quand le composant est détruit
    mediaQuery.removeEventListener('change', handler); // Supprime l'écouteur pour éviter les fuites mémoire
  });

});

//--------------------------GESTION DE LA CRÉATION DE MENU-------------------------

ref$5(false); // Variable réactive pour contrôler l'affichage du formulaire 
menuStore.menusLists; // Variable réactive pour stocker les titres des menu existantes


return (_ctx, _cache) => {
  return (_openBlock$7(), _createElementBlock$7("nav", _hoisted_1$5, [
    _cache[2] || (_cache[2] = _createElementVNode$7("h2", null, " Mes Menus ", -1)),
    _createVNode$3(StarBar),
    (_unref$6(menuStore).creatingMenusList === false)
      ? (_openBlock$7(), _createElementBlock$7("button", {
          key: 0,
          class: "add-menu-button super-action-button",
          onClick: _cache[0] || (_cache[0] = $event => (_unref$6(menuStore).creatingMenusList = true))
        }, [
          _createElementVNode$7("img", {
            class: "icon-plus",
            src: isDark.value ? _unref$6(plusWhite) : _unref$6(plusBlack),
            alt: "ajouter un menu"
          }, null, 8, _hoisted_2$5),
          _cache[1] || (_cache[1] = _createElementVNode$7("p", { class: "button-text" }, "Nouveau Menu", -1))
        ]))
      : (_openBlock$7(), _createBlock$2(ChooseMenuTitle, { key: 1 })),
    (_openBlock$7(true), _createElementBlock$7(_Fragment$4, null, _renderList$4(_unref$6(menuStore).menusLists, (item, index) => {
      return (_openBlock$7(), _createBlock$2(MenuTitle, {
        key: item._id,
        menuId: item._id,
        title: item.title,
        onClick: _withModifiers$3($event => (_unref$6(menuStore).selectMenusList(item._id)), ["self"])
      }, null, 8, ["menuId", "title", "onClick"]))
    }), 128))
  ]))
}
}

};
const Sidebar = /*#__PURE__*/_export_sfc(_sfc_main$5, [['__scopeId',"data-v-cb93310d"]]);

const arrowLeft = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(137,%20108,%20221)'%20d='M73.4%20297.4C60.9%20309.9%2060.9%20330.2%2073.4%20342.7L233.4%20502.7C245.9%20515.2%20266.2%20515.2%20278.7%20502.7C291.2%20490.2%20291.2%20469.9%20278.7%20457.4L173.3%20352L544%20352C561.7%20352%20576%20337.7%20576%20320C576%20302.3%20561.7%20288%20544%20288L173.3%20288L278.7%20182.6C291.2%20170.1%20291.2%20149.8%20278.7%20137.3C266.2%20124.8%20245.9%20124.8%20233.4%20137.3L73.4%20297.3z'/%3e%3c/svg%3e";

const AngleDown = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M297.4%20438.6C309.9%20451.1%20330.2%20451.1%20342.7%20438.6L502.7%20278.6C515.2%20266.1%20515.2%20245.8%20502.7%20233.3C490.2%20220.8%20469.9%20220.8%20457.4%20233.3L320%20370.7L182.6%20233.4C170.1%20220.9%20149.8%20220.9%20137.3%20233.4C124.8%20245.9%20124.8%20266.2%20137.3%20278.7L297.3%20438.7z'/%3e%3c/svg%3e";

const xMark = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='%23e35d6a'%20d='M183.1%20137.4C170.6%20124.9%20150.3%20124.9%20137.8%20137.4C125.3%20149.9%20125.3%20170.2%20137.8%20182.7L275.2%20320L137.9%20457.4C125.4%20469.9%20125.4%20490.2%20137.9%20502.7C150.4%20515.2%20170.7%20515.2%20183.2%20502.7L320.5%20365.3L457.9%20502.6C470.4%20515.1%20490.7%20515.1%20503.2%20502.6C515.7%20490.1%20515.7%20469.8%20503.2%20457.3L365.8%20320L503.1%20182.6C515.6%20170.1%20515.6%20149.8%20503.1%20137.3C490.6%20124.8%20470.3%20124.8%20457.8%20137.3L320.5%20274.7L183.1%20137.4z'/%3e%3c/svg%3e";

const pencilWhite = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M100.4%20417.2C104.5%20402.6%20112.2%20389.3%20123%20378.5L304.2%20197.3L338.1%20163.4C354.7%20180%20389.4%20214.7%20442.1%20267.4L476%20301.3L442.1%20335.2L260.9%20516.4C250.2%20527.1%20236.8%20534.9%20222.2%20539L94.4%20574.6C86.1%20576.9%2077.1%20574.6%2071%20568.4C64.9%20562.2%2062.6%20553.3%2064.9%20545L100.4%20417.2zM156%20413.5C151.6%20418.2%20148.4%20423.9%20146.7%20430.1L122.6%20517L209.5%20492.9C215.9%20491.1%20221.7%20487.8%20226.5%20483.2L155.9%20413.5zM510%20267.4C493.4%20250.8%20458.7%20216.1%20406%20163.4L372%20129.5C398.5%20103%20413.4%2088.1%20416.9%2084.6C430.4%2071%20448.8%2063.4%20468%2063.4C487.2%2063.4%20505.6%2071%20519.1%2084.6L554.8%20120.3C568.4%20133.9%20576%20152.3%20576%20171.4C576%20190.5%20568.4%20209%20554.8%20222.5C551.3%20226%20536.4%20240.9%20509.9%20267.4z'/%3e%3c/svg%3e";

const {openBlock:_openBlock$6,createElementBlock:_createElementBlock$6,createCommentVNode:_createCommentVNode$3,createElementVNode:_createElementVNode$6,toDisplayString:_toDisplayString$5,unref:_unref$5,withModifiers:_withModifiers$2,normalizeStyle:_normalizeStyle$1} = await importShared('vue');


const _hoisted_1$4 = {
  key: 0,
  class: "selected-overlay"
};
const _hoisted_2$4 = ["src", "alt"];
const _hoisted_3$4 = { class: "header-box" };
const _hoisted_4$4 = {
  key: 0,
  class: "menu-name"
};
const _hoisted_5$4 = ["src"];
const _hoisted_6$2 = { class: "option-box" };
const _hoisted_7$2 = ["src"];
const _hoisted_8$2 = ["src"];
const _hoisted_9$2 = ["src"];

const {ref: ref$4,onMounted: onMounted$5,computed: computed$4} = await importShared('vue');

const _sfc_main$4 = {
  __name: 'result',
  props: {
  item: {
    type: Object,
    required: true
  },
  categoryColor: {
    type: String,
    required: true
  }
},
  setup(__props) {

const trash = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M232.7%2069.9L224%2096L128%2096C110.3%2096%2096%20110.3%2096%20128C96%20145.7%20110.3%20160%20128%20160L512%20160C529.7%20160%20544%20145.7%20544%20128C544%20110.3%20529.7%2096%20512%2096L416%2096L407.3%2069.9C402.9%2056.8%20390.7%2048%20376.9%2048L263.1%2048C249.3%2048%20237.1%2056.8%20232.7%2069.9zM512%20208L128%20208L149.1%20531.1C150.7%20556.4%20171.7%20576%20197%20576L443%20576C468.3%20576%20489.3%20556.4%20490.9%20531.1L512%20208z'/%3e%3c/svg%3e", import.meta.url).href;
new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M320%20208C289.1%20208%20264%20182.9%20264%20152C264%20121.1%20289.1%2096%20320%2096C350.9%2096%20376%20121.1%20376%20152C376%20182.9%20350.9%20208%20320%20208zM320%20432C350.9%20432%20376%20457.1%20376%20488C376%20518.9%20350.9%20544%20320%20544C289.1%20544%20264%20518.9%20264%20488C264%20457.1%20289.1%20432%20320%20432zM376%20320C376%20350.9%20350.9%20376%20320%20376C289.1%20376%20264%20350.9%20264%20320C264%20289.1%20289.1%20264%20320%20264C350.9%20264%20376%20289.1%20376%20320z'/%3e%3c/svg%3e", import.meta.url).href;
const optionDark = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(0,%200,%200)'%20d='M320%20208C289.1%20208%20264%20182.9%20264%20152C264%20121.1%20289.1%2096%20320%2096C350.9%2096%20376%20121.1%20376%20152C376%20182.9%20350.9%20208%20320%20208zM320%20432C350.9%20432%20376%20457.1%20376%20488C376%20518.9%20350.9%20544%20320%20544C289.1%20544%20264%20518.9%20264%20488C264%20457.1%20289.1%20432%20320%20432zM376%20320C376%20350.9%20350.9%20376%20320%20376C289.1%20376%20264%20350.9%20264%20320C264%20289.1%20289.1%20264%20320%20264C350.9%20264%20376%20289.1%20376%20320z'/%3e%3c/svg%3e", import.meta.url).href;

// a ajouter après: @click="menuStore.showMenusListSelection = true; menuStore.selectedCategory = category"

const menuStore = useMenuStore();

const props = __props;

const menuElement = ref$4(null);

//je chercher dans chaque catégorie du tableau du menu qui correspond au nom du menu sélectionnée pour afficher son label et son image
const menu = computed$4(() =>

  menuStore.suggestedMenus

    .find(menu => menu.name === props.item.name)

);

const selected = ref$4(props.item.done); // Variable réactive pour suivre l'état de sélection du menu
const updateMenuStatus = () => {
if (showOptions.value) {
  showOptions.value = false; // Ferme les options si elles sont ouvertes
  }else {
  selected.value = !selected.value;
  menuStore.updateMenuStatus(props.item.dayIndex,props.item.category, selected.value);//penser à mettre l'id
  }
};

const showOptions = ref$4(false);

//je récupères l'ensemble des taches pour modifier leurs order dans du menuList
computed$4(() => {
    return menuStore.selectedMenusList ? menuStore.selectedMenusList.items : [];
});


const updateThisChoice = () => {
menuStore.showMenusListSelection = true;//j'ouvre la liste de sélection des menus
menuStore.selectedCategory = props.item.category;//je sélectionne la catégorie du menu sélectionné pour afficher les menus de la catégorie dans la liste de sélection des menus
menuStore.selectedDay = props.item.dayIndex;//je sélectionne le jour du menu sélectionné pour afficher les menus de la catégorie dans la liste de sélection des menus

};


return (_ctx, _cache) => {
  return (_openBlock$6(), _createElementBlock$6("div", {
    class: "choices-box",
    style: _normalizeStyle$1({ backgroundColor: props.categoryColor }),
    onClick: _cache[4] || (_cache[4] = $event => (props.item.name === 'Aucune-selection' ? (_unref$5(menuStore).showMenusListSelection = true, _unref$5(menuStore).selectedCategory = props.item.category, _unref$5(menuStore).selectedDay = props.item.dayIndex) : (updateMenuStatus(), showOptions.value = false))),
    ref_key: "menuElement",
    ref: menuElement
  }, [
    (selected.value)
      ? (_openBlock$6(), _createElementBlock$6("div", _hoisted_1$4))
      : _createCommentVNode$3("", true),
    _createElementVNode$6("img", {
      src: menu.value.source,
      loading: "lazy",
      alt: menu.value.label,
      class: "menu-image"
    }, null, 8, _hoisted_2$4),
    _createElementVNode$6("div", _hoisted_3$4, [
      (!showOptions.value)
        ? (_openBlock$6(), _createElementBlock$6("p", _hoisted_4$4, _toDisplayString$5(menu.value.label), 1))
        : _createCommentVNode$3("", true),
      (!showOptions.value)
        ? (_openBlock$6(), _createElementBlock$6("img", {
            key: 1,
            src: _unref$5(optionDark),
            alt: "Options",
            class: "icon",
            onClick: _cache[0] || (_cache[0] = _withModifiers$2($event => (showOptions.value = !showOptions.value), ["stop"]))
          }, null, 8, _hoisted_5$4))
        : _createCommentVNode$3("", true),
      _createElementVNode$6("div", _hoisted_6$2, [
        (showOptions.value)
          ? (_openBlock$6(), _createElementBlock$6("img", {
              key: 0,
              src: _unref$5(trash),
              alt: "Supprimer",
              class: "trash-icon",
              onClick: _cache[1] || (_cache[1] = _withModifiers$2($event => {_unref$5(menuStore).showDeleteConfirmationMenu = true; _unref$5(menuStore).toDeleteId = props.item._id; _unref$5(menuStore).toDeleteName = menu.value.label; _unref$5(menuStore).toDeleteImg = menu.value.source; showOptions.value = !showOptions.value; _unref$5(menuStore).selectedDay = props.item.dayIndex; _unref$5(menuStore).selectedCategory = props.item.category;}, ["stop"]))
            }, null, 8, _hoisted_7$2))
          : _createCommentVNode$3("", true),
        (showOptions.value)
          ? (_openBlock$6(), _createElementBlock$6("img", {
              key: 1,
              src: _unref$5(pencilWhite),
              alt: "Modifier",
              class: "pencil-icon",
              onClick: _cache[2] || (_cache[2] = _withModifiers$2($event => {updateThisChoice(); showOptions.value = !showOptions.value;}, ["stop"]))
            }, null, 8, _hoisted_8$2))
          : _createCommentVNode$3("", true),
        (showOptions.value)
          ? (_openBlock$6(), _createElementBlock$6("img", {
              key: 2,
              src: _unref$5(xMark),
              alt: "Retour",
              class: "arrow-left-icon",
              onClick: _cache[3] || (_cache[3] = _withModifiers$2($event => ( showOptions.value = false), ["stop"]))
            }, null, 8, _hoisted_9$2))
          : _createCommentVNode$3("", true)
      ])
    ])
  ], 4))
}
}

};
const Result = /*#__PURE__*/_export_sfc(_sfc_main$4, [['__scopeId',"data-v-89c0883d"]]);

const Look = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(255,%20255,%20255)'%20d='M480%20272C480%20317.9%20465.1%20360.3%20440%20394.7L566.6%20521.4C579.1%20533.9%20579.1%20554.2%20566.6%20566.7C554.1%20579.2%20533.8%20579.2%20521.3%20566.7L394.7%20440C360.3%20465.1%20317.9%20480%20272%20480C157.1%20480%2064%20386.9%2064%20272C64%20157.1%20157.1%2064%20272%2064C386.9%2064%20480%20157.1%20480%20272zM272%20416C351.5%20416%20416%20351.5%20416%20272C416%20192.5%20351.5%20128%20272%20128C192.5%20128%20128%20192.5%20128%20272C128%20351.5%20192.5%20416%20272%20416z'/%3e%3c/svg%3e";

const {normalizeClass:_normalizeClass$1,createElementVNode:_createElementVNode$5,toDisplayString:_toDisplayString$4,unref:_unref$4,renderList:_renderList$3,Fragment:_Fragment$3,openBlock:_openBlock$5,createElementBlock:_createElementBlock$5,createCommentVNode:_createCommentVNode$2,createTextVNode:_createTextVNode$3,withModifiers:_withModifiers$1,createStaticVNode:_createStaticVNode} = await importShared('vue');


const _hoisted_1$3 = ["src", "alt"];
const _hoisted_2$3 = { class: "menu-name" };
const _hoisted_3$3 = ["src"];
const _hoisted_4$3 = { class: "menu-info" };
const _hoisted_5$3 = ["src", "alt"];
const _hoisted_6$1 = { class: "menu-details" };
const _hoisted_7$1 = { class: "menu-info-title" };
const _hoisted_8$1 = { class: "menu-name" };
const _hoisted_9$1 = { class: "diet-icons" };
const _hoisted_10$1 = ["src", "alt"];
const _hoisted_11$1 = ["src", "alt"];
const _hoisted_12$1 = {
  key: 1,
  class: "ingredients-list"
};
const _hoisted_13$1 = { class: "menu-text" };
const _hoisted_14$1 = {
  key: 2,
  class: "ingredients-list"
};
const _hoisted_15$1 = ["id", "value", "onInput"];
const _hoisted_16$1 = ["id", "value", "onInput"];
const _hoisted_17$1 = ["id", "value", "onInput"];
const _hoisted_18 = ["id", "value", "onInput"];
const _hoisted_19 = ["id", "value", "onInput"];
const _hoisted_20 = ["value"];
const _hoisted_21 = {
  key: 3,
  class: "button-box"
};
const _hoisted_22 = {
  key: 4,
  class: "button-box"
};

const {ref: ref$3,onMounted: onMounted$4,computed: computed$3} = await importShared('vue');

const _sfc_main$3 = {
  __name: 'choices',
  props: {
  name: {
    type: String,
    required: true
  },
  label: {
    type: String,
    required: true
  },
  source: {
    type: URL,
    required: true
  },
  servings: {
    type: Number,
    required: true
  },
  diets: {
    type: Array,
    required: true
  },
  creationType: {
    type: String,
    required: true
  },
  ingredients: {
    type: Array,
    required: true
  },
  category: {
    type: String,
    required: true
  }
},
  setup(__props) {

const without = new URL("data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20640%20640'%3e%3c!--!Font%20Awesome%20Free%207.2.0%20by%20@fontawesome%20-%20https://fontawesome.com%20License%20-%20https://fontawesome.com/license/free%20Copyright%202026%20Fonticons,%20Inc.--%3e%3cpath%20fill='rgb(0,%200,%200)'%20d='M431.2%20476.5L163.5%20208.8C141.1%20240.2%20128%20278.6%20128%20320C128%20426%20214%20512%20320%20512C361.5%20512%20399.9%20498.9%20431.2%20476.5zM476.5%20431.2C498.9%20399.8%20512%20361.4%20512%20320C512%20214%20426%20128%20320%20128C278.5%20128%20240.1%20141.1%20208.8%20163.5L476.5%20431.2zM64%20320C64%20178.6%20178.6%2064%20320%2064C461.4%2064%20576%20178.6%20576%20320C576%20461.4%20461.4%20576%20320%20576C178.6%20576%2064%20461.4%2064%20320z'/%3e%3c/svg%3e", import.meta.url).href;

const menuStore = useMenuStore();

//--------------Mes props-----------------
const props = __props;

//----------------------------- Mes states et fonctions -----------------------------
const selected = ref$3(false);//Ne mets pas le filtre

//Quand je sélectionne un menu
const select = () => {

  selected.value = true;//je met le filtre sur le menu sélectionné
  menuStore.showMenusListSelection = false;//je ferme la liste de sélection des menus
  //j'ajoute le menu sélectionné à la liste des menus sélectionnés
  menuStore.addMenu({ name: props.name, category: props.category, dayIndex: menuStore.selectedDay });
};



//j'ouvre la modal info du menu pour afficher les détails du menu sélectionné
const isOpen = ref$3(false);
//j'affiche les détails du menu sélectionné dans la modal info du menu
const showInfo = ref$3(false);

//j'associe les diets au icones et without associés correspondants pour les afficher dans la modal info du menu à partir du tableau de données des menus
const dietIcons = computed$3(() => {
  return props.diets.map(diet => {
    const DietType = typeMenu.find(menu => menu.value === diet);
    return DietType ? 
     { icon: DietType.src, without: DietType.without }
    : null
  }).filter(icon => icon !== null)
});

//update des ingrédients-----------------------------------
const personalizedRecipe =
  menuStore.selectedMenusList?.items.find(
    item =>
      item.name === props.name &&
      item.ingredientOverwritten?.length
  );

const copyIngredients = ref$3(

  personalizedRecipe?.ingredientOverwritten?.map(
    ingredient => ({ ...ingredient })
  )

  ??

  props.ingredients.map(
    ingredient => ({ ...ingredient })
  )

);

const updatingIngredients = ref$3(false);

const cancelUpdate = () => {
  updatingIngredients.value = false;
  copyIngredients.value = props.ingredients.map(ingredient => ({ ...ingredient })); // Réinitialise la copie des ingrédients à partir des données d'origine
};

const AddIngredient = () => {
  copyIngredients.value.push({ name: '', quantity: 0, unit: '',storePackageQuantity: 0, price: 0 }); // Ajoute un nouvel ingrédient vide à la liste pour permettre à l'utilisateur de le remplir
};

const personalize = () => {
  select();
  // Met à jour les ingrédients du menu sélectionné dans le store
  menuStore.updateMenuIngredients(props.name, copyIngredients.value);
  updatingIngredients.value = false; // Ferme la personnalisation des ingrédients
};



const selectSuggestion = (ingredients) => {
console.log('Suggestion sélectionnée:', ingredients); // Affiche la suggestion sélectionnée dans la console pour vérification
  copyIngredients.value.push({ name: ingredients.name, quantity: ingredients.quantity, unit: ingredients.unit,storePackageQuantity: ingredients.storePackageQuantity, price: ingredients.price }); // Ajoute un nouvel ingrédient vide à la liste pour permettre à l'utilisateur de le remplir

};



return (_ctx, _cache) => {
  return (_openBlock$5(), _createElementBlock$5("div", {
    class: _normalizeClass$1(isOpen.value ? 'choices-box-open' : 'choices-box')
  }, [
    _createElementVNode$5("img", {
      onClick: _cache[0] || (_cache[0] = $event => (select())),
      src: props.source,
      loading: "lazy",
      alt: props.label,
      class: _normalizeClass$1(isOpen.value ? 'menu-image-open' : 'menu-image')
    }, null, 10, _hoisted_1$3),
    _createElementVNode$5("div", {
      class: "menu-name-box",
      onClick: _cache[1] || (_cache[1] = $event => (showInfo.value=!showInfo.value, isOpen.value=!isOpen.value))
    }, [
      _createElementVNode$5("p", _hoisted_2$3, _toDisplayString$4(props.label), 1),
      _createElementVNode$5("img", {
        src: _unref$4(Look),
        alt: "Voir plus",
        class: "angle-down"
      }, null, 8, _hoisted_3$3)
    ]),
    (showInfo.value)
      ? (_openBlock$5(), _createElementBlock$5("div", {
          key: 0,
          class: "modal-info",
          onClick: _cache[9] || (_cache[9] = _withModifiers$1($event => (showInfo.value=false, isOpen.value=false), ["self"]))
        }, [
          _createElementVNode$5("div", _hoisted_4$3, [
            _createElementVNode$5("img", {
              class: "image-info",
              src: props.source,
              loading: "lazy",
              alt: props.label
            }, null, 8, _hoisted_5$3),
            _createElementVNode$5("div", _hoisted_6$1, [
              _createElementVNode$5("p", _hoisted_7$1, _toDisplayString$4(props.label), 1),
              _createElementVNode$5("p", _hoisted_8$1, "Nombre de personnes : " + _toDisplayString$4(props.servings), 1),
              _cache[12] || (_cache[12] = _createElementVNode$5("p", { class: "menu-info-text" }, "Régimes alimentaires : ", -1)),
              _createElementVNode$5("div", _hoisted_9$1, [
                (_openBlock$5(true), _createElementBlock$5(_Fragment$3, null, _renderList$3(dietIcons.value, (icon) => {
                  return (_openBlock$5(), _createElementBlock$5("div", {
                    class: "without-box",
                    key: icon.icon
                  }, [
                    _createElementVNode$5("img", {
                      src: icon.icon,
                      class: "diet-icon",
                      alt: `Diet Icon ${icon.without}`
                    }, null, 8, _hoisted_10$1),
                    (icon.without)
                      ? (_openBlock$5(), _createElementBlock$5("img", {
                          key: 0,
                          src: _unref$4(without),
                          class: "without",
                          alt: `Diet Icon ${icon.without}`
                        }, null, 8, _hoisted_11$1))
                      : _createCommentVNode$2("", true)
                  ]))
                }), 128))
              ]),
              _cache[13] || (_cache[13] = _createElementVNode$5("p", { class: "menu-info-text" }, "Ingrédients :", -1)),
              (!updatingIngredients.value)
                ? (_openBlock$5(), _createElementBlock$5("button", {
                    key: 0,
                    class: "super-action-button",
                    onClick: _cache[2] || (_cache[2] = $event => (updatingIngredients.value = true))
                  }, "Personnaliser les ingrédients"))
                : _createCommentVNode$2("", true),
              (!updatingIngredients.value)
                ? (_openBlock$5(), _createElementBlock$5("ul", _hoisted_12$1, [
                    (_openBlock$5(true), _createElementBlock$5(_Fragment$3, null, _renderList$3(copyIngredients.value, (ingredient) => {
                      return (_openBlock$5(), _createElementBlock$5("li", {
                        key: ingredient.name
                      }, [
                        _createElementVNode$5("p", _hoisted_13$1, [
                          _createTextVNode$3(_toDisplayString$4(ingredient.name) + " - ", 1),
                          _createElementVNode$5("span", null, _toDisplayString$4(ingredient.quantity) + " " + _toDisplayString$4(ingredient.unit) + " - " + _toDisplayString$4(ingredient.price) + " €", 1)
                        ])
                      ]))
                    }), 128))
                  ]))
                : (_openBlock$5(), _createElementBlock$5("div", _hoisted_14$1, [
                    _cache[11] || (_cache[11] = _createStaticVNode("<div class=\"ingredient-header\" data-v-d37946f2><p class=\"ingredient-header-item\" data-v-d37946f2>Nom</p><p class=\"ingredient-header-item\" data-v-d37946f2>Quantité</p><p class=\"ingredient-header-item\" data-v-d37946f2>Unité</p><p class=\"ingredient-header-item\" data-v-d37946f2>Quantité en magasin</p><p class=\"ingredient-header-item\" data-v-d37946f2>Prix magasin</p></div>", 1)),
                    (_openBlock$5(true), _createElementBlock$5(_Fragment$3, null, _renderList$3(copyIngredients.value, (ingredient, index) => {
                      return (_openBlock$5(), _createElementBlock$5("div", {
                        class: "ingredient-box",
                        key: `ingredient-${index}`
                      }, [
                        _createElementVNode$5("input", {
                          class: "ingredient-item",
                          type: "text",
                          id: `ingredient-${ingredient.name}`,
                          value: ingredient.name,
                          onInput: $event => (ingredient.name = $event.target.value)
                        }, null, 40, _hoisted_15$1),
                        _createElementVNode$5("input", {
                          class: "ingredient-item",
                          type: "number",
                          id: `ingredient-quantity-${ingredient.name}`,
                          value: ingredient.quantity,
                          onInput: $event => (ingredient.quantity = $event.target.value)
                        }, null, 40, _hoisted_16$1),
                        _createElementVNode$5("input", {
                          class: "ingredient-item",
                          type: "text",
                          id: `ingredient-unit-${ingredient.name}`,
                          value: ingredient.unit,
                          onInput: $event => (ingredient.unit = $event.target.value)
                        }, null, 40, _hoisted_17$1),
                        _createElementVNode$5("input", {
                          class: "ingredient-item",
                          type: "number",
                          id: `store-quantity-${ingredient.name}`,
                          value: ingredient.storePackageQuantity,
                          onInput: $event => (ingredient.storePackageQuantity = $event.target.value)
                        }, null, 40, _hoisted_18),
                        _createElementVNode$5("input", {
                          class: "ingredient-item",
                          type: "number",
                          id: `ingredient-price-${ingredient.name}`,
                          value: ingredient.price,
                          onInput: $event => (ingredient.price = $event.target.value)
                        }, null, 40, _hoisted_19)
                      ]))
                    }), 128)),
                    _createElementVNode$5("select", {
                      name: "myIngredient",
                      id: "my-ingredients-selection",
                      onChange: _cache[3] || (_cache[3] = $event => (selectSuggestion(_unref$4(menuStore).ingredientSuggestions.find(suggestion => suggestion.name === $event.target.value))))
                    }, [
                      _cache[10] || (_cache[10] = _createElementVNode$5("option", {
                        value: "",
                        disabled: "",
                        selected: ""
                      }, "Choisir parmis mes personnalisations", -1)),
                      (_openBlock$5(true), _createElementBlock$5(_Fragment$3, null, _renderList$3(_unref$4(menuStore).ingredientSuggestions, (suggestion, index) => {
                        return (_openBlock$5(), _createElementBlock$5("option", {
                          key: `suggestion-${index}`,
                          value: suggestion.name
                        }, _toDisplayString$4(suggestion.name) + " - " + _toDisplayString$4(suggestion.quantity) + " " + _toDisplayString$4(suggestion.unit) + " - " + _toDisplayString$4(suggestion.price) + " € ", 9, _hoisted_20))
                      }), 128))
                    ], 32),
                    _createElementVNode$5("button", {
                      class: "primary-button",
                      onClick: _cache[4] || (_cache[4] = $event => (AddIngredient()))
                    }, "Ajouter un ingrédient")
                  ])),
              (updatingIngredients.value)
                ? (_openBlock$5(), _createElementBlock$5("div", _hoisted_21, [
                    _createElementVNode$5("button", {
                      class: "super-action-button",
                      onClick: _cache[5] || (_cache[5] = $event => (personalize()))
                    }, "Terminer la personnalisation"),
                    _createElementVNode$5("button", {
                      class: "secondary-button",
                      onClick: _cache[6] || (_cache[6] = $event => (cancelUpdate()))
                    }, "Annuler")
                  ]))
                : _createCommentVNode$2("", true),
              (!updatingIngredients.value)
                ? (_openBlock$5(), _createElementBlock$5("div", _hoisted_22, [
                    _createElementVNode$5("button", {
                      class: "primary-button",
                      onClick: _cache[7] || (_cache[7] = $event => {select(); showInfo.value=false, isOpen.value=false;})
                    }, "Ajouter"),
                    _createElementVNode$5("button", {
                      class: "secondary-button",
                      onClick: _cache[8] || (_cache[8] = $event => (showInfo.value=false, isOpen.value=false))
                    }, "Fermer ")
                  ]))
                : _createCommentVNode$2("", true)
            ])
          ])
        ]))
      : _createCommentVNode$2("", true)
  ], 2))
}
}

};
const Choices = /*#__PURE__*/_export_sfc(_sfc_main$3, [['__scopeId',"data-v-d37946f2"]]);

// composables/useDevice.js
const {ref: ref$2,onMounted: onMounted$3,onUnmounted} = await importShared('vue');


function useIsMobile() {
  const isMobile = ref$2(window.innerWidth <= 768);

  const update = () => {
    isMobile.value = window.innerWidth <= 768;
  };
  // Ajouter un écouteur d'événement pour mettre à jour la valeur de isMobile lors du redimensionnement de la fenêtre
  onMounted$3(() => {
    window.addEventListener('resize', update);
  });
  // Nettoyer l'écouteur d'événement lorsque le composant est démonté
  onUnmounted(() => {
      window.removeEventListener('resize', update);
    });

  return { isMobile }
}
/* importer dans le composant :
 import { useIsMobile } from '../composables/useIsMobile.js'
  const { isMobile } = useIsMobile()
  */

const {unref:_unref$3,createElementVNode:_createElementVNode$4,toDisplayString:_toDisplayString$3,renderList:_renderList$2,Fragment:_Fragment$2,openBlock:_openBlock$4,createElementBlock:_createElementBlock$4,createBlock:_createBlock$1} = await importShared('vue');


const _hoisted_1$2 = { class: "back-arrow-box" };
const _hoisted_2$2 = ["src"];
const _hoisted_3$2 = { class: "category-box" };
const _hoisted_4$2 = { class: "category-name" };
const _hoisted_5$2 = { class: "choices-container" };

const {onMounted: onMounted$2,ref: ref$1,computed: computed$2} = await importShared('vue');


const _sfc_main$2 = {
  __name: 'categoryMenus',
  props: {
  category: {
    type: String,
    required: true
  }
},
  setup(__props) {

const menuStore = useMenuStore();

const props = __props;
//je sélection les menus de la catégorie et je les affiche dans le composant choices
const categoryMenus = computed$2(() => {
  const selectedDiets =
    menuStore.selectedMenusList?.diets || [];

  return menuStore.suggestedMenus.filter(menu => {//pour chaque menu suggéré,
    return (
      menu.category === props.category //je vérifie que le menu appartient à la catégorie sélectionnée, 
      && 
      //je prend tout les régimes sélectionnés par le user et je vérifie que le menu les inclus
      selectedDiets.every(diet => menu.diets.includes(diet)) 
      )
  })
});
ref$1(false);



return (_ctx, _cache) => {
  return (_openBlock$4(), _createElementBlock$4("div", null, [
    _createElementVNode$4("div", _hoisted_1$2, [
      _createElementVNode$4("img", {
        src: _unref$3(arrowLeft),
        alt: "Retour",
        class: "back-arrow",
        onClick: _cache[0] || (_cache[0] = $event => (_unref$3(menuStore).showMenusListSelection = false))
      }, null, 8, _hoisted_2$2)
    ]),
    _createElementVNode$4("div", _hoisted_3$2, [
      _createElementVNode$4("p", _hoisted_4$2, _toDisplayString$3(props.category), 1)
    ]),
    _createElementVNode$4("div", _hoisted_5$2, [
      (_openBlock$4(true), _createElementBlock$4(_Fragment$2, null, _renderList$2(categoryMenus.value, (menu, index) => {
        return (_openBlock$4(), _createBlock$1(Choices, {
          key: `menu-${index} `,
          name: menu.name,
          label: menu.label,
          source: menu.source,
          servings: menu.servings,
          diets: menu.diets,
          creationType: menu.creationType,
          ingredients: menu.ingredients,
          category: props.category
        }, null, 8, ["name", "label", "source", "servings", "diets", "creationType", "ingredients", "category"]))
      }), 128))
    ])
  ]))
}
}

};
const CategoryMenus = /*#__PURE__*/_export_sfc(_sfc_main$2, [['__scopeId',"data-v-b75816ed"]]);

function getAugmentedNamespace(n) {
  if (Object.prototype.hasOwnProperty.call(n, '__esModule')) return n;
  var f = n.default;
	if (typeof f == "function") {
		var a = function a () {
			var isInstance = false;
      try {
        isInstance = this instanceof a;
      } catch {}
			if (isInstance) {
        return Reflect.construct(f, arguments, this.constructor);
			}
			return f.apply(this, arguments);
		};
		a.prototype = f.prototype;
  } else a = {};
  Object.defineProperty(a, '__esModule', {value: true});
	Object.keys(n).forEach(function (k) {
		var d = Object.getOwnPropertyDescriptor(n, k);
		Object.defineProperty(a, k, d.get ? d : {
			enumerable: true,
			get: function () {
				return n[k];
			}
		});
	});
	return a;
}

var vuedraggable_umd$1 = {exports: {}};

/**!
 * Sortable 1.10.2
 * @author	RubaXa   <trash@rubaxa.org>
 * @author	owenm    <owen23355@gmail.com>
 * @license MIT
 */
function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function (obj) {
      return typeof obj;
    };
  } else {
    _typeof = function (obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};
    var ownKeys = Object.keys(source);

    if (typeof Object.getOwnPropertySymbols === 'function') {
      ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) {
        return Object.getOwnPropertyDescriptor(source, sym).enumerable;
      }));
    }

    ownKeys.forEach(function (key) {
      _defineProperty(target, key, source[key]);
    });
  }

  return target;
}

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

function _objectWithoutProperties(source, excluded) {
  if (source == null) return {};

  var target = _objectWithoutPropertiesLoose(source, excluded);

  var key, i;

  if (Object.getOwnPropertySymbols) {
    var sourceSymbolKeys = Object.getOwnPropertySymbols(source);

    for (i = 0; i < sourceSymbolKeys.length; i++) {
      key = sourceSymbolKeys[i];
      if (excluded.indexOf(key) >= 0) continue;
      if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
      target[key] = source[key];
    }
  }

  return target;
}

function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread();
}

function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) {
    for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) arr2[i] = arr[i];

    return arr2;
  }
}

function _iterableToArray(iter) {
  if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter);
}

function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance");
}

var version = "1.10.2";

function userAgent(pattern) {
  if (typeof window !== 'undefined' && window.navigator) {
    return !!
    /*@__PURE__*/
    navigator.userAgent.match(pattern);
  }
}

var IE11OrLess = userAgent(/(?:Trident.*rv[ :]?11\.|msie|iemobile|Windows Phone)/i);
var Edge = userAgent(/Edge/i);
var FireFox = userAgent(/firefox/i);
var Safari = userAgent(/safari/i) && !userAgent(/chrome/i) && !userAgent(/android/i);
var IOS = userAgent(/iP(ad|od|hone)/i);
var ChromeForAndroid = userAgent(/chrome/i) && userAgent(/android/i);

var captureMode = {
  capture: false,
  passive: false
};

function on(el, event, fn) {
  el.addEventListener(event, fn, !IE11OrLess && captureMode);
}

function off(el, event, fn) {
  el.removeEventListener(event, fn, !IE11OrLess && captureMode);
}

function matches(
/**HTMLElement*/
el,
/**String*/
selector) {
  if (!selector) return;
  selector[0] === '>' && (selector = selector.substring(1));

  if (el) {
    try {
      if (el.matches) {
        return el.matches(selector);
      } else if (el.msMatchesSelector) {
        return el.msMatchesSelector(selector);
      } else if (el.webkitMatchesSelector) {
        return el.webkitMatchesSelector(selector);
      }
    } catch (_) {
      return false;
    }
  }

  return false;
}

function getParentOrHost(el) {
  return el.host && el !== document && el.host.nodeType ? el.host : el.parentNode;
}

function closest(
/**HTMLElement*/
el,
/**String*/
selector,
/**HTMLElement*/
ctx, includeCTX) {
  if (el) {
    ctx = ctx || document;

    do {
      if (selector != null && (selector[0] === '>' ? el.parentNode === ctx && matches(el, selector) : matches(el, selector)) || includeCTX && el === ctx) {
        return el;
      }

      if (el === ctx) break;
      /* jshint boss:true */
    } while (el = getParentOrHost(el));
  }

  return null;
}

var R_SPACE = /\s+/g;

function toggleClass(el, name, state) {
  if (el && name) {
    if (el.classList) {
      el.classList[state ? 'add' : 'remove'](name);
    } else {
      var className = (' ' + el.className + ' ').replace(R_SPACE, ' ').replace(' ' + name + ' ', ' ');
      el.className = (className + (state ? ' ' + name : '')).replace(R_SPACE, ' ');
    }
  }
}

function css(el, prop, val) {
  var style = el && el.style;

  if (style) {
    if (val === void 0) {
      if (document.defaultView && document.defaultView.getComputedStyle) {
        val = document.defaultView.getComputedStyle(el, '');
      } else if (el.currentStyle) {
        val = el.currentStyle;
      }

      return prop === void 0 ? val : val[prop];
    } else {
      if (!(prop in style) && prop.indexOf('webkit') === -1) {
        prop = '-webkit-' + prop;
      }

      style[prop] = val + (typeof val === 'string' ? '' : 'px');
    }
  }
}

function matrix(el, selfOnly) {
  var appliedTransforms = '';

  if (typeof el === 'string') {
    appliedTransforms = el;
  } else {
    do {
      var transform = css(el, 'transform');

      if (transform && transform !== 'none') {
        appliedTransforms = transform + ' ' + appliedTransforms;
      }
      /* jshint boss:true */

    } while (!selfOnly && (el = el.parentNode));
  }

  var matrixFn = window.DOMMatrix || window.WebKitCSSMatrix || window.CSSMatrix || window.MSCSSMatrix;
  /*jshint -W056 */

  return matrixFn && new matrixFn(appliedTransforms);
}

function find(ctx, tagName, iterator) {
  if (ctx) {
    var list = ctx.getElementsByTagName(tagName),
        i = 0,
        n = list.length;

    if (iterator) {
      for (; i < n; i++) {
        iterator(list[i], i);
      }
    }

    return list;
  }

  return [];
}

function getWindowScrollingElement() {
  var scrollingElement = document.scrollingElement;

  if (scrollingElement) {
    return scrollingElement;
  } else {
    return document.documentElement;
  }
}
/**
 * Returns the "bounding client rect" of given element
 * @param  {HTMLElement} el                       The element whose boundingClientRect is wanted
 * @param  {[Boolean]} relativeToContainingBlock  Whether the rect should be relative to the containing block of (including) the container
 * @param  {[Boolean]} relativeToNonStaticParent  Whether the rect should be relative to the relative parent of (including) the contaienr
 * @param  {[Boolean]} undoScale                  Whether the container's scale() should be undone
 * @param  {[HTMLElement]} container              The parent the element will be placed in
 * @return {Object}                               The boundingClientRect of el, with specified adjustments
 */


function getRect(el, relativeToContainingBlock, relativeToNonStaticParent, undoScale, container) {
  if (!el.getBoundingClientRect && el !== window) return;
  var elRect, top, left, bottom, right, height, width;

  if (el !== window && el !== getWindowScrollingElement()) {
    elRect = el.getBoundingClientRect();
    top = elRect.top;
    left = elRect.left;
    bottom = elRect.bottom;
    right = elRect.right;
    height = elRect.height;
    width = elRect.width;
  } else {
    top = 0;
    left = 0;
    bottom = window.innerHeight;
    right = window.innerWidth;
    height = window.innerHeight;
    width = window.innerWidth;
  }

  if ((relativeToContainingBlock || relativeToNonStaticParent) && el !== window) {
    // Adjust for translate()
    container = container || el.parentNode; // solves #1123 (see: https://stackoverflow.com/a/37953806/6088312)
    // Not needed on <= IE11

    if (!IE11OrLess) {
      do {
        if (container && container.getBoundingClientRect && (css(container, 'transform') !== 'none' || relativeToNonStaticParent && css(container, 'position') !== 'static')) {
          var containerRect = container.getBoundingClientRect(); // Set relative to edges of padding box of container

          top -= containerRect.top + parseInt(css(container, 'border-top-width'));
          left -= containerRect.left + parseInt(css(container, 'border-left-width'));
          bottom = top + elRect.height;
          right = left + elRect.width;
          break;
        }
        /* jshint boss:true */

      } while (container = container.parentNode);
    }
  }

  if (undoScale && el !== window) {
    // Adjust for scale()
    var elMatrix = matrix(container || el),
        scaleX = elMatrix && elMatrix.a,
        scaleY = elMatrix && elMatrix.d;

    if (elMatrix) {
      top /= scaleY;
      left /= scaleX;
      width /= scaleX;
      height /= scaleY;
      bottom = top + height;
      right = left + width;
    }
  }

  return {
    top: top,
    left: left,
    bottom: bottom,
    right: right,
    width: width,
    height: height
  };
}
/**
 * Checks if a side of an element is scrolled past a side of its parents
 * @param  {HTMLElement}  el           The element who's side being scrolled out of view is in question
 * @param  {String}       elSide       Side of the element in question ('top', 'left', 'right', 'bottom')
 * @param  {String}       parentSide   Side of the parent in question ('top', 'left', 'right', 'bottom')
 * @return {HTMLElement}               The parent scroll element that the el's side is scrolled past, or null if there is no such element
 */


function isScrolledPast(el, elSide, parentSide) {
  var parent = getParentAutoScrollElement(el, true),
      elSideVal = getRect(el)[elSide];
  /* jshint boss:true */

  while (parent) {
    var parentSideVal = getRect(parent)[parentSide],
        visible = void 0;

    {
      visible = elSideVal >= parentSideVal;
    }

    if (!visible) return parent;
    if (parent === getWindowScrollingElement()) break;
    parent = getParentAutoScrollElement(parent, false);
  }

  return false;
}
/**
 * Gets nth child of el, ignoring hidden children, sortable's elements (does not ignore clone if it's visible)
 * and non-draggable elements
 * @param  {HTMLElement} el       The parent element
 * @param  {Number} childNum      The index of the child
 * @param  {Object} options       Parent Sortable's options
 * @return {HTMLElement}          The child at index childNum, or null if not found
 */


function getChild(el, childNum, options) {
  var currentChild = 0,
      i = 0,
      children = el.children;

  while (i < children.length) {
    if (children[i].style.display !== 'none' && children[i] !== Sortable.ghost && children[i] !== Sortable.dragged && closest(children[i], options.draggable, el, false)) {
      if (currentChild === childNum) {
        return children[i];
      }

      currentChild++;
    }

    i++;
  }

  return null;
}
/**
 * Gets the last child in the el, ignoring ghostEl or invisible elements (clones)
 * @param  {HTMLElement} el       Parent element
 * @param  {selector} selector    Any other elements that should be ignored
 * @return {HTMLElement}          The last child, ignoring ghostEl
 */


function lastChild(el, selector) {
  var last = el.lastElementChild;

  while (last && (last === Sortable.ghost || css(last, 'display') === 'none' || selector && !matches(last, selector))) {
    last = last.previousElementSibling;
  }

  return last || null;
}
/**
 * Returns the index of an element within its parent for a selected set of
 * elements
 * @param  {HTMLElement} el
 * @param  {selector} selector
 * @return {number}
 */


function index(el, selector) {
  var index = 0;

  if (!el || !el.parentNode) {
    return -1;
  }
  /* jshint boss:true */


  while (el = el.previousElementSibling) {
    if (el.nodeName.toUpperCase() !== 'TEMPLATE' && el !== Sortable.clone && (!selector || matches(el, selector))) {
      index++;
    }
  }

  return index;
}
/**
 * Returns the scroll offset of the given element, added with all the scroll offsets of parent elements.
 * The value is returned in real pixels.
 * @param  {HTMLElement} el
 * @return {Array}             Offsets in the format of [left, top]
 */


function getRelativeScrollOffset(el) {
  var offsetLeft = 0,
      offsetTop = 0,
      winScroller = getWindowScrollingElement();

  if (el) {
    do {
      var elMatrix = matrix(el),
          scaleX = elMatrix.a,
          scaleY = elMatrix.d;
      offsetLeft += el.scrollLeft * scaleX;
      offsetTop += el.scrollTop * scaleY;
    } while (el !== winScroller && (el = el.parentNode));
  }

  return [offsetLeft, offsetTop];
}
/**
 * Returns the index of the object within the given array
 * @param  {Array} arr   Array that may or may not hold the object
 * @param  {Object} obj  An object that has a key-value pair unique to and identical to a key-value pair in the object you want to find
 * @return {Number}      The index of the object in the array, or -1
 */


function indexOfObject(arr, obj) {
  for (var i in arr) {
    if (!arr.hasOwnProperty(i)) continue;

    for (var key in obj) {
      if (obj.hasOwnProperty(key) && obj[key] === arr[i][key]) return Number(i);
    }
  }

  return -1;
}

function getParentAutoScrollElement(el, includeSelf) {
  // skip to window
  if (!el || !el.getBoundingClientRect) return getWindowScrollingElement();
  var elem = el;
  var gotSelf = false;

  do {
    // we don't need to get elem css if it isn't even overflowing in the first place (performance)
    if (elem.clientWidth < elem.scrollWidth || elem.clientHeight < elem.scrollHeight) {
      var elemCSS = css(elem);

      if (elem.clientWidth < elem.scrollWidth && (elemCSS.overflowX == 'auto' || elemCSS.overflowX == 'scroll') || elem.clientHeight < elem.scrollHeight && (elemCSS.overflowY == 'auto' || elemCSS.overflowY == 'scroll')) {
        if (!elem.getBoundingClientRect || elem === document.body) return getWindowScrollingElement();
        if (gotSelf || includeSelf) return elem;
        gotSelf = true;
      }
    }
    /* jshint boss:true */

  } while (elem = elem.parentNode);

  return getWindowScrollingElement();
}

function extend(dst, src) {
  if (dst && src) {
    for (var key in src) {
      if (src.hasOwnProperty(key)) {
        dst[key] = src[key];
      }
    }
  }

  return dst;
}

function isRectEqual(rect1, rect2) {
  return Math.round(rect1.top) === Math.round(rect2.top) && Math.round(rect1.left) === Math.round(rect2.left) && Math.round(rect1.height) === Math.round(rect2.height) && Math.round(rect1.width) === Math.round(rect2.width);
}

var _throttleTimeout;

function throttle(callback, ms) {
  return function () {
    if (!_throttleTimeout) {
      var args = arguments,
          _this = this;

      if (args.length === 1) {
        callback.call(_this, args[0]);
      } else {
        callback.apply(_this, args);
      }

      _throttleTimeout = setTimeout(function () {
        _throttleTimeout = void 0;
      }, ms);
    }
  };
}

function cancelThrottle() {
  clearTimeout(_throttleTimeout);
  _throttleTimeout = void 0;
}

function scrollBy(el, x, y) {
  el.scrollLeft += x;
  el.scrollTop += y;
}

function clone(el) {
  var Polymer = window.Polymer;
  var $ = window.jQuery || window.Zepto;

  if (Polymer && Polymer.dom) {
    return Polymer.dom(el).cloneNode(true);
  } else if ($) {
    return $(el).clone(true)[0];
  } else {
    return el.cloneNode(true);
  }
}

function setRect(el, rect) {
  css(el, 'position', 'absolute');
  css(el, 'top', rect.top);
  css(el, 'left', rect.left);
  css(el, 'width', rect.width);
  css(el, 'height', rect.height);
}

function unsetRect(el) {
  css(el, 'position', '');
  css(el, 'top', '');
  css(el, 'left', '');
  css(el, 'width', '');
  css(el, 'height', '');
}

var expando = 'Sortable' + new Date().getTime();

function AnimationStateManager() {
  var animationStates = [],
      animationCallbackId;
  return {
    captureAnimationState: function captureAnimationState() {
      animationStates = [];
      if (!this.options.animation) return;
      var children = [].slice.call(this.el.children);
      children.forEach(function (child) {
        if (css(child, 'display') === 'none' || child === Sortable.ghost) return;
        animationStates.push({
          target: child,
          rect: getRect(child)
        });

        var fromRect = _objectSpread({}, animationStates[animationStates.length - 1].rect); // If animating: compensate for current animation


        if (child.thisAnimationDuration) {
          var childMatrix = matrix(child, true);

          if (childMatrix) {
            fromRect.top -= childMatrix.f;
            fromRect.left -= childMatrix.e;
          }
        }

        child.fromRect = fromRect;
      });
    },
    addAnimationState: function addAnimationState(state) {
      animationStates.push(state);
    },
    removeAnimationState: function removeAnimationState(target) {
      animationStates.splice(indexOfObject(animationStates, {
        target: target
      }), 1);
    },
    animateAll: function animateAll(callback) {
      var _this = this;

      if (!this.options.animation) {
        clearTimeout(animationCallbackId);
        if (typeof callback === 'function') callback();
        return;
      }

      var animating = false,
          animationTime = 0;
      animationStates.forEach(function (state) {
        var time = 0,
            target = state.target,
            fromRect = target.fromRect,
            toRect = getRect(target),
            prevFromRect = target.prevFromRect,
            prevToRect = target.prevToRect,
            animatingRect = state.rect,
            targetMatrix = matrix(target, true);

        if (targetMatrix) {
          // Compensate for current animation
          toRect.top -= targetMatrix.f;
          toRect.left -= targetMatrix.e;
        }

        target.toRect = toRect;

        if (target.thisAnimationDuration) {
          // Could also check if animatingRect is between fromRect and toRect
          if (isRectEqual(prevFromRect, toRect) && !isRectEqual(fromRect, toRect) && // Make sure animatingRect is on line between toRect & fromRect
          (animatingRect.top - toRect.top) / (animatingRect.left - toRect.left) === (fromRect.top - toRect.top) / (fromRect.left - toRect.left)) {
            // If returning to same place as started from animation and on same axis
            time = calculateRealTime(animatingRect, prevFromRect, prevToRect, _this.options);
          }
        } // if fromRect != toRect: animate


        if (!isRectEqual(toRect, fromRect)) {
          target.prevFromRect = fromRect;
          target.prevToRect = toRect;

          if (!time) {
            time = _this.options.animation;
          }

          _this.animate(target, animatingRect, toRect, time);
        }

        if (time) {
          animating = true;
          animationTime = Math.max(animationTime, time);
          clearTimeout(target.animationResetTimer);
          target.animationResetTimer = setTimeout(function () {
            target.animationTime = 0;
            target.prevFromRect = null;
            target.fromRect = null;
            target.prevToRect = null;
            target.thisAnimationDuration = null;
          }, time);
          target.thisAnimationDuration = time;
        }
      });
      clearTimeout(animationCallbackId);

      if (!animating) {
        if (typeof callback === 'function') callback();
      } else {
        animationCallbackId = setTimeout(function () {
          if (typeof callback === 'function') callback();
        }, animationTime);
      }

      animationStates = [];
    },
    animate: function animate(target, currentRect, toRect, duration) {
      if (duration) {
        css(target, 'transition', '');
        css(target, 'transform', '');
        var elMatrix = matrix(this.el),
            scaleX = elMatrix && elMatrix.a,
            scaleY = elMatrix && elMatrix.d,
            translateX = (currentRect.left - toRect.left) / (scaleX || 1),
            translateY = (currentRect.top - toRect.top) / (scaleY || 1);
        target.animatingX = !!translateX;
        target.animatingY = !!translateY;
        css(target, 'transform', 'translate3d(' + translateX + 'px,' + translateY + 'px,0)');
        repaint(target); // repaint

        css(target, 'transition', 'transform ' + duration + 'ms' + (this.options.easing ? ' ' + this.options.easing : ''));
        css(target, 'transform', 'translate3d(0,0,0)');
        typeof target.animated === 'number' && clearTimeout(target.animated);
        target.animated = setTimeout(function () {
          css(target, 'transition', '');
          css(target, 'transform', '');
          target.animated = false;
          target.animatingX = false;
          target.animatingY = false;
        }, duration);
      }
    }
  };
}

function repaint(target) {
  return target.offsetWidth;
}

function calculateRealTime(animatingRect, fromRect, toRect, options) {
  return Math.sqrt(Math.pow(fromRect.top - animatingRect.top, 2) + Math.pow(fromRect.left - animatingRect.left, 2)) / Math.sqrt(Math.pow(fromRect.top - toRect.top, 2) + Math.pow(fromRect.left - toRect.left, 2)) * options.animation;
}

var plugins = [];
var defaults = {
  initializeByDefault: true
};
var PluginManager = {
  mount: function mount(plugin) {
    // Set default static properties
    for (var option in defaults) {
      if (defaults.hasOwnProperty(option) && !(option in plugin)) {
        plugin[option] = defaults[option];
      }
    }

    plugins.push(plugin);
  },
  pluginEvent: function pluginEvent(eventName, sortable, evt) {
    var _this = this;

    this.eventCanceled = false;

    evt.cancel = function () {
      _this.eventCanceled = true;
    };

    var eventNameGlobal = eventName + 'Global';
    plugins.forEach(function (plugin) {
      if (!sortable[plugin.pluginName]) return; // Fire global events if it exists in this sortable

      if (sortable[plugin.pluginName][eventNameGlobal]) {
        sortable[plugin.pluginName][eventNameGlobal](_objectSpread({
          sortable: sortable
        }, evt));
      } // Only fire plugin event if plugin is enabled in this sortable,
      // and plugin has event defined


      if (sortable.options[plugin.pluginName] && sortable[plugin.pluginName][eventName]) {
        sortable[plugin.pluginName][eventName](_objectSpread({
          sortable: sortable
        }, evt));
      }
    });
  },
  initializePlugins: function initializePlugins(sortable, el, defaults, options) {
    plugins.forEach(function (plugin) {
      var pluginName = plugin.pluginName;
      if (!sortable.options[pluginName] && !plugin.initializeByDefault) return;
      var initialized = new plugin(sortable, el, sortable.options);
      initialized.sortable = sortable;
      initialized.options = sortable.options;
      sortable[pluginName] = initialized; // Add default options from plugin

      _extends(defaults, initialized.defaults);
    });

    for (var option in sortable.options) {
      if (!sortable.options.hasOwnProperty(option)) continue;
      var modified = this.modifyOption(sortable, option, sortable.options[option]);

      if (typeof modified !== 'undefined') {
        sortable.options[option] = modified;
      }
    }
  },
  getEventProperties: function getEventProperties(name, sortable) {
    var eventProperties = {};
    plugins.forEach(function (plugin) {
      if (typeof plugin.eventProperties !== 'function') return;

      _extends(eventProperties, plugin.eventProperties.call(sortable[plugin.pluginName], name));
    });
    return eventProperties;
  },
  modifyOption: function modifyOption(sortable, name, value) {
    var modifiedValue;
    plugins.forEach(function (plugin) {
      // Plugin must exist on the Sortable
      if (!sortable[plugin.pluginName]) return; // If static option listener exists for this option, call in the context of the Sortable's instance of this plugin

      if (plugin.optionListeners && typeof plugin.optionListeners[name] === 'function') {
        modifiedValue = plugin.optionListeners[name].call(sortable[plugin.pluginName], value);
      }
    });
    return modifiedValue;
  }
};

function dispatchEvent(_ref) {
  var sortable = _ref.sortable,
      rootEl = _ref.rootEl,
      name = _ref.name,
      targetEl = _ref.targetEl,
      cloneEl = _ref.cloneEl,
      toEl = _ref.toEl,
      fromEl = _ref.fromEl,
      oldIndex = _ref.oldIndex,
      newIndex = _ref.newIndex,
      oldDraggableIndex = _ref.oldDraggableIndex,
      newDraggableIndex = _ref.newDraggableIndex,
      originalEvent = _ref.originalEvent,
      putSortable = _ref.putSortable,
      extraEventProperties = _ref.extraEventProperties;
  sortable = sortable || rootEl && rootEl[expando];
  if (!sortable) return;
  var evt,
      options = sortable.options,
      onName = 'on' + name.charAt(0).toUpperCase() + name.substr(1); // Support for new CustomEvent feature

  if (window.CustomEvent && !IE11OrLess && !Edge) {
    evt = new CustomEvent(name, {
      bubbles: true,
      cancelable: true
    });
  } else {
    evt = document.createEvent('Event');
    evt.initEvent(name, true, true);
  }

  evt.to = toEl || rootEl;
  evt.from = fromEl || rootEl;
  evt.item = targetEl || rootEl;
  evt.clone = cloneEl;
  evt.oldIndex = oldIndex;
  evt.newIndex = newIndex;
  evt.oldDraggableIndex = oldDraggableIndex;
  evt.newDraggableIndex = newDraggableIndex;
  evt.originalEvent = originalEvent;
  evt.pullMode = putSortable ? putSortable.lastPutMode : undefined;

  var allEventProperties = _objectSpread({}, extraEventProperties, PluginManager.getEventProperties(name, sortable));

  for (var option in allEventProperties) {
    evt[option] = allEventProperties[option];
  }

  if (rootEl) {
    rootEl.dispatchEvent(evt);
  }

  if (options[onName]) {
    options[onName].call(sortable, evt);
  }
}

var pluginEvent = function pluginEvent(eventName, sortable) {
  var _ref = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {},
      originalEvent = _ref.evt,
      data = _objectWithoutProperties(_ref, ["evt"]);

  PluginManager.pluginEvent.bind(Sortable)(eventName, sortable, _objectSpread({
    dragEl: dragEl,
    parentEl: parentEl,
    ghostEl: ghostEl,
    rootEl: rootEl,
    nextEl: nextEl,
    lastDownEl: lastDownEl,
    cloneEl: cloneEl,
    cloneHidden: cloneHidden,
    dragStarted: moved,
    putSortable: putSortable,
    activeSortable: Sortable.active,
    originalEvent: originalEvent,
    oldIndex: oldIndex,
    oldDraggableIndex: oldDraggableIndex,
    newIndex: newIndex,
    newDraggableIndex: newDraggableIndex,
    hideGhostForTarget: _hideGhostForTarget,
    unhideGhostForTarget: _unhideGhostForTarget,
    cloneNowHidden: function cloneNowHidden() {
      cloneHidden = true;
    },
    cloneNowShown: function cloneNowShown() {
      cloneHidden = false;
    },
    dispatchSortableEvent: function dispatchSortableEvent(name) {
      _dispatchEvent({
        sortable: sortable,
        name: name,
        originalEvent: originalEvent
      });
    }
  }, data));
};

function _dispatchEvent(info) {
  dispatchEvent(_objectSpread({
    putSortable: putSortable,
    cloneEl: cloneEl,
    targetEl: dragEl,
    rootEl: rootEl,
    oldIndex: oldIndex,
    oldDraggableIndex: oldDraggableIndex,
    newIndex: newIndex,
    newDraggableIndex: newDraggableIndex
  }, info));
}

var dragEl,
    parentEl,
    ghostEl,
    rootEl,
    nextEl,
    lastDownEl,
    cloneEl,
    cloneHidden,
    oldIndex,
    newIndex,
    oldDraggableIndex,
    newDraggableIndex,
    activeGroup,
    putSortable,
    awaitingDragStarted = false,
    ignoreNextClick = false,
    sortables = [],
    tapEvt,
    touchEvt,
    lastDx,
    lastDy,
    tapDistanceLeft,
    tapDistanceTop,
    moved,
    lastTarget,
    lastDirection,
    pastFirstInvertThresh = false,
    isCircumstantialInvert = false,
    targetMoveDistance,
    // For positioning ghost absolutely
ghostRelativeParent,
    ghostRelativeParentInitialScroll = [],
    // (left, top)
_silent = false,
    savedInputChecked = [];
/** @const */

var documentExists = typeof document !== 'undefined',
    PositionGhostAbsolutely = IOS,
    CSSFloatProperty = Edge || IE11OrLess ? 'cssFloat' : 'float',
    // This will not pass for IE9, because IE9 DnD only works on anchors
supportDraggable = documentExists && !ChromeForAndroid && !IOS && 'draggable' in document.createElement('div'),
    supportCssPointerEvents = function () {
  if (!documentExists) return; // false when <= IE11

  if (IE11OrLess) {
    return false;
  }

  var el = document.createElement('x');
  el.style.cssText = 'pointer-events:auto';
  return el.style.pointerEvents === 'auto';
}(),
    _detectDirection = function _detectDirection(el, options) {
  var elCSS = css(el),
      elWidth = parseInt(elCSS.width) - parseInt(elCSS.paddingLeft) - parseInt(elCSS.paddingRight) - parseInt(elCSS.borderLeftWidth) - parseInt(elCSS.borderRightWidth),
      child1 = getChild(el, 0, options),
      child2 = getChild(el, 1, options),
      firstChildCSS = child1 && css(child1),
      secondChildCSS = child2 && css(child2),
      firstChildWidth = firstChildCSS && parseInt(firstChildCSS.marginLeft) + parseInt(firstChildCSS.marginRight) + getRect(child1).width,
      secondChildWidth = secondChildCSS && parseInt(secondChildCSS.marginLeft) + parseInt(secondChildCSS.marginRight) + getRect(child2).width;

  if (elCSS.display === 'flex') {
    return elCSS.flexDirection === 'column' || elCSS.flexDirection === 'column-reverse' ? 'vertical' : 'horizontal';
  }

  if (elCSS.display === 'grid') {
    return elCSS.gridTemplateColumns.split(' ').length <= 1 ? 'vertical' : 'horizontal';
  }

  if (child1 && firstChildCSS["float"] && firstChildCSS["float"] !== 'none') {
    var touchingSideChild2 = firstChildCSS["float"] === 'left' ? 'left' : 'right';
    return child2 && (secondChildCSS.clear === 'both' || secondChildCSS.clear === touchingSideChild2) ? 'vertical' : 'horizontal';
  }

  return child1 && (firstChildCSS.display === 'block' || firstChildCSS.display === 'flex' || firstChildCSS.display === 'table' || firstChildCSS.display === 'grid' || firstChildWidth >= elWidth && elCSS[CSSFloatProperty] === 'none' || child2 && elCSS[CSSFloatProperty] === 'none' && firstChildWidth + secondChildWidth > elWidth) ? 'vertical' : 'horizontal';
},
    _dragElInRowColumn = function _dragElInRowColumn(dragRect, targetRect, vertical) {
  var dragElS1Opp = vertical ? dragRect.left : dragRect.top,
      dragElS2Opp = vertical ? dragRect.right : dragRect.bottom,
      dragElOppLength = vertical ? dragRect.width : dragRect.height,
      targetS1Opp = vertical ? targetRect.left : targetRect.top,
      targetS2Opp = vertical ? targetRect.right : targetRect.bottom,
      targetOppLength = vertical ? targetRect.width : targetRect.height;
  return dragElS1Opp === targetS1Opp || dragElS2Opp === targetS2Opp || dragElS1Opp + dragElOppLength / 2 === targetS1Opp + targetOppLength / 2;
},

/**
 * Detects first nearest empty sortable to X and Y position using emptyInsertThreshold.
 * @param  {Number} x      X position
 * @param  {Number} y      Y position
 * @return {HTMLElement}   Element of the first found nearest Sortable
 */
_detectNearestEmptySortable = function _detectNearestEmptySortable(x, y) {
  var ret;
  sortables.some(function (sortable) {
    if (lastChild(sortable)) return;
    var rect = getRect(sortable),
        threshold = sortable[expando].options.emptyInsertThreshold,
        insideHorizontally = x >= rect.left - threshold && x <= rect.right + threshold,
        insideVertically = y >= rect.top - threshold && y <= rect.bottom + threshold;

    if (threshold && insideHorizontally && insideVertically) {
      return ret = sortable;
    }
  });
  return ret;
},
    _prepareGroup = function _prepareGroup(options) {
  function toFn(value, pull) {
    return function (to, from, dragEl, evt) {
      var sameGroup = to.options.group.name && from.options.group.name && to.options.group.name === from.options.group.name;

      if (value == null && (pull || sameGroup)) {
        // Default pull value
        // Default pull and put value if same group
        return true;
      } else if (value == null || value === false) {
        return false;
      } else if (pull && value === 'clone') {
        return value;
      } else if (typeof value === 'function') {
        return toFn(value(to, from, dragEl, evt), pull)(to, from, dragEl, evt);
      } else {
        var otherGroup = (pull ? to : from).options.group.name;
        return value === true || typeof value === 'string' && value === otherGroup || value.join && value.indexOf(otherGroup) > -1;
      }
    };
  }

  var group = {};
  var originalGroup = options.group;

  if (!originalGroup || _typeof(originalGroup) != 'object') {
    originalGroup = {
      name: originalGroup
    };
  }

  group.name = originalGroup.name;
  group.checkPull = toFn(originalGroup.pull, true);
  group.checkPut = toFn(originalGroup.put);
  group.revertClone = originalGroup.revertClone;
  options.group = group;
},
    _hideGhostForTarget = function _hideGhostForTarget() {
  if (!supportCssPointerEvents && ghostEl) {
    css(ghostEl, 'display', 'none');
  }
},
    _unhideGhostForTarget = function _unhideGhostForTarget() {
  if (!supportCssPointerEvents && ghostEl) {
    css(ghostEl, 'display', '');
  }
}; // #1184 fix - Prevent click event on fallback if dragged but item not changed position


if (documentExists) {
  document.addEventListener('click', function (evt) {
    if (ignoreNextClick) {
      evt.preventDefault();
      evt.stopPropagation && evt.stopPropagation();
      evt.stopImmediatePropagation && evt.stopImmediatePropagation();
      ignoreNextClick = false;
      return false;
    }
  }, true);
}

var nearestEmptyInsertDetectEvent = function nearestEmptyInsertDetectEvent(evt) {
  if (dragEl) {
    evt = evt.touches ? evt.touches[0] : evt;

    var nearest = _detectNearestEmptySortable(evt.clientX, evt.clientY);

    if (nearest) {
      // Create imitation event
      var event = {};

      for (var i in evt) {
        if (evt.hasOwnProperty(i)) {
          event[i] = evt[i];
        }
      }

      event.target = event.rootEl = nearest;
      event.preventDefault = void 0;
      event.stopPropagation = void 0;

      nearest[expando]._onDragOver(event);
    }
  }
};

var _checkOutsideTargetEl = function _checkOutsideTargetEl(evt) {
  if (dragEl) {
    dragEl.parentNode[expando]._isOutsideThisEl(evt.target);
  }
};
/**
 * @class  Sortable
 * @param  {HTMLElement}  el
 * @param  {Object}       [options]
 */


function Sortable(el, options) {
  if (!(el && el.nodeType && el.nodeType === 1)) {
    throw "Sortable: `el` must be an HTMLElement, not ".concat({}.toString.call(el));
  }

  this.el = el; // root element

  this.options = options = _extends({}, options); // Export instance

  el[expando] = this;
  var defaults = {
    group: null,
    sort: true,
    disabled: false,
    store: null,
    handle: null,
    draggable: /^[uo]l$/i.test(el.nodeName) ? '>li' : '>*',
    swapThreshold: 1,
    // percentage; 0 <= x <= 1
    invertSwap: false,
    // invert always
    invertedSwapThreshold: null,
    // will be set to same as swapThreshold if default
    removeCloneOnHide: true,
    direction: function direction() {
      return _detectDirection(el, this.options);
    },
    ghostClass: 'sortable-ghost',
    chosenClass: 'sortable-chosen',
    dragClass: 'sortable-drag',
    ignore: 'a, img',
    filter: null,
    preventOnFilter: true,
    animation: 0,
    easing: null,
    setData: function setData(dataTransfer, dragEl) {
      dataTransfer.setData('Text', dragEl.textContent);
    },
    dropBubble: false,
    dragoverBubble: false,
    dataIdAttr: 'data-id',
    delay: 0,
    delayOnTouchOnly: false,
    touchStartThreshold: (Number.parseInt ? Number : window).parseInt(window.devicePixelRatio, 10) || 1,
    forceFallback: false,
    fallbackClass: 'sortable-fallback',
    fallbackOnBody: false,
    fallbackTolerance: 0,
    fallbackOffset: {
      x: 0,
      y: 0
    },
    supportPointer: Sortable.supportPointer !== false && 'PointerEvent' in window,
    emptyInsertThreshold: 5
  };
  PluginManager.initializePlugins(this, el, defaults); // Set default options

  for (var name in defaults) {
    !(name in options) && (options[name] = defaults[name]);
  }

  _prepareGroup(options); // Bind all private methods


  for (var fn in this) {
    if (fn.charAt(0) === '_' && typeof this[fn] === 'function') {
      this[fn] = this[fn].bind(this);
    }
  } // Setup drag mode


  this.nativeDraggable = options.forceFallback ? false : supportDraggable;

  if (this.nativeDraggable) {
    // Touch start threshold cannot be greater than the native dragstart threshold
    this.options.touchStartThreshold = 1;
  } // Bind events


  if (options.supportPointer) {
    on(el, 'pointerdown', this._onTapStart);
  } else {
    on(el, 'mousedown', this._onTapStart);
    on(el, 'touchstart', this._onTapStart);
  }

  if (this.nativeDraggable) {
    on(el, 'dragover', this);
    on(el, 'dragenter', this);
  }

  sortables.push(this.el); // Restore sorting

  options.store && options.store.get && this.sort(options.store.get(this) || []); // Add animation state manager

  _extends(this, AnimationStateManager());
}

Sortable.prototype =
/** @lends Sortable.prototype */
{
  constructor: Sortable,
  _isOutsideThisEl: function _isOutsideThisEl(target) {
    if (!this.el.contains(target) && target !== this.el) {
      lastTarget = null;
    }
  },
  _getDirection: function _getDirection(evt, target) {
    return typeof this.options.direction === 'function' ? this.options.direction.call(this, evt, target, dragEl) : this.options.direction;
  },
  _onTapStart: function _onTapStart(
  /** Event|TouchEvent */
  evt) {
    if (!evt.cancelable) return;

    var _this = this,
        el = this.el,
        options = this.options,
        preventOnFilter = options.preventOnFilter,
        type = evt.type,
        touch = evt.touches && evt.touches[0] || evt.pointerType && evt.pointerType === 'touch' && evt,
        target = (touch || evt).target,
        originalTarget = evt.target.shadowRoot && (evt.path && evt.path[0] || evt.composedPath && evt.composedPath()[0]) || target,
        filter = options.filter;

    _saveInputCheckedState(el); // Don't trigger start event when an element is been dragged, otherwise the evt.oldindex always wrong when set option.group.


    if (dragEl) {
      return;
    }

    if (/mousedown|pointerdown/.test(type) && evt.button !== 0 || options.disabled) {
      return; // only left button and enabled
    } // cancel dnd if original target is content editable


    if (originalTarget.isContentEditable) {
      return;
    }

    target = closest(target, options.draggable, el, false);

    if (target && target.animated) {
      return;
    }

    if (lastDownEl === target) {
      // Ignoring duplicate `down`
      return;
    } // Get the index of the dragged element within its parent


    oldIndex = index(target);
    oldDraggableIndex = index(target, options.draggable); // Check filter

    if (typeof filter === 'function') {
      if (filter.call(this, evt, target, this)) {
        _dispatchEvent({
          sortable: _this,
          rootEl: originalTarget,
          name: 'filter',
          targetEl: target,
          toEl: el,
          fromEl: el
        });

        pluginEvent('filter', _this, {
          evt: evt
        });
        preventOnFilter && evt.cancelable && evt.preventDefault();
        return; // cancel dnd
      }
    } else if (filter) {
      filter = filter.split(',').some(function (criteria) {
        criteria = closest(originalTarget, criteria.trim(), el, false);

        if (criteria) {
          _dispatchEvent({
            sortable: _this,
            rootEl: criteria,
            name: 'filter',
            targetEl: target,
            fromEl: el,
            toEl: el
          });

          pluginEvent('filter', _this, {
            evt: evt
          });
          return true;
        }
      });

      if (filter) {
        preventOnFilter && evt.cancelable && evt.preventDefault();
        return; // cancel dnd
      }
    }

    if (options.handle && !closest(originalTarget, options.handle, el, false)) {
      return;
    } // Prepare `dragstart`


    this._prepareDragStart(evt, touch, target);
  },
  _prepareDragStart: function _prepareDragStart(
  /** Event */
  evt,
  /** Touch */
  touch,
  /** HTMLElement */
  target) {
    var _this = this,
        el = _this.el,
        options = _this.options,
        ownerDocument = el.ownerDocument,
        dragStartFn;

    if (target && !dragEl && target.parentNode === el) {
      var dragRect = getRect(target);
      rootEl = el;
      dragEl = target;
      parentEl = dragEl.parentNode;
      nextEl = dragEl.nextSibling;
      lastDownEl = target;
      activeGroup = options.group;
      Sortable.dragged = dragEl;
      tapEvt = {
        target: dragEl,
        clientX: (touch || evt).clientX,
        clientY: (touch || evt).clientY
      };
      tapDistanceLeft = tapEvt.clientX - dragRect.left;
      tapDistanceTop = tapEvt.clientY - dragRect.top;
      this._lastX = (touch || evt).clientX;
      this._lastY = (touch || evt).clientY;
      dragEl.style['will-change'] = 'all';

      dragStartFn = function dragStartFn() {
        pluginEvent('delayEnded', _this, {
          evt: evt
        });

        if (Sortable.eventCanceled) {
          _this._onDrop();

          return;
        } // Delayed drag has been triggered
        // we can re-enable the events: touchmove/mousemove


        _this._disableDelayedDragEvents();

        if (!FireFox && _this.nativeDraggable) {
          dragEl.draggable = true;
        } // Bind the events: dragstart/dragend


        _this._triggerDragStart(evt, touch); // Drag start event


        _dispatchEvent({
          sortable: _this,
          name: 'choose',
          originalEvent: evt
        }); // Chosen item


        toggleClass(dragEl, options.chosenClass, true);
      }; // Disable "draggable"


      options.ignore.split(',').forEach(function (criteria) {
        find(dragEl, criteria.trim(), _disableDraggable);
      });
      on(ownerDocument, 'dragover', nearestEmptyInsertDetectEvent);
      on(ownerDocument, 'mousemove', nearestEmptyInsertDetectEvent);
      on(ownerDocument, 'touchmove', nearestEmptyInsertDetectEvent);
      on(ownerDocument, 'mouseup', _this._onDrop);
      on(ownerDocument, 'touchend', _this._onDrop);
      on(ownerDocument, 'touchcancel', _this._onDrop); // Make dragEl draggable (must be before delay for FireFox)

      if (FireFox && this.nativeDraggable) {
        this.options.touchStartThreshold = 4;
        dragEl.draggable = true;
      }

      pluginEvent('delayStart', this, {
        evt: evt
      }); // Delay is impossible for native DnD in Edge or IE

      if (options.delay && (!options.delayOnTouchOnly || touch) && (!this.nativeDraggable || !(Edge || IE11OrLess))) {
        if (Sortable.eventCanceled) {
          this._onDrop();

          return;
        } // If the user moves the pointer or let go the click or touch
        // before the delay has been reached:
        // disable the delayed drag


        on(ownerDocument, 'mouseup', _this._disableDelayedDrag);
        on(ownerDocument, 'touchend', _this._disableDelayedDrag);
        on(ownerDocument, 'touchcancel', _this._disableDelayedDrag);
        on(ownerDocument, 'mousemove', _this._delayedDragTouchMoveHandler);
        on(ownerDocument, 'touchmove', _this._delayedDragTouchMoveHandler);
        options.supportPointer && on(ownerDocument, 'pointermove', _this._delayedDragTouchMoveHandler);
        _this._dragStartTimer = setTimeout(dragStartFn, options.delay);
      } else {
        dragStartFn();
      }
    }
  },
  _delayedDragTouchMoveHandler: function _delayedDragTouchMoveHandler(
  /** TouchEvent|PointerEvent **/
  e) {
    var touch = e.touches ? e.touches[0] : e;

    if (Math.max(Math.abs(touch.clientX - this._lastX), Math.abs(touch.clientY - this._lastY)) >= Math.floor(this.options.touchStartThreshold / (this.nativeDraggable && window.devicePixelRatio || 1))) {
      this._disableDelayedDrag();
    }
  },
  _disableDelayedDrag: function _disableDelayedDrag() {
    dragEl && _disableDraggable(dragEl);
    clearTimeout(this._dragStartTimer);

    this._disableDelayedDragEvents();
  },
  _disableDelayedDragEvents: function _disableDelayedDragEvents() {
    var ownerDocument = this.el.ownerDocument;
    off(ownerDocument, 'mouseup', this._disableDelayedDrag);
    off(ownerDocument, 'touchend', this._disableDelayedDrag);
    off(ownerDocument, 'touchcancel', this._disableDelayedDrag);
    off(ownerDocument, 'mousemove', this._delayedDragTouchMoveHandler);
    off(ownerDocument, 'touchmove', this._delayedDragTouchMoveHandler);
    off(ownerDocument, 'pointermove', this._delayedDragTouchMoveHandler);
  },
  _triggerDragStart: function _triggerDragStart(
  /** Event */
  evt,
  /** Touch */
  touch) {
    touch = touch || evt.pointerType == 'touch' && evt;

    if (!this.nativeDraggable || touch) {
      if (this.options.supportPointer) {
        on(document, 'pointermove', this._onTouchMove);
      } else if (touch) {
        on(document, 'touchmove', this._onTouchMove);
      } else {
        on(document, 'mousemove', this._onTouchMove);
      }
    } else {
      on(dragEl, 'dragend', this);
      on(rootEl, 'dragstart', this._onDragStart);
    }

    try {
      if (document.selection) {
        // Timeout neccessary for IE9
        _nextTick(function () {
          document.selection.empty();
        });
      } else {
        window.getSelection().removeAllRanges();
      }
    } catch (err) {}
  },
  _dragStarted: function _dragStarted(fallback, evt) {

    awaitingDragStarted = false;

    if (rootEl && dragEl) {
      pluginEvent('dragStarted', this, {
        evt: evt
      });

      if (this.nativeDraggable) {
        on(document, 'dragover', _checkOutsideTargetEl);
      }

      var options = this.options; // Apply effect

      !fallback && toggleClass(dragEl, options.dragClass, false);
      toggleClass(dragEl, options.ghostClass, true);
      Sortable.active = this;
      fallback && this._appendGhost(); // Drag start event

      _dispatchEvent({
        sortable: this,
        name: 'start',
        originalEvent: evt
      });
    } else {
      this._nulling();
    }
  },
  _emulateDragOver: function _emulateDragOver() {
    if (touchEvt) {
      this._lastX = touchEvt.clientX;
      this._lastY = touchEvt.clientY;

      _hideGhostForTarget();

      var target = document.elementFromPoint(touchEvt.clientX, touchEvt.clientY);
      var parent = target;

      while (target && target.shadowRoot) {
        target = target.shadowRoot.elementFromPoint(touchEvt.clientX, touchEvt.clientY);
        if (target === parent) break;
        parent = target;
      }

      dragEl.parentNode[expando]._isOutsideThisEl(target);

      if (parent) {
        do {
          if (parent[expando]) {
            var inserted = void 0;
            inserted = parent[expando]._onDragOver({
              clientX: touchEvt.clientX,
              clientY: touchEvt.clientY,
              target: target,
              rootEl: parent
            });

            if (inserted && !this.options.dragoverBubble) {
              break;
            }
          }

          target = parent; // store last element
        }
        /* jshint boss:true */
        while (parent = parent.parentNode);
      }

      _unhideGhostForTarget();
    }
  },
  _onTouchMove: function _onTouchMove(
  /**TouchEvent*/
  evt) {
    if (tapEvt) {
      var options = this.options,
          fallbackTolerance = options.fallbackTolerance,
          fallbackOffset = options.fallbackOffset,
          touch = evt.touches ? evt.touches[0] : evt,
          ghostMatrix = ghostEl && matrix(ghostEl, true),
          scaleX = ghostEl && ghostMatrix && ghostMatrix.a,
          scaleY = ghostEl && ghostMatrix && ghostMatrix.d,
          relativeScrollOffset = PositionGhostAbsolutely && ghostRelativeParent && getRelativeScrollOffset(ghostRelativeParent),
          dx = (touch.clientX - tapEvt.clientX + fallbackOffset.x) / (scaleX || 1) + (relativeScrollOffset ? relativeScrollOffset[0] - ghostRelativeParentInitialScroll[0] : 0) / (scaleX || 1),
          dy = (touch.clientY - tapEvt.clientY + fallbackOffset.y) / (scaleY || 1) + (relativeScrollOffset ? relativeScrollOffset[1] - ghostRelativeParentInitialScroll[1] : 0) / (scaleY || 1); // only set the status to dragging, when we are actually dragging

      if (!Sortable.active && !awaitingDragStarted) {
        if (fallbackTolerance && Math.max(Math.abs(touch.clientX - this._lastX), Math.abs(touch.clientY - this._lastY)) < fallbackTolerance) {
          return;
        }

        this._onDragStart(evt, true);
      }

      if (ghostEl) {
        if (ghostMatrix) {
          ghostMatrix.e += dx - (lastDx || 0);
          ghostMatrix.f += dy - (lastDy || 0);
        } else {
          ghostMatrix = {
            a: 1,
            b: 0,
            c: 0,
            d: 1,
            e: dx,
            f: dy
          };
        }

        var cssMatrix = "matrix(".concat(ghostMatrix.a, ",").concat(ghostMatrix.b, ",").concat(ghostMatrix.c, ",").concat(ghostMatrix.d, ",").concat(ghostMatrix.e, ",").concat(ghostMatrix.f, ")");
        css(ghostEl, 'webkitTransform', cssMatrix);
        css(ghostEl, 'mozTransform', cssMatrix);
        css(ghostEl, 'msTransform', cssMatrix);
        css(ghostEl, 'transform', cssMatrix);
        lastDx = dx;
        lastDy = dy;
        touchEvt = touch;
      }

      evt.cancelable && evt.preventDefault();
    }
  },
  _appendGhost: function _appendGhost() {
    // Bug if using scale(): https://stackoverflow.com/questions/2637058
    // Not being adjusted for
    if (!ghostEl) {
      var container = this.options.fallbackOnBody ? document.body : rootEl,
          rect = getRect(dragEl, true, PositionGhostAbsolutely, true, container),
          options = this.options; // Position absolutely

      if (PositionGhostAbsolutely) {
        // Get relatively positioned parent
        ghostRelativeParent = container;

        while (css(ghostRelativeParent, 'position') === 'static' && css(ghostRelativeParent, 'transform') === 'none' && ghostRelativeParent !== document) {
          ghostRelativeParent = ghostRelativeParent.parentNode;
        }

        if (ghostRelativeParent !== document.body && ghostRelativeParent !== document.documentElement) {
          if (ghostRelativeParent === document) ghostRelativeParent = getWindowScrollingElement();
          rect.top += ghostRelativeParent.scrollTop;
          rect.left += ghostRelativeParent.scrollLeft;
        } else {
          ghostRelativeParent = getWindowScrollingElement();
        }

        ghostRelativeParentInitialScroll = getRelativeScrollOffset(ghostRelativeParent);
      }

      ghostEl = dragEl.cloneNode(true);
      toggleClass(ghostEl, options.ghostClass, false);
      toggleClass(ghostEl, options.fallbackClass, true);
      toggleClass(ghostEl, options.dragClass, true);
      css(ghostEl, 'transition', '');
      css(ghostEl, 'transform', '');
      css(ghostEl, 'box-sizing', 'border-box');
      css(ghostEl, 'margin', 0);
      css(ghostEl, 'top', rect.top);
      css(ghostEl, 'left', rect.left);
      css(ghostEl, 'width', rect.width);
      css(ghostEl, 'height', rect.height);
      css(ghostEl, 'opacity', '0.8');
      css(ghostEl, 'position', PositionGhostAbsolutely ? 'absolute' : 'fixed');
      css(ghostEl, 'zIndex', '100000');
      css(ghostEl, 'pointerEvents', 'none');
      Sortable.ghost = ghostEl;
      container.appendChild(ghostEl); // Set transform-origin

      css(ghostEl, 'transform-origin', tapDistanceLeft / parseInt(ghostEl.style.width) * 100 + '% ' + tapDistanceTop / parseInt(ghostEl.style.height) * 100 + '%');
    }
  },
  _onDragStart: function _onDragStart(
  /**Event*/
  evt,
  /**boolean*/
  fallback) {
    var _this = this;

    var dataTransfer = evt.dataTransfer;
    var options = _this.options;
    pluginEvent('dragStart', this, {
      evt: evt
    });

    if (Sortable.eventCanceled) {
      this._onDrop();

      return;
    }

    pluginEvent('setupClone', this);

    if (!Sortable.eventCanceled) {
      cloneEl = clone(dragEl);
      cloneEl.draggable = false;
      cloneEl.style['will-change'] = '';

      this._hideClone();

      toggleClass(cloneEl, this.options.chosenClass, false);
      Sortable.clone = cloneEl;
    } // #1143: IFrame support workaround


    _this.cloneId = _nextTick(function () {
      pluginEvent('clone', _this);
      if (Sortable.eventCanceled) return;

      if (!_this.options.removeCloneOnHide) {
        rootEl.insertBefore(cloneEl, dragEl);
      }

      _this._hideClone();

      _dispatchEvent({
        sortable: _this,
        name: 'clone'
      });
    });
    !fallback && toggleClass(dragEl, options.dragClass, true); // Set proper drop events

    if (fallback) {
      ignoreNextClick = true;
      _this._loopId = setInterval(_this._emulateDragOver, 50);
    } else {
      // Undo what was set in _prepareDragStart before drag started
      off(document, 'mouseup', _this._onDrop);
      off(document, 'touchend', _this._onDrop);
      off(document, 'touchcancel', _this._onDrop);

      if (dataTransfer) {
        dataTransfer.effectAllowed = 'move';
        options.setData && options.setData.call(_this, dataTransfer, dragEl);
      }

      on(document, 'drop', _this); // #1276 fix:

      css(dragEl, 'transform', 'translateZ(0)');
    }

    awaitingDragStarted = true;
    _this._dragStartId = _nextTick(_this._dragStarted.bind(_this, fallback, evt));
    on(document, 'selectstart', _this);
    moved = true;

    if (Safari) {
      css(document.body, 'user-select', 'none');
    }
  },
  // Returns true - if no further action is needed (either inserted or another condition)
  _onDragOver: function _onDragOver(
  /**Event*/
  evt) {
    var el = this.el,
        target = evt.target,
        dragRect,
        targetRect,
        revert,
        options = this.options,
        group = options.group,
        activeSortable = Sortable.active,
        isOwner = activeGroup === group,
        canSort = options.sort,
        fromSortable = putSortable || activeSortable,
        vertical,
        _this = this,
        completedFired = false;

    if (_silent) return;

    function dragOverEvent(name, extra) {
      pluginEvent(name, _this, _objectSpread({
        evt: evt,
        isOwner: isOwner,
        axis: vertical ? 'vertical' : 'horizontal',
        revert: revert,
        dragRect: dragRect,
        targetRect: targetRect,
        canSort: canSort,
        fromSortable: fromSortable,
        target: target,
        completed: completed,
        onMove: function onMove(target, after) {
          return _onMove(rootEl, el, dragEl, dragRect, target, getRect(target), evt, after);
        },
        changed: changed
      }, extra));
    } // Capture animation state


    function capture() {
      dragOverEvent('dragOverAnimationCapture');

      _this.captureAnimationState();

      if (_this !== fromSortable) {
        fromSortable.captureAnimationState();
      }
    } // Return invocation when dragEl is inserted (or completed)


    function completed(insertion) {
      dragOverEvent('dragOverCompleted', {
        insertion: insertion
      });

      if (insertion) {
        // Clones must be hidden before folding animation to capture dragRectAbsolute properly
        if (isOwner) {
          activeSortable._hideClone();
        } else {
          activeSortable._showClone(_this);
        }

        if (_this !== fromSortable) {
          // Set ghost class to new sortable's ghost class
          toggleClass(dragEl, putSortable ? putSortable.options.ghostClass : activeSortable.options.ghostClass, false);
          toggleClass(dragEl, options.ghostClass, true);
        }

        if (putSortable !== _this && _this !== Sortable.active) {
          putSortable = _this;
        } else if (_this === Sortable.active && putSortable) {
          putSortable = null;
        } // Animation


        if (fromSortable === _this) {
          _this._ignoreWhileAnimating = target;
        }

        _this.animateAll(function () {
          dragOverEvent('dragOverAnimationComplete');
          _this._ignoreWhileAnimating = null;
        });

        if (_this !== fromSortable) {
          fromSortable.animateAll();
          fromSortable._ignoreWhileAnimating = null;
        }
      } // Null lastTarget if it is not inside a previously swapped element


      if (target === dragEl && !dragEl.animated || target === el && !target.animated) {
        lastTarget = null;
      } // no bubbling and not fallback


      if (!options.dragoverBubble && !evt.rootEl && target !== document) {
        dragEl.parentNode[expando]._isOutsideThisEl(evt.target); // Do not detect for empty insert if already inserted


        !insertion && nearestEmptyInsertDetectEvent(evt);
      }

      !options.dragoverBubble && evt.stopPropagation && evt.stopPropagation();
      return completedFired = true;
    } // Call when dragEl has been inserted


    function changed() {
      newIndex = index(dragEl);
      newDraggableIndex = index(dragEl, options.draggable);

      _dispatchEvent({
        sortable: _this,
        name: 'change',
        toEl: el,
        newIndex: newIndex,
        newDraggableIndex: newDraggableIndex,
        originalEvent: evt
      });
    }

    if (evt.preventDefault !== void 0) {
      evt.cancelable && evt.preventDefault();
    }

    target = closest(target, options.draggable, el, true);
    dragOverEvent('dragOver');
    if (Sortable.eventCanceled) return completedFired;

    if (dragEl.contains(evt.target) || target.animated && target.animatingX && target.animatingY || _this._ignoreWhileAnimating === target) {
      return completed(false);
    }

    ignoreNextClick = false;

    if (activeSortable && !options.disabled && (isOwner ? canSort || (revert = !rootEl.contains(dragEl)) // Reverting item into the original list
    : putSortable === this || (this.lastPutMode = activeGroup.checkPull(this, activeSortable, dragEl, evt)) && group.checkPut(this, activeSortable, dragEl, evt))) {
      vertical = this._getDirection(evt, target) === 'vertical';
      dragRect = getRect(dragEl);
      dragOverEvent('dragOverValid');
      if (Sortable.eventCanceled) return completedFired;

      if (revert) {
        parentEl = rootEl; // actualization

        capture();

        this._hideClone();

        dragOverEvent('revert');

        if (!Sortable.eventCanceled) {
          if (nextEl) {
            rootEl.insertBefore(dragEl, nextEl);
          } else {
            rootEl.appendChild(dragEl);
          }
        }

        return completed(true);
      }

      var elLastChild = lastChild(el, options.draggable);

      if (!elLastChild || _ghostIsLast(evt, vertical, this) && !elLastChild.animated) {
        // If already at end of list: Do not insert
        if (elLastChild === dragEl) {
          return completed(false);
        } // assign target only if condition is true


        if (elLastChild && el === evt.target) {
          target = elLastChild;
        }

        if (target) {
          targetRect = getRect(target);
        }

        if (_onMove(rootEl, el, dragEl, dragRect, target, targetRect, evt, !!target) !== false) {
          capture();
          el.appendChild(dragEl);
          parentEl = el; // actualization

          changed();
          return completed(true);
        }
      } else if (target.parentNode === el) {
        targetRect = getRect(target);
        var direction = 0,
            targetBeforeFirstSwap,
            differentLevel = dragEl.parentNode !== el,
            differentRowCol = !_dragElInRowColumn(dragEl.animated && dragEl.toRect || dragRect, target.animated && target.toRect || targetRect, vertical),
            side1 = vertical ? 'top' : 'left',
            scrolledPastTop = isScrolledPast(target, 'top', 'top') || isScrolledPast(dragEl, 'top', 'top'),
            scrollBefore = scrolledPastTop ? scrolledPastTop.scrollTop : void 0;

        if (lastTarget !== target) {
          targetBeforeFirstSwap = targetRect[side1];
          pastFirstInvertThresh = false;
          isCircumstantialInvert = !differentRowCol && options.invertSwap || differentLevel;
        }

        direction = _getSwapDirection(evt, target, targetRect, vertical, differentRowCol ? 1 : options.swapThreshold, options.invertedSwapThreshold == null ? options.swapThreshold : options.invertedSwapThreshold, isCircumstantialInvert, lastTarget === target);
        var sibling;

        if (direction !== 0) {
          // Check if target is beside dragEl in respective direction (ignoring hidden elements)
          var dragIndex = index(dragEl);

          do {
            dragIndex -= direction;
            sibling = parentEl.children[dragIndex];
          } while (sibling && (css(sibling, 'display') === 'none' || sibling === ghostEl));
        } // If dragEl is already beside target: Do not insert


        if (direction === 0 || sibling === target) {
          return completed(false);
        }

        lastTarget = target;
        lastDirection = direction;
        var nextSibling = target.nextElementSibling,
            after = false;
        after = direction === 1;

        var moveVector = _onMove(rootEl, el, dragEl, dragRect, target, targetRect, evt, after);

        if (moveVector !== false) {
          if (moveVector === 1 || moveVector === -1) {
            after = moveVector === 1;
          }

          _silent = true;
          setTimeout(_unsilent, 30);
          capture();

          if (after && !nextSibling) {
            el.appendChild(dragEl);
          } else {
            target.parentNode.insertBefore(dragEl, after ? nextSibling : target);
          } // Undo chrome's scroll adjustment (has no effect on other browsers)


          if (scrolledPastTop) {
            scrollBy(scrolledPastTop, 0, scrollBefore - scrolledPastTop.scrollTop);
          }

          parentEl = dragEl.parentNode; // actualization
          // must be done before animation

          if (targetBeforeFirstSwap !== undefined && !isCircumstantialInvert) {
            targetMoveDistance = Math.abs(targetBeforeFirstSwap - getRect(target)[side1]);
          }

          changed();
          return completed(true);
        }
      }

      if (el.contains(dragEl)) {
        return completed(false);
      }
    }

    return false;
  },
  _ignoreWhileAnimating: null,
  _offMoveEvents: function _offMoveEvents() {
    off(document, 'mousemove', this._onTouchMove);
    off(document, 'touchmove', this._onTouchMove);
    off(document, 'pointermove', this._onTouchMove);
    off(document, 'dragover', nearestEmptyInsertDetectEvent);
    off(document, 'mousemove', nearestEmptyInsertDetectEvent);
    off(document, 'touchmove', nearestEmptyInsertDetectEvent);
  },
  _offUpEvents: function _offUpEvents() {
    var ownerDocument = this.el.ownerDocument;
    off(ownerDocument, 'mouseup', this._onDrop);
    off(ownerDocument, 'touchend', this._onDrop);
    off(ownerDocument, 'pointerup', this._onDrop);
    off(ownerDocument, 'touchcancel', this._onDrop);
    off(document, 'selectstart', this);
  },
  _onDrop: function _onDrop(
  /**Event*/
  evt) {
    var el = this.el,
        options = this.options; // Get the index of the dragged element within its parent

    newIndex = index(dragEl);
    newDraggableIndex = index(dragEl, options.draggable);
    pluginEvent('drop', this, {
      evt: evt
    });
    parentEl = dragEl && dragEl.parentNode; // Get again after plugin event

    newIndex = index(dragEl);
    newDraggableIndex = index(dragEl, options.draggable);

    if (Sortable.eventCanceled) {
      this._nulling();

      return;
    }

    awaitingDragStarted = false;
    isCircumstantialInvert = false;
    pastFirstInvertThresh = false;
    clearInterval(this._loopId);
    clearTimeout(this._dragStartTimer);

    _cancelNextTick(this.cloneId);

    _cancelNextTick(this._dragStartId); // Unbind events


    if (this.nativeDraggable) {
      off(document, 'drop', this);
      off(el, 'dragstart', this._onDragStart);
    }

    this._offMoveEvents();

    this._offUpEvents();

    if (Safari) {
      css(document.body, 'user-select', '');
    }

    css(dragEl, 'transform', '');

    if (evt) {
      if (moved) {
        evt.cancelable && evt.preventDefault();
        !options.dropBubble && evt.stopPropagation();
      }

      ghostEl && ghostEl.parentNode && ghostEl.parentNode.removeChild(ghostEl);

      if (rootEl === parentEl || putSortable && putSortable.lastPutMode !== 'clone') {
        // Remove clone(s)
        cloneEl && cloneEl.parentNode && cloneEl.parentNode.removeChild(cloneEl);
      }

      if (dragEl) {
        if (this.nativeDraggable) {
          off(dragEl, 'dragend', this);
        }

        _disableDraggable(dragEl);

        dragEl.style['will-change'] = ''; // Remove classes
        // ghostClass is added in dragStarted

        if (moved && !awaitingDragStarted) {
          toggleClass(dragEl, putSortable ? putSortable.options.ghostClass : this.options.ghostClass, false);
        }

        toggleClass(dragEl, this.options.chosenClass, false); // Drag stop event

        _dispatchEvent({
          sortable: this,
          name: 'unchoose',
          toEl: parentEl,
          newIndex: null,
          newDraggableIndex: null,
          originalEvent: evt
        });

        if (rootEl !== parentEl) {
          if (newIndex >= 0) {
            // Add event
            _dispatchEvent({
              rootEl: parentEl,
              name: 'add',
              toEl: parentEl,
              fromEl: rootEl,
              originalEvent: evt
            }); // Remove event


            _dispatchEvent({
              sortable: this,
              name: 'remove',
              toEl: parentEl,
              originalEvent: evt
            }); // drag from one list and drop into another


            _dispatchEvent({
              rootEl: parentEl,
              name: 'sort',
              toEl: parentEl,
              fromEl: rootEl,
              originalEvent: evt
            });

            _dispatchEvent({
              sortable: this,
              name: 'sort',
              toEl: parentEl,
              originalEvent: evt
            });
          }

          putSortable && putSortable.save();
        } else {
          if (newIndex !== oldIndex) {
            if (newIndex >= 0) {
              // drag & drop within the same list
              _dispatchEvent({
                sortable: this,
                name: 'update',
                toEl: parentEl,
                originalEvent: evt
              });

              _dispatchEvent({
                sortable: this,
                name: 'sort',
                toEl: parentEl,
                originalEvent: evt
              });
            }
          }
        }

        if (Sortable.active) {
          /* jshint eqnull:true */
          if (newIndex == null || newIndex === -1) {
            newIndex = oldIndex;
            newDraggableIndex = oldDraggableIndex;
          }

          _dispatchEvent({
            sortable: this,
            name: 'end',
            toEl: parentEl,
            originalEvent: evt
          }); // Save sorting


          this.save();
        }
      }
    }

    this._nulling();
  },
  _nulling: function _nulling() {
    pluginEvent('nulling', this);
    rootEl = dragEl = parentEl = ghostEl = nextEl = cloneEl = lastDownEl = cloneHidden = tapEvt = touchEvt = moved = newIndex = newDraggableIndex = oldIndex = oldDraggableIndex = lastTarget = lastDirection = putSortable = activeGroup = Sortable.dragged = Sortable.ghost = Sortable.clone = Sortable.active = null;
    savedInputChecked.forEach(function (el) {
      el.checked = true;
    });
    savedInputChecked.length = lastDx = lastDy = 0;
  },
  handleEvent: function handleEvent(
  /**Event*/
  evt) {
    switch (evt.type) {
      case 'drop':
      case 'dragend':
        this._onDrop(evt);

        break;

      case 'dragenter':
      case 'dragover':
        if (dragEl) {
          this._onDragOver(evt);

          _globalDragOver(evt);
        }

        break;

      case 'selectstart':
        evt.preventDefault();
        break;
    }
  },

  /**
   * Serializes the item into an array of string.
   * @returns {String[]}
   */
  toArray: function toArray() {
    var order = [],
        el,
        children = this.el.children,
        i = 0,
        n = children.length,
        options = this.options;

    for (; i < n; i++) {
      el = children[i];

      if (closest(el, options.draggable, this.el, false)) {
        order.push(el.getAttribute(options.dataIdAttr) || _generateId(el));
      }
    }

    return order;
  },

  /**
   * Sorts the elements according to the array.
   * @param  {String[]}  order  order of the items
   */
  sort: function sort(order) {
    var items = {},
        rootEl = this.el;
    this.toArray().forEach(function (id, i) {
      var el = rootEl.children[i];

      if (closest(el, this.options.draggable, rootEl, false)) {
        items[id] = el;
      }
    }, this);
    order.forEach(function (id) {
      if (items[id]) {
        rootEl.removeChild(items[id]);
        rootEl.appendChild(items[id]);
      }
    });
  },

  /**
   * Save the current sorting
   */
  save: function save() {
    var store = this.options.store;
    store && store.set && store.set(this);
  },

  /**
   * For each element in the set, get the first element that matches the selector by testing the element itself and traversing up through its ancestors in the DOM tree.
   * @param   {HTMLElement}  el
   * @param   {String}       [selector]  default: `options.draggable`
   * @returns {HTMLElement|null}
   */
  closest: function closest$1(el, selector) {
    return closest(el, selector || this.options.draggable, this.el, false);
  },

  /**
   * Set/get option
   * @param   {string} name
   * @param   {*}      [value]
   * @returns {*}
   */
  option: function option(name, value) {
    var options = this.options;

    if (value === void 0) {
      return options[name];
    } else {
      var modifiedValue = PluginManager.modifyOption(this, name, value);

      if (typeof modifiedValue !== 'undefined') {
        options[name] = modifiedValue;
      } else {
        options[name] = value;
      }

      if (name === 'group') {
        _prepareGroup(options);
      }
    }
  },

  /**
   * Destroy
   */
  destroy: function destroy() {
    pluginEvent('destroy', this);
    var el = this.el;
    el[expando] = null;
    off(el, 'mousedown', this._onTapStart);
    off(el, 'touchstart', this._onTapStart);
    off(el, 'pointerdown', this._onTapStart);

    if (this.nativeDraggable) {
      off(el, 'dragover', this);
      off(el, 'dragenter', this);
    } // Remove draggable attributes


    Array.prototype.forEach.call(el.querySelectorAll('[draggable]'), function (el) {
      el.removeAttribute('draggable');
    });

    this._onDrop();

    this._disableDelayedDragEvents();

    sortables.splice(sortables.indexOf(this.el), 1);
    this.el = el = null;
  },
  _hideClone: function _hideClone() {
    if (!cloneHidden) {
      pluginEvent('hideClone', this);
      if (Sortable.eventCanceled) return;
      css(cloneEl, 'display', 'none');

      if (this.options.removeCloneOnHide && cloneEl.parentNode) {
        cloneEl.parentNode.removeChild(cloneEl);
      }

      cloneHidden = true;
    }
  },
  _showClone: function _showClone(putSortable) {
    if (putSortable.lastPutMode !== 'clone') {
      this._hideClone();

      return;
    }

    if (cloneHidden) {
      pluginEvent('showClone', this);
      if (Sortable.eventCanceled) return; // show clone at dragEl or original position

      if (rootEl.contains(dragEl) && !this.options.group.revertClone) {
        rootEl.insertBefore(cloneEl, dragEl);
      } else if (nextEl) {
        rootEl.insertBefore(cloneEl, nextEl);
      } else {
        rootEl.appendChild(cloneEl);
      }

      if (this.options.group.revertClone) {
        this.animate(dragEl, cloneEl);
      }

      css(cloneEl, 'display', '');
      cloneHidden = false;
    }
  }
};

function _globalDragOver(
/**Event*/
evt) {
  if (evt.dataTransfer) {
    evt.dataTransfer.dropEffect = 'move';
  }

  evt.cancelable && evt.preventDefault();
}

function _onMove(fromEl, toEl, dragEl, dragRect, targetEl, targetRect, originalEvent, willInsertAfter) {
  var evt,
      sortable = fromEl[expando],
      onMoveFn = sortable.options.onMove,
      retVal; // Support for new CustomEvent feature

  if (window.CustomEvent && !IE11OrLess && !Edge) {
    evt = new CustomEvent('move', {
      bubbles: true,
      cancelable: true
    });
  } else {
    evt = document.createEvent('Event');
    evt.initEvent('move', true, true);
  }

  evt.to = toEl;
  evt.from = fromEl;
  evt.dragged = dragEl;
  evt.draggedRect = dragRect;
  evt.related = targetEl || toEl;
  evt.relatedRect = targetRect || getRect(toEl);
  evt.willInsertAfter = willInsertAfter;
  evt.originalEvent = originalEvent;
  fromEl.dispatchEvent(evt);

  if (onMoveFn) {
    retVal = onMoveFn.call(sortable, evt, originalEvent);
  }

  return retVal;
}

function _disableDraggable(el) {
  el.draggable = false;
}

function _unsilent() {
  _silent = false;
}

function _ghostIsLast(evt, vertical, sortable) {
  var rect = getRect(lastChild(sortable.el, sortable.options.draggable));
  var spacer = 10;
  return vertical ? evt.clientX > rect.right + spacer || evt.clientX <= rect.right && evt.clientY > rect.bottom && evt.clientX >= rect.left : evt.clientX > rect.right && evt.clientY > rect.top || evt.clientX <= rect.right && evt.clientY > rect.bottom + spacer;
}

function _getSwapDirection(evt, target, targetRect, vertical, swapThreshold, invertedSwapThreshold, invertSwap, isLastTarget) {
  var mouseOnAxis = vertical ? evt.clientY : evt.clientX,
      targetLength = vertical ? targetRect.height : targetRect.width,
      targetS1 = vertical ? targetRect.top : targetRect.left,
      targetS2 = vertical ? targetRect.bottom : targetRect.right,
      invert = false;

  if (!invertSwap) {
    // Never invert or create dragEl shadow when target movemenet causes mouse to move past the end of regular swapThreshold
    if (isLastTarget && targetMoveDistance < targetLength * swapThreshold) {
      // multiplied only by swapThreshold because mouse will already be inside target by (1 - threshold) * targetLength / 2
      // check if past first invert threshold on side opposite of lastDirection
      if (!pastFirstInvertThresh && (lastDirection === 1 ? mouseOnAxis > targetS1 + targetLength * invertedSwapThreshold / 2 : mouseOnAxis < targetS2 - targetLength * invertedSwapThreshold / 2)) {
        // past first invert threshold, do not restrict inverted threshold to dragEl shadow
        pastFirstInvertThresh = true;
      }

      if (!pastFirstInvertThresh) {
        // dragEl shadow (target move distance shadow)
        if (lastDirection === 1 ? mouseOnAxis < targetS1 + targetMoveDistance // over dragEl shadow
        : mouseOnAxis > targetS2 - targetMoveDistance) {
          return -lastDirection;
        }
      } else {
        invert = true;
      }
    } else {
      // Regular
      if (mouseOnAxis > targetS1 + targetLength * (1 - swapThreshold) / 2 && mouseOnAxis < targetS2 - targetLength * (1 - swapThreshold) / 2) {
        return _getInsertDirection(target);
      }
    }
  }

  invert = invert || invertSwap;

  if (invert) {
    // Invert of regular
    if (mouseOnAxis < targetS1 + targetLength * invertedSwapThreshold / 2 || mouseOnAxis > targetS2 - targetLength * invertedSwapThreshold / 2) {
      return mouseOnAxis > targetS1 + targetLength / 2 ? 1 : -1;
    }
  }

  return 0;
}
/**
 * Gets the direction dragEl must be swapped relative to target in order to make it
 * seem that dragEl has been "inserted" into that element's position
 * @param  {HTMLElement} target       The target whose position dragEl is being inserted at
 * @return {Number}                   Direction dragEl must be swapped
 */


function _getInsertDirection(target) {
  if (index(dragEl) < index(target)) {
    return 1;
  } else {
    return -1;
  }
}
/**
 * Generate id
 * @param   {HTMLElement} el
 * @returns {String}
 * @private
 */


function _generateId(el) {
  var str = el.tagName + el.className + el.src + el.href + el.textContent,
      i = str.length,
      sum = 0;

  while (i--) {
    sum += str.charCodeAt(i);
  }

  return sum.toString(36);
}

function _saveInputCheckedState(root) {
  savedInputChecked.length = 0;
  var inputs = root.getElementsByTagName('input');
  var idx = inputs.length;

  while (idx--) {
    var el = inputs[idx];
    el.checked && savedInputChecked.push(el);
  }
}

function _nextTick(fn) {
  return setTimeout(fn, 0);
}

function _cancelNextTick(id) {
  return clearTimeout(id);
} // Fixed #973:


if (documentExists) {
  on(document, 'touchmove', function (evt) {
    if ((Sortable.active || awaitingDragStarted) && evt.cancelable) {
      evt.preventDefault();
    }
  });
} // Export utils


Sortable.utils = {
  on: on,
  off: off,
  css: css,
  find: find,
  is: function is(el, selector) {
    return !!closest(el, selector, el, false);
  },
  extend: extend,
  throttle: throttle,
  closest: closest,
  toggleClass: toggleClass,
  clone: clone,
  index: index,
  nextTick: _nextTick,
  cancelNextTick: _cancelNextTick,
  detectDirection: _detectDirection,
  getChild: getChild
};
/**
 * Get the Sortable instance of an element
 * @param  {HTMLElement} element The element
 * @return {Sortable|undefined}         The instance of Sortable
 */

Sortable.get = function (element) {
  return element[expando];
};
/**
 * Mount a plugin to Sortable
 * @param  {...SortablePlugin|SortablePlugin[]} plugins       Plugins being mounted
 */


Sortable.mount = function () {
  for (var _len = arguments.length, plugins = new Array(_len), _key = 0; _key < _len; _key++) {
    plugins[_key] = arguments[_key];
  }

  if (plugins[0].constructor === Array) plugins = plugins[0];
  plugins.forEach(function (plugin) {
    if (!plugin.prototype || !plugin.prototype.constructor) {
      throw "Sortable: Mounted plugin must be a constructor function, not ".concat({}.toString.call(plugin));
    }

    if (plugin.utils) Sortable.utils = _objectSpread({}, Sortable.utils, plugin.utils);
    PluginManager.mount(plugin);
  });
};
/**
 * Create sortable instance
 * @param {HTMLElement}  el
 * @param {Object}      [options]
 */


Sortable.create = function (el, options) {
  return new Sortable(el, options);
}; // Export


Sortable.version = version;

var autoScrolls = [],
    scrollEl,
    scrollRootEl,
    scrolling = false,
    lastAutoScrollX,
    lastAutoScrollY,
    touchEvt$1,
    pointerElemChangedInterval;

function AutoScrollPlugin() {
  function AutoScroll() {
    this.defaults = {
      scroll: true,
      scrollSensitivity: 30,
      scrollSpeed: 10,
      bubbleScroll: true
    }; // Bind all private methods

    for (var fn in this) {
      if (fn.charAt(0) === '_' && typeof this[fn] === 'function') {
        this[fn] = this[fn].bind(this);
      }
    }
  }

  AutoScroll.prototype = {
    dragStarted: function dragStarted(_ref) {
      var originalEvent = _ref.originalEvent;

      if (this.sortable.nativeDraggable) {
        on(document, 'dragover', this._handleAutoScroll);
      } else {
        if (this.options.supportPointer) {
          on(document, 'pointermove', this._handleFallbackAutoScroll);
        } else if (originalEvent.touches) {
          on(document, 'touchmove', this._handleFallbackAutoScroll);
        } else {
          on(document, 'mousemove', this._handleFallbackAutoScroll);
        }
      }
    },
    dragOverCompleted: function dragOverCompleted(_ref2) {
      var originalEvent = _ref2.originalEvent;

      // For when bubbling is canceled and using fallback (fallback 'touchmove' always reached)
      if (!this.options.dragOverBubble && !originalEvent.rootEl) {
        this._handleAutoScroll(originalEvent);
      }
    },
    drop: function drop() {
      if (this.sortable.nativeDraggable) {
        off(document, 'dragover', this._handleAutoScroll);
      } else {
        off(document, 'pointermove', this._handleFallbackAutoScroll);
        off(document, 'touchmove', this._handleFallbackAutoScroll);
        off(document, 'mousemove', this._handleFallbackAutoScroll);
      }

      clearPointerElemChangedInterval();
      clearAutoScrolls();
      cancelThrottle();
    },
    nulling: function nulling() {
      touchEvt$1 = scrollRootEl = scrollEl = scrolling = pointerElemChangedInterval = lastAutoScrollX = lastAutoScrollY = null;
      autoScrolls.length = 0;
    },
    _handleFallbackAutoScroll: function _handleFallbackAutoScroll(evt) {
      this._handleAutoScroll(evt, true);
    },
    _handleAutoScroll: function _handleAutoScroll(evt, fallback) {
      var _this = this;

      var x = (evt.touches ? evt.touches[0] : evt).clientX,
          y = (evt.touches ? evt.touches[0] : evt).clientY,
          elem = document.elementFromPoint(x, y);
      touchEvt$1 = evt; // IE does not seem to have native autoscroll,
      // Edge's autoscroll seems too conditional,
      // MACOS Safari does not have autoscroll,
      // Firefox and Chrome are good

      if (fallback || Edge || IE11OrLess || Safari) {
        autoScroll(evt, this.options, elem, fallback); // Listener for pointer element change

        var ogElemScroller = getParentAutoScrollElement(elem, true);

        if (scrolling && (!pointerElemChangedInterval || x !== lastAutoScrollX || y !== lastAutoScrollY)) {
          pointerElemChangedInterval && clearPointerElemChangedInterval(); // Detect for pointer elem change, emulating native DnD behaviour

          pointerElemChangedInterval = setInterval(function () {
            var newElem = getParentAutoScrollElement(document.elementFromPoint(x, y), true);

            if (newElem !== ogElemScroller) {
              ogElemScroller = newElem;
              clearAutoScrolls();
            }

            autoScroll(evt, _this.options, newElem, fallback);
          }, 10);
          lastAutoScrollX = x;
          lastAutoScrollY = y;
        }
      } else {
        // if DnD is enabled (and browser has good autoscrolling), first autoscroll will already scroll, so get parent autoscroll of first autoscroll
        if (!this.options.bubbleScroll || getParentAutoScrollElement(elem, true) === getWindowScrollingElement()) {
          clearAutoScrolls();
          return;
        }

        autoScroll(evt, this.options, getParentAutoScrollElement(elem, false), false);
      }
    }
  };
  return _extends(AutoScroll, {
    pluginName: 'scroll',
    initializeByDefault: true
  });
}

function clearAutoScrolls() {
  autoScrolls.forEach(function (autoScroll) {
    clearInterval(autoScroll.pid);
  });
  autoScrolls = [];
}

function clearPointerElemChangedInterval() {
  clearInterval(pointerElemChangedInterval);
}

var autoScroll = throttle(function (evt, options, rootEl, isFallback) {
  // Bug: https://bugzilla.mozilla.org/show_bug.cgi?id=505521
  if (!options.scroll) return;
  var x = (evt.touches ? evt.touches[0] : evt).clientX,
      y = (evt.touches ? evt.touches[0] : evt).clientY,
      sens = options.scrollSensitivity,
      speed = options.scrollSpeed,
      winScroller = getWindowScrollingElement();
  var scrollThisInstance = false,
      scrollCustomFn; // New scroll root, set scrollEl

  if (scrollRootEl !== rootEl) {
    scrollRootEl = rootEl;
    clearAutoScrolls();
    scrollEl = options.scroll;
    scrollCustomFn = options.scrollFn;

    if (scrollEl === true) {
      scrollEl = getParentAutoScrollElement(rootEl, true);
    }
  }

  var layersOut = 0;
  var currentParent = scrollEl;

  do {
    var el = currentParent,
        rect = getRect(el),
        top = rect.top,
        bottom = rect.bottom,
        left = rect.left,
        right = rect.right,
        width = rect.width,
        height = rect.height,
        canScrollX = void 0,
        canScrollY = void 0,
        scrollWidth = el.scrollWidth,
        scrollHeight = el.scrollHeight,
        elCSS = css(el),
        scrollPosX = el.scrollLeft,
        scrollPosY = el.scrollTop;

    if (el === winScroller) {
      canScrollX = width < scrollWidth && (elCSS.overflowX === 'auto' || elCSS.overflowX === 'scroll' || elCSS.overflowX === 'visible');
      canScrollY = height < scrollHeight && (elCSS.overflowY === 'auto' || elCSS.overflowY === 'scroll' || elCSS.overflowY === 'visible');
    } else {
      canScrollX = width < scrollWidth && (elCSS.overflowX === 'auto' || elCSS.overflowX === 'scroll');
      canScrollY = height < scrollHeight && (elCSS.overflowY === 'auto' || elCSS.overflowY === 'scroll');
    }

    var vx = canScrollX && (Math.abs(right - x) <= sens && scrollPosX + width < scrollWidth) - (Math.abs(left - x) <= sens && !!scrollPosX);
    var vy = canScrollY && (Math.abs(bottom - y) <= sens && scrollPosY + height < scrollHeight) - (Math.abs(top - y) <= sens && !!scrollPosY);

    if (!autoScrolls[layersOut]) {
      for (var i = 0; i <= layersOut; i++) {
        if (!autoScrolls[i]) {
          autoScrolls[i] = {};
        }
      }
    }

    if (autoScrolls[layersOut].vx != vx || autoScrolls[layersOut].vy != vy || autoScrolls[layersOut].el !== el) {
      autoScrolls[layersOut].el = el;
      autoScrolls[layersOut].vx = vx;
      autoScrolls[layersOut].vy = vy;
      clearInterval(autoScrolls[layersOut].pid);

      if (vx != 0 || vy != 0) {
        scrollThisInstance = true;
        /* jshint loopfunc:true */

        autoScrolls[layersOut].pid = setInterval(function () {
          // emulate drag over during autoscroll (fallback), emulating native DnD behaviour
          if (isFallback && this.layer === 0) {
            Sortable.active._onTouchMove(touchEvt$1); // To move ghost if it is positioned absolutely

          }

          var scrollOffsetY = autoScrolls[this.layer].vy ? autoScrolls[this.layer].vy * speed : 0;
          var scrollOffsetX = autoScrolls[this.layer].vx ? autoScrolls[this.layer].vx * speed : 0;

          if (typeof scrollCustomFn === 'function') {
            if (scrollCustomFn.call(Sortable.dragged.parentNode[expando], scrollOffsetX, scrollOffsetY, evt, touchEvt$1, autoScrolls[this.layer].el) !== 'continue') {
              return;
            }
          }

          scrollBy(autoScrolls[this.layer].el, scrollOffsetX, scrollOffsetY);
        }.bind({
          layer: layersOut
        }), 24);
      }
    }

    layersOut++;
  } while (options.bubbleScroll && currentParent !== winScroller && (currentParent = getParentAutoScrollElement(currentParent, false)));

  scrolling = scrollThisInstance; // in case another function catches scrolling as false in between when it is not
}, 30);

var drop = function drop(_ref) {
  var originalEvent = _ref.originalEvent,
      putSortable = _ref.putSortable,
      dragEl = _ref.dragEl,
      activeSortable = _ref.activeSortable,
      dispatchSortableEvent = _ref.dispatchSortableEvent,
      hideGhostForTarget = _ref.hideGhostForTarget,
      unhideGhostForTarget = _ref.unhideGhostForTarget;
  if (!originalEvent) return;
  var toSortable = putSortable || activeSortable;
  hideGhostForTarget();
  var touch = originalEvent.changedTouches && originalEvent.changedTouches.length ? originalEvent.changedTouches[0] : originalEvent;
  var target = document.elementFromPoint(touch.clientX, touch.clientY);
  unhideGhostForTarget();

  if (toSortable && !toSortable.el.contains(target)) {
    dispatchSortableEvent('spill');
    this.onSpill({
      dragEl: dragEl,
      putSortable: putSortable
    });
  }
};

function Revert() {}

Revert.prototype = {
  startIndex: null,
  dragStart: function dragStart(_ref2) {
    var oldDraggableIndex = _ref2.oldDraggableIndex;
    this.startIndex = oldDraggableIndex;
  },
  onSpill: function onSpill(_ref3) {
    var dragEl = _ref3.dragEl,
        putSortable = _ref3.putSortable;
    this.sortable.captureAnimationState();

    if (putSortable) {
      putSortable.captureAnimationState();
    }

    var nextSibling = getChild(this.sortable.el, this.startIndex, this.options);

    if (nextSibling) {
      this.sortable.el.insertBefore(dragEl, nextSibling);
    } else {
      this.sortable.el.appendChild(dragEl);
    }

    this.sortable.animateAll();

    if (putSortable) {
      putSortable.animateAll();
    }
  },
  drop: drop
};

_extends(Revert, {
  pluginName: 'revertOnSpill'
});

function Remove() {}

Remove.prototype = {
  onSpill: function onSpill(_ref4) {
    var dragEl = _ref4.dragEl,
        putSortable = _ref4.putSortable;
    var parentSortable = putSortable || this.sortable;
    parentSortable.captureAnimationState();
    dragEl.parentNode && dragEl.parentNode.removeChild(dragEl);
    parentSortable.animateAll();
  },
  drop: drop
};

_extends(Remove, {
  pluginName: 'removeOnSpill'
});

var lastSwapEl;

function SwapPlugin() {
  function Swap() {
    this.defaults = {
      swapClass: 'sortable-swap-highlight'
    };
  }

  Swap.prototype = {
    dragStart: function dragStart(_ref) {
      var dragEl = _ref.dragEl;
      lastSwapEl = dragEl;
    },
    dragOverValid: function dragOverValid(_ref2) {
      var completed = _ref2.completed,
          target = _ref2.target,
          onMove = _ref2.onMove,
          activeSortable = _ref2.activeSortable,
          changed = _ref2.changed,
          cancel = _ref2.cancel;
      if (!activeSortable.options.swap) return;
      var el = this.sortable.el,
          options = this.options;

      if (target && target !== el) {
        var prevSwapEl = lastSwapEl;

        if (onMove(target) !== false) {
          toggleClass(target, options.swapClass, true);
          lastSwapEl = target;
        } else {
          lastSwapEl = null;
        }

        if (prevSwapEl && prevSwapEl !== lastSwapEl) {
          toggleClass(prevSwapEl, options.swapClass, false);
        }
      }

      changed();
      completed(true);
      cancel();
    },
    drop: function drop(_ref3) {
      var activeSortable = _ref3.activeSortable,
          putSortable = _ref3.putSortable,
          dragEl = _ref3.dragEl;
      var toSortable = putSortable || this.sortable;
      var options = this.options;
      lastSwapEl && toggleClass(lastSwapEl, options.swapClass, false);

      if (lastSwapEl && (options.swap || putSortable && putSortable.options.swap)) {
        if (dragEl !== lastSwapEl) {
          toSortable.captureAnimationState();
          if (toSortable !== activeSortable) activeSortable.captureAnimationState();
          swapNodes(dragEl, lastSwapEl);
          toSortable.animateAll();
          if (toSortable !== activeSortable) activeSortable.animateAll();
        }
      }
    },
    nulling: function nulling() {
      lastSwapEl = null;
    }
  };
  return _extends(Swap, {
    pluginName: 'swap',
    eventProperties: function eventProperties() {
      return {
        swapItem: lastSwapEl
      };
    }
  });
}

function swapNodes(n1, n2) {
  var p1 = n1.parentNode,
      p2 = n2.parentNode,
      i1,
      i2;
  if (!p1 || !p2 || p1.isEqualNode(n2) || p2.isEqualNode(n1)) return;
  i1 = index(n1);
  i2 = index(n2);

  if (p1.isEqualNode(p2) && i1 < i2) {
    i2++;
  }

  p1.insertBefore(n2, p1.children[i1]);
  p2.insertBefore(n1, p2.children[i2]);
}

var multiDragElements = [],
    multiDragClones = [],
    lastMultiDragSelect,
    // for selection with modifier key down (SHIFT)
multiDragSortable,
    initialFolding = false,
    // Initial multi-drag fold when drag started
folding = false,
    // Folding any other time
dragStarted = false,
    dragEl$1,
    clonesFromRect,
    clonesHidden;

function MultiDragPlugin() {
  function MultiDrag(sortable) {
    // Bind all private methods
    for (var fn in this) {
      if (fn.charAt(0) === '_' && typeof this[fn] === 'function') {
        this[fn] = this[fn].bind(this);
      }
    }

    if (sortable.options.supportPointer) {
      on(document, 'pointerup', this._deselectMultiDrag);
    } else {
      on(document, 'mouseup', this._deselectMultiDrag);
      on(document, 'touchend', this._deselectMultiDrag);
    }

    on(document, 'keydown', this._checkKeyDown);
    on(document, 'keyup', this._checkKeyUp);
    this.defaults = {
      selectedClass: 'sortable-selected',
      multiDragKey: null,
      setData: function setData(dataTransfer, dragEl) {
        var data = '';

        if (multiDragElements.length && multiDragSortable === sortable) {
          multiDragElements.forEach(function (multiDragElement, i) {
            data += (!i ? '' : ', ') + multiDragElement.textContent;
          });
        } else {
          data = dragEl.textContent;
        }

        dataTransfer.setData('Text', data);
      }
    };
  }

  MultiDrag.prototype = {
    multiDragKeyDown: false,
    isMultiDrag: false,
    delayStartGlobal: function delayStartGlobal(_ref) {
      var dragged = _ref.dragEl;
      dragEl$1 = dragged;
    },
    delayEnded: function delayEnded() {
      this.isMultiDrag = ~multiDragElements.indexOf(dragEl$1);
    },
    setupClone: function setupClone(_ref2) {
      var sortable = _ref2.sortable,
          cancel = _ref2.cancel;
      if (!this.isMultiDrag) return;

      for (var i = 0; i < multiDragElements.length; i++) {
        multiDragClones.push(clone(multiDragElements[i]));
        multiDragClones[i].sortableIndex = multiDragElements[i].sortableIndex;
        multiDragClones[i].draggable = false;
        multiDragClones[i].style['will-change'] = '';
        toggleClass(multiDragClones[i], this.options.selectedClass, false);
        multiDragElements[i] === dragEl$1 && toggleClass(multiDragClones[i], this.options.chosenClass, false);
      }

      sortable._hideClone();

      cancel();
    },
    clone: function clone(_ref3) {
      var sortable = _ref3.sortable,
          rootEl = _ref3.rootEl,
          dispatchSortableEvent = _ref3.dispatchSortableEvent,
          cancel = _ref3.cancel;
      if (!this.isMultiDrag) return;

      if (!this.options.removeCloneOnHide) {
        if (multiDragElements.length && multiDragSortable === sortable) {
          insertMultiDragClones(true, rootEl);
          dispatchSortableEvent('clone');
          cancel();
        }
      }
    },
    showClone: function showClone(_ref4) {
      var cloneNowShown = _ref4.cloneNowShown,
          rootEl = _ref4.rootEl,
          cancel = _ref4.cancel;
      if (!this.isMultiDrag) return;
      insertMultiDragClones(false, rootEl);
      multiDragClones.forEach(function (clone) {
        css(clone, 'display', '');
      });
      cloneNowShown();
      clonesHidden = false;
      cancel();
    },
    hideClone: function hideClone(_ref5) {
      var _this = this;

      _ref5.sortable;
          var cloneNowHidden = _ref5.cloneNowHidden,
          cancel = _ref5.cancel;
      if (!this.isMultiDrag) return;
      multiDragClones.forEach(function (clone) {
        css(clone, 'display', 'none');

        if (_this.options.removeCloneOnHide && clone.parentNode) {
          clone.parentNode.removeChild(clone);
        }
      });
      cloneNowHidden();
      clonesHidden = true;
      cancel();
    },
    dragStartGlobal: function dragStartGlobal(_ref6) {
      _ref6.sortable;

      if (!this.isMultiDrag && multiDragSortable) {
        multiDragSortable.multiDrag._deselectMultiDrag();
      }

      multiDragElements.forEach(function (multiDragElement) {
        multiDragElement.sortableIndex = index(multiDragElement);
      }); // Sort multi-drag elements

      multiDragElements = multiDragElements.sort(function (a, b) {
        return a.sortableIndex - b.sortableIndex;
      });
      dragStarted = true;
    },
    dragStarted: function dragStarted(_ref7) {
      var _this2 = this;

      var sortable = _ref7.sortable;
      if (!this.isMultiDrag) return;

      if (this.options.sort) {
        // Capture rects,
        // hide multi drag elements (by positioning them absolute),
        // set multi drag elements rects to dragRect,
        // show multi drag elements,
        // animate to rects,
        // unset rects & remove from DOM
        sortable.captureAnimationState();

        if (this.options.animation) {
          multiDragElements.forEach(function (multiDragElement) {
            if (multiDragElement === dragEl$1) return;
            css(multiDragElement, 'position', 'absolute');
          });
          var dragRect = getRect(dragEl$1, false, true, true);
          multiDragElements.forEach(function (multiDragElement) {
            if (multiDragElement === dragEl$1) return;
            setRect(multiDragElement, dragRect);
          });
          folding = true;
          initialFolding = true;
        }
      }

      sortable.animateAll(function () {
        folding = false;
        initialFolding = false;

        if (_this2.options.animation) {
          multiDragElements.forEach(function (multiDragElement) {
            unsetRect(multiDragElement);
          });
        } // Remove all auxiliary multidrag items from el, if sorting enabled


        if (_this2.options.sort) {
          removeMultiDragElements();
        }
      });
    },
    dragOver: function dragOver(_ref8) {
      var target = _ref8.target,
          completed = _ref8.completed,
          cancel = _ref8.cancel;

      if (folding && ~multiDragElements.indexOf(target)) {
        completed(false);
        cancel();
      }
    },
    revert: function revert(_ref9) {
      var fromSortable = _ref9.fromSortable,
          rootEl = _ref9.rootEl,
          sortable = _ref9.sortable,
          dragRect = _ref9.dragRect;

      if (multiDragElements.length > 1) {
        // Setup unfold animation
        multiDragElements.forEach(function (multiDragElement) {
          sortable.addAnimationState({
            target: multiDragElement,
            rect: folding ? getRect(multiDragElement) : dragRect
          });
          unsetRect(multiDragElement);
          multiDragElement.fromRect = dragRect;
          fromSortable.removeAnimationState(multiDragElement);
        });
        folding = false;
        insertMultiDragElements(!this.options.removeCloneOnHide, rootEl);
      }
    },
    dragOverCompleted: function dragOverCompleted(_ref10) {
      var sortable = _ref10.sortable,
          isOwner = _ref10.isOwner,
          insertion = _ref10.insertion,
          activeSortable = _ref10.activeSortable,
          parentEl = _ref10.parentEl,
          putSortable = _ref10.putSortable;
      var options = this.options;

      if (insertion) {
        // Clones must be hidden before folding animation to capture dragRectAbsolute properly
        if (isOwner) {
          activeSortable._hideClone();
        }

        initialFolding = false; // If leaving sort:false root, or already folding - Fold to new location

        if (options.animation && multiDragElements.length > 1 && (folding || !isOwner && !activeSortable.options.sort && !putSortable)) {
          // Fold: Set all multi drag elements's rects to dragEl's rect when multi-drag elements are invisible
          var dragRectAbsolute = getRect(dragEl$1, false, true, true);
          multiDragElements.forEach(function (multiDragElement) {
            if (multiDragElement === dragEl$1) return;
            setRect(multiDragElement, dragRectAbsolute); // Move element(s) to end of parentEl so that it does not interfere with multi-drag clones insertion if they are inserted
            // while folding, and so that we can capture them again because old sortable will no longer be fromSortable

            parentEl.appendChild(multiDragElement);
          });
          folding = true;
        } // Clones must be shown (and check to remove multi drags) after folding when interfering multiDragElements are moved out


        if (!isOwner) {
          // Only remove if not folding (folding will remove them anyways)
          if (!folding) {
            removeMultiDragElements();
          }

          if (multiDragElements.length > 1) {
            var clonesHiddenBefore = clonesHidden;

            activeSortable._showClone(sortable); // Unfold animation for clones if showing from hidden


            if (activeSortable.options.animation && !clonesHidden && clonesHiddenBefore) {
              multiDragClones.forEach(function (clone) {
                activeSortable.addAnimationState({
                  target: clone,
                  rect: clonesFromRect
                });
                clone.fromRect = clonesFromRect;
                clone.thisAnimationDuration = null;
              });
            }
          } else {
            activeSortable._showClone(sortable);
          }
        }
      }
    },
    dragOverAnimationCapture: function dragOverAnimationCapture(_ref11) {
      var dragRect = _ref11.dragRect,
          isOwner = _ref11.isOwner,
          activeSortable = _ref11.activeSortable;
      multiDragElements.forEach(function (multiDragElement) {
        multiDragElement.thisAnimationDuration = null;
      });

      if (activeSortable.options.animation && !isOwner && activeSortable.multiDrag.isMultiDrag) {
        clonesFromRect = _extends({}, dragRect);
        var dragMatrix = matrix(dragEl$1, true);
        clonesFromRect.top -= dragMatrix.f;
        clonesFromRect.left -= dragMatrix.e;
      }
    },
    dragOverAnimationComplete: function dragOverAnimationComplete() {
      if (folding) {
        folding = false;
        removeMultiDragElements();
      }
    },
    drop: function drop(_ref12) {
      var evt = _ref12.originalEvent,
          rootEl = _ref12.rootEl,
          parentEl = _ref12.parentEl,
          sortable = _ref12.sortable,
          dispatchSortableEvent = _ref12.dispatchSortableEvent,
          oldIndex = _ref12.oldIndex,
          putSortable = _ref12.putSortable;
      var toSortable = putSortable || this.sortable;
      if (!evt) return;
      var options = this.options,
          children = parentEl.children; // Multi-drag selection

      if (!dragStarted) {
        if (options.multiDragKey && !this.multiDragKeyDown) {
          this._deselectMultiDrag();
        }

        toggleClass(dragEl$1, options.selectedClass, !~multiDragElements.indexOf(dragEl$1));

        if (!~multiDragElements.indexOf(dragEl$1)) {
          multiDragElements.push(dragEl$1);
          dispatchEvent({
            sortable: sortable,
            rootEl: rootEl,
            name: 'select',
            targetEl: dragEl$1}); // Modifier activated, select from last to dragEl

          if (evt.shiftKey && lastMultiDragSelect && sortable.el.contains(lastMultiDragSelect)) {
            var lastIndex = index(lastMultiDragSelect),
                currentIndex = index(dragEl$1);

            if (~lastIndex && ~currentIndex && lastIndex !== currentIndex) {
              // Must include lastMultiDragSelect (select it), in case modified selection from no selection
              // (but previous selection existed)
              var n, i;

              if (currentIndex > lastIndex) {
                i = lastIndex;
                n = currentIndex;
              } else {
                i = currentIndex;
                n = lastIndex + 1;
              }

              for (; i < n; i++) {
                if (~multiDragElements.indexOf(children[i])) continue;
                toggleClass(children[i], options.selectedClass, true);
                multiDragElements.push(children[i]);
                dispatchEvent({
                  sortable: sortable,
                  rootEl: rootEl,
                  name: 'select',
                  targetEl: children[i]});
              }
            }
          } else {
            lastMultiDragSelect = dragEl$1;
          }

          multiDragSortable = toSortable;
        } else {
          multiDragElements.splice(multiDragElements.indexOf(dragEl$1), 1);
          lastMultiDragSelect = null;
          dispatchEvent({
            sortable: sortable,
            rootEl: rootEl,
            name: 'deselect',
            targetEl: dragEl$1});
        }
      } // Multi-drag drop


      if (dragStarted && this.isMultiDrag) {
        // Do not "unfold" after around dragEl if reverted
        if ((parentEl[expando].options.sort || parentEl !== rootEl) && multiDragElements.length > 1) {
          var dragRect = getRect(dragEl$1),
              multiDragIndex = index(dragEl$1, ':not(.' + this.options.selectedClass + ')');
          if (!initialFolding && options.animation) dragEl$1.thisAnimationDuration = null;
          toSortable.captureAnimationState();

          if (!initialFolding) {
            if (options.animation) {
              dragEl$1.fromRect = dragRect;
              multiDragElements.forEach(function (multiDragElement) {
                multiDragElement.thisAnimationDuration = null;

                if (multiDragElement !== dragEl$1) {
                  var rect = folding ? getRect(multiDragElement) : dragRect;
                  multiDragElement.fromRect = rect; // Prepare unfold animation

                  toSortable.addAnimationState({
                    target: multiDragElement,
                    rect: rect
                  });
                }
              });
            } // Multi drag elements are not necessarily removed from the DOM on drop, so to reinsert
            // properly they must all be removed


            removeMultiDragElements();
            multiDragElements.forEach(function (multiDragElement) {
              if (children[multiDragIndex]) {
                parentEl.insertBefore(multiDragElement, children[multiDragIndex]);
              } else {
                parentEl.appendChild(multiDragElement);
              }

              multiDragIndex++;
            }); // If initial folding is done, the elements may have changed position because they are now
            // unfolding around dragEl, even though dragEl may not have his index changed, so update event
            // must be fired here as Sortable will not.

            if (oldIndex === index(dragEl$1)) {
              var update = false;
              multiDragElements.forEach(function (multiDragElement) {
                if (multiDragElement.sortableIndex !== index(multiDragElement)) {
                  update = true;
                  return;
                }
              });

              if (update) {
                dispatchSortableEvent('update');
              }
            }
          } // Must be done after capturing individual rects (scroll bar)


          multiDragElements.forEach(function (multiDragElement) {
            unsetRect(multiDragElement);
          });
          toSortable.animateAll();
        }

        multiDragSortable = toSortable;
      } // Remove clones if necessary


      if (rootEl === parentEl || putSortable && putSortable.lastPutMode !== 'clone') {
        multiDragClones.forEach(function (clone) {
          clone.parentNode && clone.parentNode.removeChild(clone);
        });
      }
    },
    nullingGlobal: function nullingGlobal() {
      this.isMultiDrag = dragStarted = false;
      multiDragClones.length = 0;
    },
    destroyGlobal: function destroyGlobal() {
      this._deselectMultiDrag();

      off(document, 'pointerup', this._deselectMultiDrag);
      off(document, 'mouseup', this._deselectMultiDrag);
      off(document, 'touchend', this._deselectMultiDrag);
      off(document, 'keydown', this._checkKeyDown);
      off(document, 'keyup', this._checkKeyUp);
    },
    _deselectMultiDrag: function _deselectMultiDrag(evt) {
      if (typeof dragStarted !== "undefined" && dragStarted) return; // Only deselect if selection is in this sortable

      if (multiDragSortable !== this.sortable) return; // Only deselect if target is not item in this sortable

      if (evt && closest(evt.target, this.options.draggable, this.sortable.el, false)) return; // Only deselect if left click

      if (evt && evt.button !== 0) return;

      while (multiDragElements.length) {
        var el = multiDragElements[0];
        toggleClass(el, this.options.selectedClass, false);
        multiDragElements.shift();
        dispatchEvent({
          sortable: this.sortable,
          rootEl: this.sortable.el,
          name: 'deselect',
          targetEl: el});
      }
    },
    _checkKeyDown: function _checkKeyDown(evt) {
      if (evt.key === this.options.multiDragKey) {
        this.multiDragKeyDown = true;
      }
    },
    _checkKeyUp: function _checkKeyUp(evt) {
      if (evt.key === this.options.multiDragKey) {
        this.multiDragKeyDown = false;
      }
    }
  };
  return _extends(MultiDrag, {
    // Static methods & properties
    pluginName: 'multiDrag',
    utils: {
      /**
       * Selects the provided multi-drag item
       * @param  {HTMLElement} el    The element to be selected
       */
      select: function select(el) {
        var sortable = el.parentNode[expando];
        if (!sortable || !sortable.options.multiDrag || ~multiDragElements.indexOf(el)) return;

        if (multiDragSortable && multiDragSortable !== sortable) {
          multiDragSortable.multiDrag._deselectMultiDrag();

          multiDragSortable = sortable;
        }

        toggleClass(el, sortable.options.selectedClass, true);
        multiDragElements.push(el);
      },

      /**
       * Deselects the provided multi-drag item
       * @param  {HTMLElement} el    The element to be deselected
       */
      deselect: function deselect(el) {
        var sortable = el.parentNode[expando],
            index = multiDragElements.indexOf(el);
        if (!sortable || !sortable.options.multiDrag || !~index) return;
        toggleClass(el, sortable.options.selectedClass, false);
        multiDragElements.splice(index, 1);
      }
    },
    eventProperties: function eventProperties() {
      var _this3 = this;

      var oldIndicies = [],
          newIndicies = [];
      multiDragElements.forEach(function (multiDragElement) {
        oldIndicies.push({
          multiDragElement: multiDragElement,
          index: multiDragElement.sortableIndex
        }); // multiDragElements will already be sorted if folding

        var newIndex;

        if (folding && multiDragElement !== dragEl$1) {
          newIndex = -1;
        } else if (folding) {
          newIndex = index(multiDragElement, ':not(.' + _this3.options.selectedClass + ')');
        } else {
          newIndex = index(multiDragElement);
        }

        newIndicies.push({
          multiDragElement: multiDragElement,
          index: newIndex
        });
      });
      return {
        items: _toConsumableArray(multiDragElements),
        clones: [].concat(multiDragClones),
        oldIndicies: oldIndicies,
        newIndicies: newIndicies
      };
    },
    optionListeners: {
      multiDragKey: function multiDragKey(key) {
        key = key.toLowerCase();

        if (key === 'ctrl') {
          key = 'Control';
        } else if (key.length > 1) {
          key = key.charAt(0).toUpperCase() + key.substr(1);
        }

        return key;
      }
    }
  });
}

function insertMultiDragElements(clonesInserted, rootEl) {
  multiDragElements.forEach(function (multiDragElement, i) {
    var target = rootEl.children[multiDragElement.sortableIndex + (clonesInserted ? Number(i) : 0)];

    if (target) {
      rootEl.insertBefore(multiDragElement, target);
    } else {
      rootEl.appendChild(multiDragElement);
    }
  });
}
/**
 * Insert multi-drag clones
 * @param  {[Boolean]} elementsInserted  Whether the multi-drag elements are inserted
 * @param  {HTMLElement} rootEl
 */


function insertMultiDragClones(elementsInserted, rootEl) {
  multiDragClones.forEach(function (clone, i) {
    var target = rootEl.children[clone.sortableIndex + (elementsInserted ? Number(i) : 0)];

    if (target) {
      rootEl.insertBefore(clone, target);
    } else {
      rootEl.appendChild(clone);
    }
  });
}

function removeMultiDragElements() {
  multiDragElements.forEach(function (multiDragElement) {
    if (multiDragElement === dragEl$1) return;
    multiDragElement.parentNode && multiDragElement.parentNode.removeChild(multiDragElement);
  });
}

Sortable.mount(new AutoScrollPlugin());
Sortable.mount(Remove, Revert);

const sortable_esm = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  MultiDrag: MultiDragPlugin,
  Sortable,
  Swap: SwapPlugin,
  default: Sortable
}, Symbol.toStringTag, { value: 'Module' }));

const require$$0 = /*@__PURE__*/getAugmentedNamespace(sortable_esm);

var vuedraggable_umd = vuedraggable_umd$1.exports;

var hasRequiredVuedraggable_umd;

function requireVuedraggable_umd () {
	if (hasRequiredVuedraggable_umd) return vuedraggable_umd$1.exports;
	hasRequiredVuedraggable_umd = 1;
	(function (module, exports) {
		(function webpackUniversalModuleDefinition(root, factory) {
			module.exports = factory(require$$0);
		})((typeof self !== 'undefined' ? self : vuedraggable_umd), function(__WEBPACK_EXTERNAL_MODULE_a352__) {
		return /******/ (function(modules) { // webpackBootstrap
		/******/ 	// The module cache
		/******/ 	var installedModules = {};
		/******/
		/******/ 	// The require function
		/******/ 	function __webpack_require__(moduleId) {
		/******/
		/******/ 		// Check if module is in cache
		/******/ 		if(installedModules[moduleId]) {
		/******/ 			return installedModules[moduleId].exports;
		/******/ 		}
		/******/ 		// Create a new module (and put it into the cache)
		/******/ 		var module = installedModules[moduleId] = {
		/******/ 			i: moduleId,
		/******/ 			l: false,
		/******/ 			exports: {}
		/******/ 		};
		/******/
		/******/ 		// Execute the module function
		/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
		/******/
		/******/ 		// Flag the module as loaded
		/******/ 		module.l = true;
		/******/
		/******/ 		// Return the exports of the module
		/******/ 		return module.exports;
		/******/ 	}
		/******/
		/******/
		/******/ 	// expose the modules object (__webpack_modules__)
		/******/ 	__webpack_require__.m = modules;
		/******/
		/******/ 	// expose the module cache
		/******/ 	__webpack_require__.c = installedModules;
		/******/
		/******/ 	// define getter function for harmony exports
		/******/ 	__webpack_require__.d = function(exports, name, getter) {
		/******/ 		if(!__webpack_require__.o(exports, name)) {
		/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
		/******/ 		}
		/******/ 	};
		/******/
		/******/ 	// define __esModule on exports
		/******/ 	__webpack_require__.r = function(exports) {
		/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
		/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
		/******/ 		}
		/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
		/******/ 	};
		/******/
		/******/ 	// create a fake namespace object
		/******/ 	// mode & 1: value is a module id, require it
		/******/ 	// mode & 2: merge all properties of value into the ns
		/******/ 	// mode & 4: return value when already ns object
		/******/ 	// mode & 8|1: behave like require
		/******/ 	__webpack_require__.t = function(value, mode) {
		/******/ 		if(mode & 1) value = __webpack_require__(value);
		/******/ 		if(mode & 8) return value;
		/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
		/******/ 		var ns = Object.create(null);
		/******/ 		__webpack_require__.r(ns);
		/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
		/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
		/******/ 		return ns;
		/******/ 	};
		/******/
		/******/ 	// getDefaultExport function for compatibility with non-harmony modules
		/******/ 	__webpack_require__.n = function(module) {
		/******/ 		var getter = module && module.__esModule ?
		/******/ 			function getDefault() { return module['default']; } :
		/******/ 			function getModuleExports() { return module; };
		/******/ 		__webpack_require__.d(getter, 'a', getter);
		/******/ 		return getter;
		/******/ 	};
		/******/
		/******/ 	// Object.prototype.hasOwnProperty.call
		/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
		/******/
		/******/ 	// __webpack_public_path__
		/******/ 	__webpack_require__.p = "";
		/******/
		/******/
		/******/ 	// Load entry module and return exports
		/******/ 	return __webpack_require__(__webpack_require__.s = "fb15");
		/******/ })
		/************************************************************************/
		/******/ ({

		/***/ "01f9":
		/***/ (function(module, exports, __webpack_require__) {

		var LIBRARY = __webpack_require__("2d00");
		var $export = __webpack_require__("5ca1");
		var redefine = __webpack_require__("2aba");
		var hide = __webpack_require__("32e9");
		var Iterators = __webpack_require__("84f2");
		var $iterCreate = __webpack_require__("41a0");
		var setToStringTag = __webpack_require__("7f20");
		var getPrototypeOf = __webpack_require__("38fd");
		var ITERATOR = __webpack_require__("2b4c")('iterator');
		var BUGGY = !([].keys && 'next' in [].keys()); // Safari has buggy iterators w/o `next`
		var FF_ITERATOR = '@@iterator';
		var KEYS = 'keys';
		var VALUES = 'values';

		var returnThis = function () { return this; };

		module.exports = function (Base, NAME, Constructor, next, DEFAULT, IS_SET, FORCED) {
		  $iterCreate(Constructor, NAME, next);
		  var getMethod = function (kind) {
		    if (!BUGGY && kind in proto) return proto[kind];
		    switch (kind) {
		      case KEYS: return function keys() { return new Constructor(this, kind); };
		      case VALUES: return function values() { return new Constructor(this, kind); };
		    } return function entries() { return new Constructor(this, kind); };
		  };
		  var TAG = NAME + ' Iterator';
		  var DEF_VALUES = DEFAULT == VALUES;
		  var VALUES_BUG = false;
		  var proto = Base.prototype;
		  var $native = proto[ITERATOR] || proto[FF_ITERATOR] || DEFAULT && proto[DEFAULT];
		  var $default = $native || getMethod(DEFAULT);
		  var $entries = DEFAULT ? !DEF_VALUES ? $default : getMethod('entries') : undefined;
		  var $anyNative = NAME == 'Array' ? proto.entries || $native : $native;
		  var methods, key, IteratorPrototype;
		  // Fix native
		  if ($anyNative) {
		    IteratorPrototype = getPrototypeOf($anyNative.call(new Base()));
		    if (IteratorPrototype !== Object.prototype && IteratorPrototype.next) {
		      // Set @@toStringTag to native iterators
		      setToStringTag(IteratorPrototype, TAG, true);
		      // fix for some old engines
		      if (!LIBRARY && typeof IteratorPrototype[ITERATOR] != 'function') hide(IteratorPrototype, ITERATOR, returnThis);
		    }
		  }
		  // fix Array#{values, @@iterator}.name in V8 / FF
		  if (DEF_VALUES && $native && $native.name !== VALUES) {
		    VALUES_BUG = true;
		    $default = function values() { return $native.call(this); };
		  }
		  // Define iterator
		  if ((!LIBRARY || FORCED) && (BUGGY || VALUES_BUG || !proto[ITERATOR])) {
		    hide(proto, ITERATOR, $default);
		  }
		  // Plug for library
		  Iterators[NAME] = $default;
		  Iterators[TAG] = returnThis;
		  if (DEFAULT) {
		    methods = {
		      values: DEF_VALUES ? $default : getMethod(VALUES),
		      keys: IS_SET ? $default : getMethod(KEYS),
		      entries: $entries
		    };
		    if (FORCED) for (key in methods) {
		      if (!(key in proto)) redefine(proto, key, methods[key]);
		    } else $export($export.P + $export.F * (BUGGY || VALUES_BUG), NAME, methods);
		  }
		  return methods;
		};


		/***/ }),

		/***/ "02f4":
		/***/ (function(module, exports, __webpack_require__) {

		var toInteger = __webpack_require__("4588");
		var defined = __webpack_require__("be13");
		// true  -> String#at
		// false -> String#codePointAt
		module.exports = function (TO_STRING) {
		  return function (that, pos) {
		    var s = String(defined(that));
		    var i = toInteger(pos);
		    var l = s.length;
		    var a, b;
		    if (i < 0 || i >= l) return TO_STRING ? '' : undefined;
		    a = s.charCodeAt(i);
		    return a < 0xd800 || a > 0xdbff || i + 1 === l || (b = s.charCodeAt(i + 1)) < 0xdc00 || b > 0xdfff
		      ? TO_STRING ? s.charAt(i) : a
		      : TO_STRING ? s.slice(i, i + 2) : (a - 0xd800 << 10) + (b - 0xdc00) + 0x10000;
		  };
		};


		/***/ }),

		/***/ "0390":
		/***/ (function(module, exports, __webpack_require__) {

		var at = __webpack_require__("02f4")(true);

		 // `AdvanceStringIndex` abstract operation
		// https://tc39.github.io/ecma262/#sec-advancestringindex
		module.exports = function (S, index, unicode) {
		  return index + (unicode ? at(S, index).length : 1);
		};


		/***/ }),

		/***/ "0bfb":
		/***/ (function(module, exports, __webpack_require__) {

		// 21.2.5.3 get RegExp.prototype.flags
		var anObject = __webpack_require__("cb7c");
		module.exports = function () {
		  var that = anObject(this);
		  var result = '';
		  if (that.global) result += 'g';
		  if (that.ignoreCase) result += 'i';
		  if (that.multiline) result += 'm';
		  if (that.unicode) result += 'u';
		  if (that.sticky) result += 'y';
		  return result;
		};


		/***/ }),

		/***/ "0d58":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.2.14 / 15.2.3.14 Object.keys(O)
		var $keys = __webpack_require__("ce10");
		var enumBugKeys = __webpack_require__("e11e");

		module.exports = Object.keys || function keys(O) {
		  return $keys(O, enumBugKeys);
		};


		/***/ }),

		/***/ "1495":
		/***/ (function(module, exports, __webpack_require__) {

		var dP = __webpack_require__("86cc");
		var anObject = __webpack_require__("cb7c");
		var getKeys = __webpack_require__("0d58");

		module.exports = __webpack_require__("9e1e") ? Object.defineProperties : function defineProperties(O, Properties) {
		  anObject(O);
		  var keys = getKeys(Properties);
		  var length = keys.length;
		  var i = 0;
		  var P;
		  while (length > i) dP.f(O, P = keys[i++], Properties[P]);
		  return O;
		};


		/***/ }),

		/***/ "214f":
		/***/ (function(module, exports, __webpack_require__) {

		__webpack_require__("b0c5");
		var redefine = __webpack_require__("2aba");
		var hide = __webpack_require__("32e9");
		var fails = __webpack_require__("79e5");
		var defined = __webpack_require__("be13");
		var wks = __webpack_require__("2b4c");
		var regexpExec = __webpack_require__("520a");

		var SPECIES = wks('species');

		var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function () {
		  // #replace needs built-in support for named groups.
		  // #match works fine because it just return the exec results, even if it has
		  // a "grops" property.
		  var re = /./;
		  re.exec = function () {
		    var result = [];
		    result.groups = { a: '7' };
		    return result;
		  };
		  return ''.replace(re, '$<a>') !== '7';
		});

		var SPLIT_WORKS_WITH_OVERWRITTEN_EXEC = (function () {
		  // Chrome 51 has a buggy "split" implementation when RegExp#exec !== nativeExec
		  var re = /(?:)/;
		  var originalExec = re.exec;
		  re.exec = function () { return originalExec.apply(this, arguments); };
		  var result = 'ab'.split(re);
		  return result.length === 2 && result[0] === 'a' && result[1] === 'b';
		})();

		module.exports = function (KEY, length, exec) {
		  var SYMBOL = wks(KEY);

		  var DELEGATES_TO_SYMBOL = !fails(function () {
		    // String methods call symbol-named RegEp methods
		    var O = {};
		    O[SYMBOL] = function () { return 7; };
		    return ''[KEY](O) != 7;
		  });

		  var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL ? !fails(function () {
		    // Symbol-named RegExp methods call .exec
		    var execCalled = false;
		    var re = /a/;
		    re.exec = function () { execCalled = true; return null; };
		    if (KEY === 'split') {
		      // RegExp[@@split] doesn't call the regex's exec method, but first creates
		      // a new one. We need to return the patched regex when creating the new one.
		      re.constructor = {};
		      re.constructor[SPECIES] = function () { return re; };
		    }
		    re[SYMBOL]('');
		    return !execCalled;
		  }) : undefined;

		  if (
		    !DELEGATES_TO_SYMBOL ||
		    !DELEGATES_TO_EXEC ||
		    (KEY === 'replace' && !REPLACE_SUPPORTS_NAMED_GROUPS) ||
		    (KEY === 'split' && !SPLIT_WORKS_WITH_OVERWRITTEN_EXEC)
		  ) {
		    var nativeRegExpMethod = /./[SYMBOL];
		    var fns = exec(
		      defined,
		      SYMBOL,
		      ''[KEY],
		      function maybeCallNative(nativeMethod, regexp, str, arg2, forceStringMethod) {
		        if (regexp.exec === regexpExec) {
		          if (DELEGATES_TO_SYMBOL && !forceStringMethod) {
		            // The native String method already delegates to @@method (this
		            // polyfilled function), leasing to infinite recursion.
		            // We avoid it by directly calling the native @@method method.
		            return { done: true, value: nativeRegExpMethod.call(regexp, str, arg2) };
		          }
		          return { done: true, value: nativeMethod.call(str, regexp, arg2) };
		        }
		        return { done: false };
		      }
		    );
		    var strfn = fns[0];
		    var rxfn = fns[1];

		    redefine(String.prototype, KEY, strfn);
		    hide(RegExp.prototype, SYMBOL, length == 2
		      // 21.2.5.8 RegExp.prototype[@@replace](string, replaceValue)
		      // 21.2.5.11 RegExp.prototype[@@split](string, limit)
		      ? function (string, arg) { return rxfn.call(string, this, arg); }
		      // 21.2.5.6 RegExp.prototype[@@match](string)
		      // 21.2.5.9 RegExp.prototype[@@search](string)
		      : function (string) { return rxfn.call(string, this); }
		    );
		  }
		};


		/***/ }),

		/***/ "230e":
		/***/ (function(module, exports, __webpack_require__) {

		var isObject = __webpack_require__("d3f4");
		var document = __webpack_require__("7726").document;
		// typeof document.createElement is 'object' in old IE
		var is = isObject(document) && isObject(document.createElement);
		module.exports = function (it) {
		  return is ? document.createElement(it) : {};
		};


		/***/ }),

		/***/ "23c6":
		/***/ (function(module, exports, __webpack_require__) {

		// getting tag from 19.1.3.6 Object.prototype.toString()
		var cof = __webpack_require__("2d95");
		var TAG = __webpack_require__("2b4c")('toStringTag');
		// ES3 wrong here
		var ARG = cof(function () { return arguments; }()) == 'Arguments';

		// fallback for IE11 Script Access Denied error
		var tryGet = function (it, key) {
		  try {
		    return it[key];
		  } catch (e) { /* empty */ }
		};

		module.exports = function (it) {
		  var O, T, B;
		  return it === undefined ? 'Undefined' : it === null ? 'Null'
		    // @@toStringTag case
		    : typeof (T = tryGet(O = Object(it), TAG)) == 'string' ? T
		    // builtinTag case
		    : ARG ? cof(O)
		    // ES3 arguments fallback
		    : (B = cof(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : B;
		};


		/***/ }),

		/***/ "2621":
		/***/ (function(module, exports) {

		exports.f = Object.getOwnPropertySymbols;


		/***/ }),

		/***/ "2aba":
		/***/ (function(module, exports, __webpack_require__) {

		var global = __webpack_require__("7726");
		var hide = __webpack_require__("32e9");
		var has = __webpack_require__("69a8");
		var SRC = __webpack_require__("ca5a")('src');
		var $toString = __webpack_require__("fa5b");
		var TO_STRING = 'toString';
		var TPL = ('' + $toString).split(TO_STRING);

		__webpack_require__("8378").inspectSource = function (it) {
		  return $toString.call(it);
		};

		(module.exports = function (O, key, val, safe) {
		  var isFunction = typeof val == 'function';
		  if (isFunction) has(val, 'name') || hide(val, 'name', key);
		  if (O[key] === val) return;
		  if (isFunction) has(val, SRC) || hide(val, SRC, O[key] ? '' + O[key] : TPL.join(String(key)));
		  if (O === global) {
		    O[key] = val;
		  } else if (!safe) {
		    delete O[key];
		    hide(O, key, val);
		  } else if (O[key]) {
		    O[key] = val;
		  } else {
		    hide(O, key, val);
		  }
		// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
		})(Function.prototype, TO_STRING, function toString() {
		  return typeof this == 'function' && this[SRC] || $toString.call(this);
		});


		/***/ }),

		/***/ "2aeb":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
		var anObject = __webpack_require__("cb7c");
		var dPs = __webpack_require__("1495");
		var enumBugKeys = __webpack_require__("e11e");
		var IE_PROTO = __webpack_require__("613b")('IE_PROTO');
		var Empty = function () { /* empty */ };
		var PROTOTYPE = 'prototype';

		// Create object with fake `null` prototype: use iframe Object with cleared prototype
		var createDict = function () {
		  // Thrash, waste and sodomy: IE GC bug
		  var iframe = __webpack_require__("230e")('iframe');
		  var i = enumBugKeys.length;
		  var lt = '<';
		  var gt = '>';
		  var iframeDocument;
		  iframe.style.display = 'none';
		  __webpack_require__("fab2").appendChild(iframe);
		  iframe.src = 'javascript:'; // eslint-disable-line no-script-url
		  // createDict = iframe.contentWindow.Object;
		  // html.removeChild(iframe);
		  iframeDocument = iframe.contentWindow.document;
		  iframeDocument.open();
		  iframeDocument.write(lt + 'script' + gt + 'document.F=Object' + lt + '/script' + gt);
		  iframeDocument.close();
		  createDict = iframeDocument.F;
		  while (i--) delete createDict[PROTOTYPE][enumBugKeys[i]];
		  return createDict();
		};

		module.exports = Object.create || function create(O, Properties) {
		  var result;
		  if (O !== null) {
		    Empty[PROTOTYPE] = anObject(O);
		    result = new Empty();
		    Empty[PROTOTYPE] = null;
		    // add "__proto__" for Object.getPrototypeOf polyfill
		    result[IE_PROTO] = O;
		  } else result = createDict();
		  return Properties === undefined ? result : dPs(result, Properties);
		};


		/***/ }),

		/***/ "2b4c":
		/***/ (function(module, exports, __webpack_require__) {

		var store = __webpack_require__("5537")('wks');
		var uid = __webpack_require__("ca5a");
		var Symbol = __webpack_require__("7726").Symbol;
		var USE_SYMBOL = typeof Symbol == 'function';

		var $exports = module.exports = function (name) {
		  return store[name] || (store[name] =
		    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
		};

		$exports.store = store;


		/***/ }),

		/***/ "2d00":
		/***/ (function(module, exports) {

		module.exports = false;


		/***/ }),

		/***/ "2d95":
		/***/ (function(module, exports) {

		var toString = {}.toString;

		module.exports = function (it) {
		  return toString.call(it).slice(8, -1);
		};


		/***/ }),

		/***/ "2fdb":
		/***/ (function(module, exports, __webpack_require__) {
		// 21.1.3.7 String.prototype.includes(searchString, position = 0)

		var $export = __webpack_require__("5ca1");
		var context = __webpack_require__("d2c8");
		var INCLUDES = 'includes';

		$export($export.P + $export.F * __webpack_require__("5147")(INCLUDES), 'String', {
		  includes: function includes(searchString /* , position = 0 */) {
		    return !!~context(this, searchString, INCLUDES)
		      .indexOf(searchString, arguments.length > 1 ? arguments[1] : undefined);
		  }
		});


		/***/ }),

		/***/ "32e9":
		/***/ (function(module, exports, __webpack_require__) {

		var dP = __webpack_require__("86cc");
		var createDesc = __webpack_require__("4630");
		module.exports = __webpack_require__("9e1e") ? function (object, key, value) {
		  return dP.f(object, key, createDesc(1, value));
		} : function (object, key, value) {
		  object[key] = value;
		  return object;
		};


		/***/ }),

		/***/ "38fd":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.2.9 / 15.2.3.2 Object.getPrototypeOf(O)
		var has = __webpack_require__("69a8");
		var toObject = __webpack_require__("4bf8");
		var IE_PROTO = __webpack_require__("613b")('IE_PROTO');
		var ObjectProto = Object.prototype;

		module.exports = Object.getPrototypeOf || function (O) {
		  O = toObject(O);
		  if (has(O, IE_PROTO)) return O[IE_PROTO];
		  if (typeof O.constructor == 'function' && O instanceof O.constructor) {
		    return O.constructor.prototype;
		  } return O instanceof Object ? ObjectProto : null;
		};


		/***/ }),

		/***/ "41a0":
		/***/ (function(module, exports, __webpack_require__) {

		var create = __webpack_require__("2aeb");
		var descriptor = __webpack_require__("4630");
		var setToStringTag = __webpack_require__("7f20");
		var IteratorPrototype = {};

		// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
		__webpack_require__("32e9")(IteratorPrototype, __webpack_require__("2b4c")('iterator'), function () { return this; });

		module.exports = function (Constructor, NAME, next) {
		  Constructor.prototype = create(IteratorPrototype, { next: descriptor(1, next) });
		  setToStringTag(Constructor, NAME + ' Iterator');
		};


		/***/ }),

		/***/ "456d":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.2.14 Object.keys(O)
		var toObject = __webpack_require__("4bf8");
		var $keys = __webpack_require__("0d58");

		__webpack_require__("5eda")('keys', function () {
		  return function keys(it) {
		    return $keys(toObject(it));
		  };
		});


		/***/ }),

		/***/ "4588":
		/***/ (function(module, exports) {

		// 7.1.4 ToInteger
		var ceil = Math.ceil;
		var floor = Math.floor;
		module.exports = function (it) {
		  return isNaN(it = +it) ? 0 : (it > 0 ? floor : ceil)(it);
		};


		/***/ }),

		/***/ "4630":
		/***/ (function(module, exports) {

		module.exports = function (bitmap, value) {
		  return {
		    enumerable: !(bitmap & 1),
		    configurable: !(bitmap & 2),
		    writable: !(bitmap & 4),
		    value: value
		  };
		};


		/***/ }),

		/***/ "4bf8":
		/***/ (function(module, exports, __webpack_require__) {

		// 7.1.13 ToObject(argument)
		var defined = __webpack_require__("be13");
		module.exports = function (it) {
		  return Object(defined(it));
		};


		/***/ }),

		/***/ "5147":
		/***/ (function(module, exports, __webpack_require__) {

		var MATCH = __webpack_require__("2b4c")('match');
		module.exports = function (KEY) {
		  var re = /./;
		  try {
		    '/./'[KEY](re);
		  } catch (e) {
		    try {
		      re[MATCH] = false;
		      return !'/./'[KEY](re);
		    } catch (f) { /* empty */ }
		  } return true;
		};


		/***/ }),

		/***/ "520a":
		/***/ (function(module, exports, __webpack_require__) {


		var regexpFlags = __webpack_require__("0bfb");

		var nativeExec = RegExp.prototype.exec;
		// This always refers to the native implementation, because the
		// String#replace polyfill uses ./fix-regexp-well-known-symbol-logic.js,
		// which loads this file before patching the method.
		var nativeReplace = String.prototype.replace;

		var patchedExec = nativeExec;

		var LAST_INDEX = 'lastIndex';

		var UPDATES_LAST_INDEX_WRONG = (function () {
		  var re1 = /a/,
		      re2 = /b*/g;
		  nativeExec.call(re1, 'a');
		  nativeExec.call(re2, 'a');
		  return re1[LAST_INDEX] !== 0 || re2[LAST_INDEX] !== 0;
		})();

		// nonparticipating capturing group, copied from es5-shim's String#split patch.
		var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;

		var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED;

		if (PATCH) {
		  patchedExec = function exec(str) {
		    var re = this;
		    var lastIndex, reCopy, match, i;

		    if (NPCG_INCLUDED) {
		      reCopy = new RegExp('^' + re.source + '$(?!\\s)', regexpFlags.call(re));
		    }
		    if (UPDATES_LAST_INDEX_WRONG) lastIndex = re[LAST_INDEX];

		    match = nativeExec.call(re, str);

		    if (UPDATES_LAST_INDEX_WRONG && match) {
		      re[LAST_INDEX] = re.global ? match.index + match[0].length : lastIndex;
		    }
		    if (NPCG_INCLUDED && match && match.length > 1) {
		      // Fix browsers whose `exec` methods don't consistently return `undefined`
		      // for NPCG, like IE8. NOTE: This doesn' work for /(.?)?/
		      // eslint-disable-next-line no-loop-func
		      nativeReplace.call(match[0], reCopy, function () {
		        for (i = 1; i < arguments.length - 2; i++) {
		          if (arguments[i] === undefined) match[i] = undefined;
		        }
		      });
		    }

		    return match;
		  };
		}

		module.exports = patchedExec;


		/***/ }),

		/***/ "52a7":
		/***/ (function(module, exports) {

		exports.f = {}.propertyIsEnumerable;


		/***/ }),

		/***/ "5537":
		/***/ (function(module, exports, __webpack_require__) {

		var core = __webpack_require__("8378");
		var global = __webpack_require__("7726");
		var SHARED = '__core-js_shared__';
		var store = global[SHARED] || (global[SHARED] = {});

		(module.exports = function (key, value) {
		  return store[key] || (store[key] = value !== undefined ? value : {});
		})('versions', []).push({
		  version: core.version,
		  mode: __webpack_require__("2d00") ? 'pure' : 'global',
		  copyright: '© 2019 Denis Pushkarev (zloirock.ru)'
		});


		/***/ }),

		/***/ "5ca1":
		/***/ (function(module, exports, __webpack_require__) {

		var global = __webpack_require__("7726");
		var core = __webpack_require__("8378");
		var hide = __webpack_require__("32e9");
		var redefine = __webpack_require__("2aba");
		var ctx = __webpack_require__("9b43");
		var PROTOTYPE = 'prototype';

		var $export = function (type, name, source) {
		  var IS_FORCED = type & $export.F;
		  var IS_GLOBAL = type & $export.G;
		  var IS_STATIC = type & $export.S;
		  var IS_PROTO = type & $export.P;
		  var IS_BIND = type & $export.B;
		  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] || (global[name] = {}) : (global[name] || {})[PROTOTYPE];
		  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
		  var expProto = exports[PROTOTYPE] || (exports[PROTOTYPE] = {});
		  var key, own, out, exp;
		  if (IS_GLOBAL) source = name;
		  for (key in source) {
		    // contains in native
		    own = !IS_FORCED && target && target[key] !== undefined;
		    // export native or passed
		    out = (own ? target : source)[key];
		    // bind timers to global for call from export context
		    exp = IS_BIND && own ? ctx(out, global) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
		    // extend global
		    if (target) redefine(target, key, out, type & $export.U);
		    // export
		    if (exports[key] != out) hide(exports, key, exp);
		    if (IS_PROTO && expProto[key] != out) expProto[key] = out;
		  }
		};
		global.core = core;
		// type bitmap
		$export.F = 1;   // forced
		$export.G = 2;   // global
		$export.S = 4;   // static
		$export.P = 8;   // proto
		$export.B = 16;  // bind
		$export.W = 32;  // wrap
		$export.U = 64;  // safe
		$export.R = 128; // real proto method for `library`
		module.exports = $export;


		/***/ }),

		/***/ "5eda":
		/***/ (function(module, exports, __webpack_require__) {

		// most Object methods by ES6 should accept primitives
		var $export = __webpack_require__("5ca1");
		var core = __webpack_require__("8378");
		var fails = __webpack_require__("79e5");
		module.exports = function (KEY, exec) {
		  var fn = (core.Object || {})[KEY] || Object[KEY];
		  var exp = {};
		  exp[KEY] = exec(fn);
		  $export($export.S + $export.F * fails(function () { fn(1); }), 'Object', exp);
		};


		/***/ }),

		/***/ "5f1b":
		/***/ (function(module, exports, __webpack_require__) {


		var classof = __webpack_require__("23c6");
		var builtinExec = RegExp.prototype.exec;

		 // `RegExpExec` abstract operation
		// https://tc39.github.io/ecma262/#sec-regexpexec
		module.exports = function (R, S) {
		  var exec = R.exec;
		  if (typeof exec === 'function') {
		    var result = exec.call(R, S);
		    if (typeof result !== 'object') {
		      throw new TypeError('RegExp exec method returned something other than an Object or null');
		    }
		    return result;
		  }
		  if (classof(R) !== 'RegExp') {
		    throw new TypeError('RegExp#exec called on incompatible receiver');
		  }
		  return builtinExec.call(R, S);
		};


		/***/ }),

		/***/ "613b":
		/***/ (function(module, exports, __webpack_require__) {

		var shared = __webpack_require__("5537")('keys');
		var uid = __webpack_require__("ca5a");
		module.exports = function (key) {
		  return shared[key] || (shared[key] = uid(key));
		};


		/***/ }),

		/***/ "626a":
		/***/ (function(module, exports, __webpack_require__) {

		// fallback for non-array-like ES3 and non-enumerable old V8 strings
		var cof = __webpack_require__("2d95");
		// eslint-disable-next-line no-prototype-builtins
		module.exports = Object('z').propertyIsEnumerable(0) ? Object : function (it) {
		  return cof(it) == 'String' ? it.split('') : Object(it);
		};


		/***/ }),

		/***/ "6762":
		/***/ (function(module, exports, __webpack_require__) {

		// https://github.com/tc39/Array.prototype.includes
		var $export = __webpack_require__("5ca1");
		var $includes = __webpack_require__("c366")(true);

		$export($export.P, 'Array', {
		  includes: function includes(el /* , fromIndex = 0 */) {
		    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
		  }
		});

		__webpack_require__("9c6c")('includes');


		/***/ }),

		/***/ "6821":
		/***/ (function(module, exports, __webpack_require__) {

		// to indexed object, toObject with fallback for non-array-like ES3 strings
		var IObject = __webpack_require__("626a");
		var defined = __webpack_require__("be13");
		module.exports = function (it) {
		  return IObject(defined(it));
		};


		/***/ }),

		/***/ "69a8":
		/***/ (function(module, exports) {

		var hasOwnProperty = {}.hasOwnProperty;
		module.exports = function (it, key) {
		  return hasOwnProperty.call(it, key);
		};


		/***/ }),

		/***/ "6a99":
		/***/ (function(module, exports, __webpack_require__) {

		// 7.1.1 ToPrimitive(input [, PreferredType])
		var isObject = __webpack_require__("d3f4");
		// instead of the ES6 spec version, we didn't implement @@toPrimitive case
		// and the second argument - flag - preferred type is a string
		module.exports = function (it, S) {
		  if (!isObject(it)) return it;
		  var fn, val;
		  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
		  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
		  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
		  throw TypeError("Can't convert object to primitive value");
		};


		/***/ }),

		/***/ "7333":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.2.1 Object.assign(target, source, ...)
		var getKeys = __webpack_require__("0d58");
		var gOPS = __webpack_require__("2621");
		var pIE = __webpack_require__("52a7");
		var toObject = __webpack_require__("4bf8");
		var IObject = __webpack_require__("626a");
		var $assign = Object.assign;

		// should work with symbols and should have deterministic property order (V8 bug)
		module.exports = !$assign || __webpack_require__("79e5")(function () {
		  var A = {};
		  var B = {};
		  // eslint-disable-next-line no-undef
		  var S = Symbol();
		  var K = 'abcdefghijklmnopqrst';
		  A[S] = 7;
		  K.split('').forEach(function (k) { B[k] = k; });
		  return $assign({}, A)[S] != 7 || Object.keys($assign({}, B)).join('') != K;
		}) ? function assign(target, source) { // eslint-disable-line no-unused-vars
		  var T = toObject(target);
		  var aLen = arguments.length;
		  var index = 1;
		  var getSymbols = gOPS.f;
		  var isEnum = pIE.f;
		  while (aLen > index) {
		    var S = IObject(arguments[index++]);
		    var keys = getSymbols ? getKeys(S).concat(getSymbols(S)) : getKeys(S);
		    var length = keys.length;
		    var j = 0;
		    var key;
		    while (length > j) if (isEnum.call(S, key = keys[j++])) T[key] = S[key];
		  } return T;
		} : $assign;


		/***/ }),

		/***/ "7726":
		/***/ (function(module, exports) {

		// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
		var global = module.exports = typeof window != 'undefined' && window.Math == Math
		  ? window : typeof self != 'undefined' && self.Math == Math ? self
		  // eslint-disable-next-line no-new-func
		  : Function('return this')();
		if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef


		/***/ }),

		/***/ "77f1":
		/***/ (function(module, exports, __webpack_require__) {

		var toInteger = __webpack_require__("4588");
		var max = Math.max;
		var min = Math.min;
		module.exports = function (index, length) {
		  index = toInteger(index);
		  return index < 0 ? max(index + length, 0) : min(index, length);
		};


		/***/ }),

		/***/ "79e5":
		/***/ (function(module, exports) {

		module.exports = function (exec) {
		  try {
		    return !!exec();
		  } catch (e) {
		    return true;
		  }
		};


		/***/ }),

		/***/ "7f20":
		/***/ (function(module, exports, __webpack_require__) {

		var def = __webpack_require__("86cc").f;
		var has = __webpack_require__("69a8");
		var TAG = __webpack_require__("2b4c")('toStringTag');

		module.exports = function (it, tag, stat) {
		  if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
		};


		/***/ }),

		/***/ "8378":
		/***/ (function(module, exports) {

		var core = module.exports = { version: '2.6.5' };
		if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef


		/***/ }),

		/***/ "84f2":
		/***/ (function(module, exports) {

		module.exports = {};


		/***/ }),

		/***/ "86cc":
		/***/ (function(module, exports, __webpack_require__) {

		var anObject = __webpack_require__("cb7c");
		var IE8_DOM_DEFINE = __webpack_require__("c69a");
		var toPrimitive = __webpack_require__("6a99");
		var dP = Object.defineProperty;

		exports.f = __webpack_require__("9e1e") ? Object.defineProperty : function defineProperty(O, P, Attributes) {
		  anObject(O);
		  P = toPrimitive(P, true);
		  anObject(Attributes);
		  if (IE8_DOM_DEFINE) try {
		    return dP(O, P, Attributes);
		  } catch (e) { /* empty */ }
		  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
		  if ('value' in Attributes) O[P] = Attributes.value;
		  return O;
		};


		/***/ }),

		/***/ "9b43":
		/***/ (function(module, exports, __webpack_require__) {

		// optional / simple context binding
		var aFunction = __webpack_require__("d8e8");
		module.exports = function (fn, that, length) {
		  aFunction(fn);
		  if (that === undefined) return fn;
		  switch (length) {
		    case 1: return function (a) {
		      return fn.call(that, a);
		    };
		    case 2: return function (a, b) {
		      return fn.call(that, a, b);
		    };
		    case 3: return function (a, b, c) {
		      return fn.call(that, a, b, c);
		    };
		  }
		  return function (/* ...args */) {
		    return fn.apply(that, arguments);
		  };
		};


		/***/ }),

		/***/ "9c6c":
		/***/ (function(module, exports, __webpack_require__) {

		// 22.1.3.31 Array.prototype[@@unscopables]
		var UNSCOPABLES = __webpack_require__("2b4c")('unscopables');
		var ArrayProto = Array.prototype;
		if (ArrayProto[UNSCOPABLES] == undefined) __webpack_require__("32e9")(ArrayProto, UNSCOPABLES, {});
		module.exports = function (key) {
		  ArrayProto[UNSCOPABLES][key] = true;
		};


		/***/ }),

		/***/ "9def":
		/***/ (function(module, exports, __webpack_require__) {

		// 7.1.15 ToLength
		var toInteger = __webpack_require__("4588");
		var min = Math.min;
		module.exports = function (it) {
		  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
		};


		/***/ }),

		/***/ "9e1e":
		/***/ (function(module, exports, __webpack_require__) {

		// Thank's IE8 for his funny defineProperty
		module.exports = !__webpack_require__("79e5")(function () {
		  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
		});


		/***/ }),

		/***/ "a352":
		/***/ (function(module, exports) {

		module.exports = __WEBPACK_EXTERNAL_MODULE_a352__;

		/***/ }),

		/***/ "a481":
		/***/ (function(module, exports, __webpack_require__) {


		var anObject = __webpack_require__("cb7c");
		var toObject = __webpack_require__("4bf8");
		var toLength = __webpack_require__("9def");
		var toInteger = __webpack_require__("4588");
		var advanceStringIndex = __webpack_require__("0390");
		var regExpExec = __webpack_require__("5f1b");
		var max = Math.max;
		var min = Math.min;
		var floor = Math.floor;
		var SUBSTITUTION_SYMBOLS = /\$([$&`']|\d\d?|<[^>]*>)/g;
		var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&`']|\d\d?)/g;

		var maybeToString = function (it) {
		  return it === undefined ? it : String(it);
		};

		// @@replace logic
		__webpack_require__("214f")('replace', 2, function (defined, REPLACE, $replace, maybeCallNative) {
		  return [
		    // `String.prototype.replace` method
		    // https://tc39.github.io/ecma262/#sec-string.prototype.replace
		    function replace(searchValue, replaceValue) {
		      var O = defined(this);
		      var fn = searchValue == undefined ? undefined : searchValue[REPLACE];
		      return fn !== undefined
		        ? fn.call(searchValue, O, replaceValue)
		        : $replace.call(String(O), searchValue, replaceValue);
		    },
		    // `RegExp.prototype[@@replace]` method
		    // https://tc39.github.io/ecma262/#sec-regexp.prototype-@@replace
		    function (regexp, replaceValue) {
		      var res = maybeCallNative($replace, regexp, this, replaceValue);
		      if (res.done) return res.value;

		      var rx = anObject(regexp);
		      var S = String(this);
		      var functionalReplace = typeof replaceValue === 'function';
		      if (!functionalReplace) replaceValue = String(replaceValue);
		      var global = rx.global;
		      if (global) {
		        var fullUnicode = rx.unicode;
		        rx.lastIndex = 0;
		      }
		      var results = [];
		      while (true) {
		        var result = regExpExec(rx, S);
		        if (result === null) break;
		        results.push(result);
		        if (!global) break;
		        var matchStr = String(result[0]);
		        if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
		      }
		      var accumulatedResult = '';
		      var nextSourcePosition = 0;
		      for (var i = 0; i < results.length; i++) {
		        result = results[i];
		        var matched = String(result[0]);
		        var position = max(min(toInteger(result.index), S.length), 0);
		        var captures = [];
		        // NOTE: This is equivalent to
		        //   captures = result.slice(1).map(maybeToString)
		        // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
		        // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
		        // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.
		        for (var j = 1; j < result.length; j++) captures.push(maybeToString(result[j]));
		        var namedCaptures = result.groups;
		        if (functionalReplace) {
		          var replacerArgs = [matched].concat(captures, position, S);
		          if (namedCaptures !== undefined) replacerArgs.push(namedCaptures);
		          var replacement = String(replaceValue.apply(undefined, replacerArgs));
		        } else {
		          replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
		        }
		        if (position >= nextSourcePosition) {
		          accumulatedResult += S.slice(nextSourcePosition, position) + replacement;
		          nextSourcePosition = position + matched.length;
		        }
		      }
		      return accumulatedResult + S.slice(nextSourcePosition);
		    }
		  ];

		    // https://tc39.github.io/ecma262/#sec-getsubstitution
		  function getSubstitution(matched, str, position, captures, namedCaptures, replacement) {
		    var tailPos = position + matched.length;
		    var m = captures.length;
		    var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;
		    if (namedCaptures !== undefined) {
		      namedCaptures = toObject(namedCaptures);
		      symbols = SUBSTITUTION_SYMBOLS;
		    }
		    return $replace.call(replacement, symbols, function (match, ch) {
		      var capture;
		      switch (ch.charAt(0)) {
		        case '$': return '$';
		        case '&': return matched;
		        case '`': return str.slice(0, position);
		        case "'": return str.slice(tailPos);
		        case '<':
		          capture = namedCaptures[ch.slice(1, -1)];
		          break;
		        default: // \d\d?
		          var n = +ch;
		          if (n === 0) return match;
		          if (n > m) {
		            var f = floor(n / 10);
		            if (f === 0) return match;
		            if (f <= m) return captures[f - 1] === undefined ? ch.charAt(1) : captures[f - 1] + ch.charAt(1);
		            return match;
		          }
		          capture = captures[n - 1];
		      }
		      return capture === undefined ? '' : capture;
		    });
		  }
		});


		/***/ }),

		/***/ "aae3":
		/***/ (function(module, exports, __webpack_require__) {

		// 7.2.8 IsRegExp(argument)
		var isObject = __webpack_require__("d3f4");
		var cof = __webpack_require__("2d95");
		var MATCH = __webpack_require__("2b4c")('match');
		module.exports = function (it) {
		  var isRegExp;
		  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : cof(it) == 'RegExp');
		};


		/***/ }),

		/***/ "ac6a":
		/***/ (function(module, exports, __webpack_require__) {

		var $iterators = __webpack_require__("cadf");
		var getKeys = __webpack_require__("0d58");
		var redefine = __webpack_require__("2aba");
		var global = __webpack_require__("7726");
		var hide = __webpack_require__("32e9");
		var Iterators = __webpack_require__("84f2");
		var wks = __webpack_require__("2b4c");
		var ITERATOR = wks('iterator');
		var TO_STRING_TAG = wks('toStringTag');
		var ArrayValues = Iterators.Array;

		var DOMIterables = {
		  CSSRuleList: true, // TODO: Not spec compliant, should be false.
		  CSSStyleDeclaration: false,
		  CSSValueList: false,
		  ClientRectList: false,
		  DOMRectList: false,
		  DOMStringList: false,
		  DOMTokenList: true,
		  DataTransferItemList: false,
		  FileList: false,
		  HTMLAllCollection: false,
		  HTMLCollection: false,
		  HTMLFormElement: false,
		  HTMLSelectElement: false,
		  MediaList: true, // TODO: Not spec compliant, should be false.
		  MimeTypeArray: false,
		  NamedNodeMap: false,
		  NodeList: true,
		  PaintRequestList: false,
		  Plugin: false,
		  PluginArray: false,
		  SVGLengthList: false,
		  SVGNumberList: false,
		  SVGPathSegList: false,
		  SVGPointList: false,
		  SVGStringList: false,
		  SVGTransformList: false,
		  SourceBufferList: false,
		  StyleSheetList: true, // TODO: Not spec compliant, should be false.
		  TextTrackCueList: false,
		  TextTrackList: false,
		  TouchList: false
		};

		for (var collections = getKeys(DOMIterables), i = 0; i < collections.length; i++) {
		  var NAME = collections[i];
		  var explicit = DOMIterables[NAME];
		  var Collection = global[NAME];
		  var proto = Collection && Collection.prototype;
		  var key;
		  if (proto) {
		    if (!proto[ITERATOR]) hide(proto, ITERATOR, ArrayValues);
		    if (!proto[TO_STRING_TAG]) hide(proto, TO_STRING_TAG, NAME);
		    Iterators[NAME] = ArrayValues;
		    if (explicit) for (key in $iterators) if (!proto[key]) redefine(proto, key, $iterators[key], true);
		  }
		}


		/***/ }),

		/***/ "b0c5":
		/***/ (function(module, exports, __webpack_require__) {

		var regexpExec = __webpack_require__("520a");
		__webpack_require__("5ca1")({
		  target: 'RegExp',
		  proto: true,
		  forced: regexpExec !== /./.exec
		}, {
		  exec: regexpExec
		});


		/***/ }),

		/***/ "be13":
		/***/ (function(module, exports) {

		// 7.2.1 RequireObjectCoercible(argument)
		module.exports = function (it) {
		  if (it == undefined) throw TypeError("Can't call method on  " + it);
		  return it;
		};


		/***/ }),

		/***/ "c366":
		/***/ (function(module, exports, __webpack_require__) {

		// false -> Array#indexOf
		// true  -> Array#includes
		var toIObject = __webpack_require__("6821");
		var toLength = __webpack_require__("9def");
		var toAbsoluteIndex = __webpack_require__("77f1");
		module.exports = function (IS_INCLUDES) {
		  return function ($this, el, fromIndex) {
		    var O = toIObject($this);
		    var length = toLength(O.length);
		    var index = toAbsoluteIndex(fromIndex, length);
		    var value;
		    // Array#includes uses SameValueZero equality algorithm
		    // eslint-disable-next-line no-self-compare
		    if (IS_INCLUDES && el != el) while (length > index) {
		      value = O[index++];
		      // eslint-disable-next-line no-self-compare
		      if (value != value) return true;
		    // Array#indexOf ignores holes, Array#includes - not
		    } else for (;length > index; index++) if (IS_INCLUDES || index in O) {
		      if (O[index] === el) return IS_INCLUDES || index || 0;
		    } return !IS_INCLUDES && -1;
		  };
		};


		/***/ }),

		/***/ "c649":
		/***/ (function(module, __webpack_exports__, __webpack_require__) {
		/* WEBPACK VAR INJECTION */(function(global) {/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return insertNodeAt; });
		/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return camelize; });
		/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return console; });
		/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return removeNode; });
		/* harmony import */ __webpack_require__("a481");


		function getConsole() {
		  if (typeof window !== "undefined") {
		    return window.console;
		  }

		  return global.console;
		}

		var console = getConsole();

		function cached(fn) {
		  var cache = Object.create(null);
		  return function cachedFn(str) {
		    var hit = cache[str];
		    return hit || (cache[str] = fn(str));
		  };
		}

		var regex = /-(\w)/g;
		var camelize = cached(function (str) {
		  return str.replace(regex, function (_, c) {
		    return c ? c.toUpperCase() : "";
		  });
		});

		function removeNode(node) {
		  if (node.parentElement !== null) {
		    node.parentElement.removeChild(node);
		  }
		}

		function insertNodeAt(fatherNode, node, position) {
		  var refNode = position === 0 ? fatherNode.children[0] : fatherNode.children[position - 1].nextSibling;
		  fatherNode.insertBefore(node, refNode);
		}


		/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__("c8ba")));

		/***/ }),

		/***/ "c69a":
		/***/ (function(module, exports, __webpack_require__) {

		module.exports = !__webpack_require__("9e1e") && !__webpack_require__("79e5")(function () {
		  return Object.defineProperty(__webpack_require__("230e")('div'), 'a', { get: function () { return 7; } }).a != 7;
		});


		/***/ }),

		/***/ "c8ba":
		/***/ (function(module, exports) {

		var g;

		// This works in non-strict mode
		g = (function() {
			return this;
		})();

		try {
			// This works if eval is allowed (see CSP)
			g = g || new Function("return this")();
		} catch (e) {
			// This works if the window reference is available
			if (typeof window === "object") g = window;
		}

		// g can still be undefined, but nothing to do about it...
		// We return undefined, instead of nothing here, so it's
		// easier to handle this case. if(!global) { ...}

		module.exports = g;


		/***/ }),

		/***/ "ca5a":
		/***/ (function(module, exports) {

		var id = 0;
		var px = Math.random();
		module.exports = function (key) {
		  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
		};


		/***/ }),

		/***/ "cadf":
		/***/ (function(module, exports, __webpack_require__) {

		var addToUnscopables = __webpack_require__("9c6c");
		var step = __webpack_require__("d53b");
		var Iterators = __webpack_require__("84f2");
		var toIObject = __webpack_require__("6821");

		// 22.1.3.4 Array.prototype.entries()
		// 22.1.3.13 Array.prototype.keys()
		// 22.1.3.29 Array.prototype.values()
		// 22.1.3.30 Array.prototype[@@iterator]()
		module.exports = __webpack_require__("01f9")(Array, 'Array', function (iterated, kind) {
		  this._t = toIObject(iterated); // target
		  this._i = 0;                   // next index
		  this._k = kind;                // kind
		// 22.1.5.2.1 %ArrayIteratorPrototype%.next()
		}, function () {
		  var O = this._t;
		  var kind = this._k;
		  var index = this._i++;
		  if (!O || index >= O.length) {
		    this._t = undefined;
		    return step(1);
		  }
		  if (kind == 'keys') return step(0, index);
		  if (kind == 'values') return step(0, O[index]);
		  return step(0, [index, O[index]]);
		}, 'values');

		// argumentsList[@@iterator] is %ArrayProto_values% (9.4.4.6, 9.4.4.7)
		Iterators.Arguments = Iterators.Array;

		addToUnscopables('keys');
		addToUnscopables('values');
		addToUnscopables('entries');


		/***/ }),

		/***/ "cb7c":
		/***/ (function(module, exports, __webpack_require__) {

		var isObject = __webpack_require__("d3f4");
		module.exports = function (it) {
		  if (!isObject(it)) throw TypeError(it + ' is not an object!');
		  return it;
		};


		/***/ }),

		/***/ "ce10":
		/***/ (function(module, exports, __webpack_require__) {

		var has = __webpack_require__("69a8");
		var toIObject = __webpack_require__("6821");
		var arrayIndexOf = __webpack_require__("c366")(false);
		var IE_PROTO = __webpack_require__("613b")('IE_PROTO');

		module.exports = function (object, names) {
		  var O = toIObject(object);
		  var i = 0;
		  var result = [];
		  var key;
		  for (key in O) if (key != IE_PROTO) has(O, key) && result.push(key);
		  // Don't enum bug & hidden keys
		  while (names.length > i) if (has(O, key = names[i++])) {
		    ~arrayIndexOf(result, key) || result.push(key);
		  }
		  return result;
		};


		/***/ }),

		/***/ "d2c8":
		/***/ (function(module, exports, __webpack_require__) {

		// helper for String#{startsWith, endsWith, includes}
		var isRegExp = __webpack_require__("aae3");
		var defined = __webpack_require__("be13");

		module.exports = function (that, searchString, NAME) {
		  if (isRegExp(searchString)) throw TypeError('String#' + NAME + " doesn't accept regex!");
		  return String(defined(that));
		};


		/***/ }),

		/***/ "d3f4":
		/***/ (function(module, exports) {

		module.exports = function (it) {
		  return typeof it === 'object' ? it !== null : typeof it === 'function';
		};


		/***/ }),

		/***/ "d53b":
		/***/ (function(module, exports) {

		module.exports = function (done, value) {
		  return { value: value, done: !!done };
		};


		/***/ }),

		/***/ "d8e8":
		/***/ (function(module, exports) {

		module.exports = function (it) {
		  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
		  return it;
		};


		/***/ }),

		/***/ "e11e":
		/***/ (function(module, exports) {

		// IE 8- don't enum bug keys
		module.exports = (
		  'constructor,hasOwnProperty,isPrototypeOf,propertyIsEnumerable,toLocaleString,toString,valueOf'
		).split(',');


		/***/ }),

		/***/ "f559":
		/***/ (function(module, exports, __webpack_require__) {
		// 21.1.3.18 String.prototype.startsWith(searchString [, position ])

		var $export = __webpack_require__("5ca1");
		var toLength = __webpack_require__("9def");
		var context = __webpack_require__("d2c8");
		var STARTS_WITH = 'startsWith';
		var $startsWith = ''[STARTS_WITH];

		$export($export.P + $export.F * __webpack_require__("5147")(STARTS_WITH), 'String', {
		  startsWith: function startsWith(searchString /* , position = 0 */) {
		    var that = context(this, searchString, STARTS_WITH);
		    var index = toLength(Math.min(arguments.length > 1 ? arguments[1] : undefined, that.length));
		    var search = String(searchString);
		    return $startsWith
		      ? $startsWith.call(that, search, index)
		      : that.slice(index, index + search.length) === search;
		  }
		});


		/***/ }),

		/***/ "f6fd":
		/***/ (function(module, exports) {

		// document.currentScript polyfill by Adam Miller

		// MIT license

		(function(document){
		  var currentScript = "currentScript",
		      scripts = document.getElementsByTagName('script'); // Live NodeList collection

		  // If browser needs currentScript polyfill, add get currentScript() to the document object
		  if (!(currentScript in document)) {
		    Object.defineProperty(document, currentScript, {
		      get: function(){

		        // IE 6-10 supports script readyState
		        // IE 10+ support stack trace
		        try { throw new Error(); }
		        catch (err) {

		          // Find the second match for the "at" string to get file src url from stack.
		          // Specifically works with the format of stack traces in IE.
		          var i, res = ((/.*at [^\(]*\((.*):.+:.+\)$/ig).exec(err.stack) || [false])[1];

		          // For all scripts on the page, if src matches or if ready state is interactive, return the script tag
		          for(i in scripts){
		            if(scripts[i].src == res || scripts[i].readyState == "interactive"){
		              return scripts[i];
		            }
		          }

		          // If no match, return null
		          return null;
		        }
		      }
		    });
		  }
		})(document);


		/***/ }),

		/***/ "f751":
		/***/ (function(module, exports, __webpack_require__) {

		// 19.1.3.1 Object.assign(target, source)
		var $export = __webpack_require__("5ca1");

		$export($export.S + $export.F, 'Object', { assign: __webpack_require__("7333") });


		/***/ }),

		/***/ "fa5b":
		/***/ (function(module, exports, __webpack_require__) {

		module.exports = __webpack_require__("5537")('native-function-to-string', Function.toString);


		/***/ }),

		/***/ "fab2":
		/***/ (function(module, exports, __webpack_require__) {

		var document = __webpack_require__("7726").document;
		module.exports = document && document.documentElement;


		/***/ }),

		/***/ "fb15":
		/***/ (function(module, __webpack_exports__, __webpack_require__) {
		// ESM COMPAT FLAG
		__webpack_require__.r(__webpack_exports__);

		// CONCATENATED MODULE: ./node_modules/@vue/cli-service/lib/commands/build/setPublicPath.js
		// This file is imported into lib/wc client bundles.

		if (typeof window !== 'undefined') {
		  {
		    __webpack_require__("f6fd");
		  }

		  var setPublicPath_i;
		  if ((setPublicPath_i = window.document.currentScript) && (setPublicPath_i = setPublicPath_i.src.match(/(.+\/)[^/]+\.js(\?.*)?$/))) {
		    __webpack_require__.p = setPublicPath_i[1]; // eslint-disable-line
		  }
		}

		// EXTERNAL MODULE: ./node_modules/core-js/modules/es6.object.assign.js
		__webpack_require__("f751");

		// EXTERNAL MODULE: ./node_modules/core-js/modules/es6.string.starts-with.js
		__webpack_require__("f559");

		// EXTERNAL MODULE: ./node_modules/core-js/modules/web.dom.iterable.js
		__webpack_require__("ac6a");

		// EXTERNAL MODULE: ./node_modules/core-js/modules/es6.array.iterator.js
		__webpack_require__("cadf");

		// EXTERNAL MODULE: ./node_modules/core-js/modules/es6.object.keys.js
		__webpack_require__("456d");

		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/arrayWithHoles.js
		function _arrayWithHoles(arr) {
		  if (Array.isArray(arr)) return arr;
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/iterableToArrayLimit.js
		function _iterableToArrayLimit(arr, i) {
		  if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return;
		  var _arr = [];
		  var _n = true;
		  var _d = false;
		  var _e = undefined;

		  try {
		    for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
		      _arr.push(_s.value);

		      if (i && _arr.length === i) break;
		    }
		  } catch (err) {
		    _d = true;
		    _e = err;
		  } finally {
		    try {
		      if (!_n && _i["return"] != null) _i["return"]();
		    } finally {
		      if (_d) throw _e;
		    }
		  }

		  return _arr;
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/arrayLikeToArray.js
		function _arrayLikeToArray(arr, len) {
		  if (len == null || len > arr.length) len = arr.length;

		  for (var i = 0, arr2 = new Array(len); i < len; i++) {
		    arr2[i] = arr[i];
		  }

		  return arr2;
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/unsupportedIterableToArray.js

		function _unsupportedIterableToArray(o, minLen) {
		  if (!o) return;
		  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
		  var n = Object.prototype.toString.call(o).slice(8, -1);
		  if (n === "Object" && o.constructor) n = o.constructor.name;
		  if (n === "Map" || n === "Set") return Array.from(o);
		  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/nonIterableRest.js
		function _nonIterableRest() {
		  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/slicedToArray.js




		function _slicedToArray(arr, i) {
		  return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest();
		}
		// EXTERNAL MODULE: ./node_modules/core-js/modules/es7.array.includes.js
		__webpack_require__("6762");

		// EXTERNAL MODULE: ./node_modules/core-js/modules/es6.string.includes.js
		__webpack_require__("2fdb");

		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/arrayWithoutHoles.js

		function _arrayWithoutHoles(arr) {
		  if (Array.isArray(arr)) return _arrayLikeToArray(arr);
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/iterableToArray.js
		function _iterableToArray(iter) {
		  if (typeof Symbol !== "undefined" && Symbol.iterator in Object(iter)) return Array.from(iter);
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/nonIterableSpread.js
		function _nonIterableSpread() {
		  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
		}
		// CONCATENATED MODULE: ./node_modules/@babel/runtime/helpers/esm/toConsumableArray.js




		function _toConsumableArray(arr) {
		  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
		}
		// EXTERNAL MODULE: external {"commonjs":"sortablejs","commonjs2":"sortablejs","amd":"sortablejs","root":"Sortable"}
		var external_commonjs_sortablejs_commonjs2_sortablejs_amd_sortablejs_root_Sortable_ = __webpack_require__("a352");
		var external_commonjs_sortablejs_commonjs2_sortablejs_amd_sortablejs_root_Sortable_default = /*#__PURE__*/__webpack_require__.n(external_commonjs_sortablejs_commonjs2_sortablejs_amd_sortablejs_root_Sortable_);

		// EXTERNAL MODULE: ./src/util/helper.js
		var helper = __webpack_require__("c649");

		// CONCATENATED MODULE: ./src/vuedraggable.js












		function buildAttribute(object, propName, value) {
		  if (value === undefined) {
		    return object;
		  }

		  object = object || {};
		  object[propName] = value;
		  return object;
		}

		function computeVmIndex(vnodes, element) {
		  return vnodes.map(function (elt) {
		    return elt.elm;
		  }).indexOf(element);
		}

		function _computeIndexes(slots, children, isTransition, footerOffset) {
		  if (!slots) {
		    return [];
		  }

		  var elmFromNodes = slots.map(function (elt) {
		    return elt.elm;
		  });
		  var footerIndex = children.length - footerOffset;

		  var rawIndexes = _toConsumableArray(children).map(function (elt, idx) {
		    return idx >= footerIndex ? elmFromNodes.length : elmFromNodes.indexOf(elt);
		  });

		  return isTransition ? rawIndexes.filter(function (ind) {
		    return ind !== -1;
		  }) : rawIndexes;
		}

		function emit(evtName, evtData) {
		  var _this = this;

		  this.$nextTick(function () {
		    return _this.$emit(evtName.toLowerCase(), evtData);
		  });
		}

		function delegateAndEmit(evtName) {
		  var _this2 = this;

		  return function (evtData) {
		    if (_this2.realList !== null) {
		      _this2["onDrag" + evtName](evtData);
		    }

		    emit.call(_this2, evtName, evtData);
		  };
		}

		function isTransitionName(name) {
		  return ["transition-group", "TransitionGroup"].includes(name);
		}

		function vuedraggable_isTransition(slots) {
		  if (!slots || slots.length !== 1) {
		    return false;
		  }

		  var _slots = _slicedToArray(slots, 1),
		      componentOptions = _slots[0].componentOptions;

		  if (!componentOptions) {
		    return false;
		  }

		  return isTransitionName(componentOptions.tag);
		}

		function getSlot(slot, scopedSlot, key) {
		  return slot[key] || (scopedSlot[key] ? scopedSlot[key]() : undefined);
		}

		function computeChildrenAndOffsets(children, slot, scopedSlot) {
		  var headerOffset = 0;
		  var footerOffset = 0;
		  var header = getSlot(slot, scopedSlot, "header");

		  if (header) {
		    headerOffset = header.length;
		    children = children ? [].concat(_toConsumableArray(header), _toConsumableArray(children)) : _toConsumableArray(header);
		  }

		  var footer = getSlot(slot, scopedSlot, "footer");

		  if (footer) {
		    footerOffset = footer.length;
		    children = children ? [].concat(_toConsumableArray(children), _toConsumableArray(footer)) : _toConsumableArray(footer);
		  }

		  return {
		    children: children,
		    headerOffset: headerOffset,
		    footerOffset: footerOffset
		  };
		}

		function getComponentAttributes($attrs, componentData) {
		  var attributes = null;

		  var update = function update(name, value) {
		    attributes = buildAttribute(attributes, name, value);
		  };

		  var attrs = Object.keys($attrs).filter(function (key) {
		    return key === "id" || key.startsWith("data-");
		  }).reduce(function (res, key) {
		    res[key] = $attrs[key];
		    return res;
		  }, {});
		  update("attrs", attrs);

		  if (!componentData) {
		    return attributes;
		  }

		  var on = componentData.on,
		      props = componentData.props,
		      componentDataAttrs = componentData.attrs;
		  update("on", on);
		  update("props", props);
		  Object.assign(attributes.attrs, componentDataAttrs);
		  return attributes;
		}

		var eventsListened = ["Start", "Add", "Remove", "Update", "End"];
		var eventsToEmit = ["Choose", "Unchoose", "Sort", "Filter", "Clone"];
		var readonlyProperties = ["Move"].concat(eventsListened, eventsToEmit).map(function (evt) {
		  return "on" + evt;
		});
		var draggingElement = null;
		var props = {
		  options: Object,
		  list: {
		    type: Array,
		    required: false,
		    default: null
		  },
		  value: {
		    type: Array,
		    required: false,
		    default: null
		  },
		  noTransitionOnDrag: {
		    type: Boolean,
		    default: false
		  },
		  clone: {
		    type: Function,
		    default: function _default(original) {
		      return original;
		    }
		  },
		  element: {
		    type: String,
		    default: "div"
		  },
		  tag: {
		    type: String,
		    default: null
		  },
		  move: {
		    type: Function,
		    default: null
		  },
		  componentData: {
		    type: Object,
		    required: false,
		    default: null
		  }
		};
		var draggableComponent = {
		  name: "draggable",
		  inheritAttrs: false,
		  props: props,
		  data: function data() {
		    return {
		      transitionMode: false,
		      noneFunctionalComponentMode: false
		    };
		  },
		  render: function render(h) {
		    var slots = this.$slots.default;
		    this.transitionMode = vuedraggable_isTransition(slots);

		    var _computeChildrenAndOf = computeChildrenAndOffsets(slots, this.$slots, this.$scopedSlots),
		        children = _computeChildrenAndOf.children,
		        headerOffset = _computeChildrenAndOf.headerOffset,
		        footerOffset = _computeChildrenAndOf.footerOffset;

		    this.headerOffset = headerOffset;
		    this.footerOffset = footerOffset;
		    var attributes = getComponentAttributes(this.$attrs, this.componentData);
		    return h(this.getTag(), attributes, children);
		  },
		  created: function created() {
		    if (this.list !== null && this.value !== null) {
		      helper["b" /* console */].error("Value and list props are mutually exclusive! Please set one or another.");
		    }

		    if (this.element !== "div") {
		      helper["b" /* console */].warn("Element props is deprecated please use tag props instead. See https://github.com/SortableJS/Vue.Draggable/blob/master/documentation/migrate.md#element-props");
		    }

		    if (this.options !== undefined) {
		      helper["b" /* console */].warn("Options props is deprecated, add sortable options directly as vue.draggable item, or use v-bind. See https://github.com/SortableJS/Vue.Draggable/blob/master/documentation/migrate.md#options-props");
		    }
		  },
		  mounted: function mounted() {
		    var _this3 = this;

		    this.noneFunctionalComponentMode = this.getTag().toLowerCase() !== this.$el.nodeName.toLowerCase() && !this.getIsFunctional();

		    if (this.noneFunctionalComponentMode && this.transitionMode) {
		      throw new Error("Transition-group inside component is not supported. Please alter tag value or remove transition-group. Current tag value: ".concat(this.getTag()));
		    }

		    var optionsAdded = {};
		    eventsListened.forEach(function (elt) {
		      optionsAdded["on" + elt] = delegateAndEmit.call(_this3, elt);
		    });
		    eventsToEmit.forEach(function (elt) {
		      optionsAdded["on" + elt] = emit.bind(_this3, elt);
		    });
		    var attributes = Object.keys(this.$attrs).reduce(function (res, key) {
		      res[Object(helper["a" /* camelize */])(key)] = _this3.$attrs[key];
		      return res;
		    }, {});
		    var options = Object.assign({}, this.options, attributes, optionsAdded, {
		      onMove: function onMove(evt, originalEvent) {
		        return _this3.onDragMove(evt, originalEvent);
		      }
		    });
		    !("draggable" in options) && (options.draggable = ">*");
		    this._sortable = new external_commonjs_sortablejs_commonjs2_sortablejs_amd_sortablejs_root_Sortable_default.a(this.rootContainer, options);
		    this.computeIndexes();
		  },
		  beforeDestroy: function beforeDestroy() {
		    if (this._sortable !== undefined) this._sortable.destroy();
		  },
		  computed: {
		    rootContainer: function rootContainer() {
		      return this.transitionMode ? this.$el.children[0] : this.$el;
		    },
		    realList: function realList() {
		      return this.list ? this.list : this.value;
		    }
		  },
		  watch: {
		    options: {
		      handler: function handler(newOptionValue) {
		        this.updateOptions(newOptionValue);
		      },
		      deep: true
		    },
		    $attrs: {
		      handler: function handler(newOptionValue) {
		        this.updateOptions(newOptionValue);
		      },
		      deep: true
		    },
		    realList: function realList() {
		      this.computeIndexes();
		    }
		  },
		  methods: {
		    getIsFunctional: function getIsFunctional() {
		      var fnOptions = this._vnode.fnOptions;
		      return fnOptions && fnOptions.functional;
		    },
		    getTag: function getTag() {
		      return this.tag || this.element;
		    },
		    updateOptions: function updateOptions(newOptionValue) {
		      for (var property in newOptionValue) {
		        var value = Object(helper["a" /* camelize */])(property);

		        if (readonlyProperties.indexOf(value) === -1) {
		          this._sortable.option(value, newOptionValue[property]);
		        }
		      }
		    },
		    getChildrenNodes: function getChildrenNodes() {
		      if (this.noneFunctionalComponentMode) {
		        return this.$children[0].$slots.default;
		      }

		      var rawNodes = this.$slots.default;
		      return this.transitionMode ? rawNodes[0].child.$slots.default : rawNodes;
		    },
		    computeIndexes: function computeIndexes() {
		      var _this4 = this;

		      this.$nextTick(function () {
		        _this4.visibleIndexes = _computeIndexes(_this4.getChildrenNodes(), _this4.rootContainer.children, _this4.transitionMode, _this4.footerOffset);
		      });
		    },
		    getUnderlyingVm: function getUnderlyingVm(htmlElt) {
		      var index = computeVmIndex(this.getChildrenNodes() || [], htmlElt);

		      if (index === -1) {
		        //Edge case during move callback: related element might be
		        //an element different from collection
		        return null;
		      }

		      var element = this.realList[index];
		      return {
		        index: index,
		        element: element
		      };
		    },
		    getUnderlyingPotencialDraggableComponent: function getUnderlyingPotencialDraggableComponent(_ref) {
		      var vue = _ref.__vue__;

		      if (!vue || !vue.$options || !isTransitionName(vue.$options._componentTag)) {
		        if (!("realList" in vue) && vue.$children.length === 1 && "realList" in vue.$children[0]) return vue.$children[0];
		        return vue;
		      }

		      return vue.$parent;
		    },
		    emitChanges: function emitChanges(evt) {
		      var _this5 = this;

		      this.$nextTick(function () {
		        _this5.$emit("change", evt);
		      });
		    },
		    alterList: function alterList(onList) {
		      if (this.list) {
		        onList(this.list);
		        return;
		      }

		      var newList = _toConsumableArray(this.value);

		      onList(newList);
		      this.$emit("input", newList);
		    },
		    spliceList: function spliceList() {
		      var _arguments = arguments;

		      var spliceList = function spliceList(list) {
		        return list.splice.apply(list, _toConsumableArray(_arguments));
		      };

		      this.alterList(spliceList);
		    },
		    updatePosition: function updatePosition(oldIndex, newIndex) {
		      var updatePosition = function updatePosition(list) {
		        return list.splice(newIndex, 0, list.splice(oldIndex, 1)[0]);
		      };

		      this.alterList(updatePosition);
		    },
		    getRelatedContextFromMoveEvent: function getRelatedContextFromMoveEvent(_ref2) {
		      var to = _ref2.to,
		          related = _ref2.related;
		      var component = this.getUnderlyingPotencialDraggableComponent(to);

		      if (!component) {
		        return {
		          component: component
		        };
		      }

		      var list = component.realList;
		      var context = {
		        list: list,
		        component: component
		      };

		      if (to !== related && list && component.getUnderlyingVm) {
		        var destination = component.getUnderlyingVm(related);

		        if (destination) {
		          return Object.assign(destination, context);
		        }
		      }

		      return context;
		    },
		    getVmIndex: function getVmIndex(domIndex) {
		      var indexes = this.visibleIndexes;
		      var numberIndexes = indexes.length;
		      return domIndex > numberIndexes - 1 ? numberIndexes : indexes[domIndex];
		    },
		    getComponent: function getComponent() {
		      return this.$slots.default[0].componentInstance;
		    },
		    resetTransitionData: function resetTransitionData(index) {
		      if (!this.noTransitionOnDrag || !this.transitionMode) {
		        return;
		      }

		      var nodes = this.getChildrenNodes();
		      nodes[index].data = null;
		      var transitionContainer = this.getComponent();
		      transitionContainer.children = [];
		      transitionContainer.kept = undefined;
		    },
		    onDragStart: function onDragStart(evt) {
		      this.context = this.getUnderlyingVm(evt.item);
		      evt.item._underlying_vm_ = this.clone(this.context.element);
		      draggingElement = evt.item;
		    },
		    onDragAdd: function onDragAdd(evt) {
		      var element = evt.item._underlying_vm_;

		      if (element === undefined) {
		        return;
		      }

		      Object(helper["d" /* removeNode */])(evt.item);
		      var newIndex = this.getVmIndex(evt.newIndex);
		      this.spliceList(newIndex, 0, element);
		      this.computeIndexes();
		      var added = {
		        element: element,
		        newIndex: newIndex
		      };
		      this.emitChanges({
		        added: added
		      });
		    },
		    onDragRemove: function onDragRemove(evt) {
		      Object(helper["c" /* insertNodeAt */])(this.rootContainer, evt.item, evt.oldIndex);

		      if (evt.pullMode === "clone") {
		        Object(helper["d" /* removeNode */])(evt.clone);
		        return;
		      }

		      var oldIndex = this.context.index;
		      this.spliceList(oldIndex, 1);
		      var removed = {
		        element: this.context.element,
		        oldIndex: oldIndex
		      };
		      this.resetTransitionData(oldIndex);
		      this.emitChanges({
		        removed: removed
		      });
		    },
		    onDragUpdate: function onDragUpdate(evt) {
		      Object(helper["d" /* removeNode */])(evt.item);
		      Object(helper["c" /* insertNodeAt */])(evt.from, evt.item, evt.oldIndex);
		      var oldIndex = this.context.index;
		      var newIndex = this.getVmIndex(evt.newIndex);
		      this.updatePosition(oldIndex, newIndex);
		      var moved = {
		        element: this.context.element,
		        oldIndex: oldIndex,
		        newIndex: newIndex
		      };
		      this.emitChanges({
		        moved: moved
		      });
		    },
		    updateProperty: function updateProperty(evt, propertyName) {
		      evt.hasOwnProperty(propertyName) && (evt[propertyName] += this.headerOffset);
		    },
		    computeFutureIndex: function computeFutureIndex(relatedContext, evt) {
		      if (!relatedContext.element) {
		        return 0;
		      }

		      var domChildren = _toConsumableArray(evt.to.children).filter(function (el) {
		        return el.style["display"] !== "none";
		      });

		      var currentDOMIndex = domChildren.indexOf(evt.related);
		      var currentIndex = relatedContext.component.getVmIndex(currentDOMIndex);
		      var draggedInList = domChildren.indexOf(draggingElement) !== -1;
		      return draggedInList || !evt.willInsertAfter ? currentIndex : currentIndex + 1;
		    },
		    onDragMove: function onDragMove(evt, originalEvent) {
		      var onMove = this.move;

		      if (!onMove || !this.realList) {
		        return true;
		      }

		      var relatedContext = this.getRelatedContextFromMoveEvent(evt);
		      var draggedContext = this.context;
		      var futureIndex = this.computeFutureIndex(relatedContext, evt);
		      Object.assign(draggedContext, {
		        futureIndex: futureIndex
		      });
		      var sendEvt = Object.assign({}, evt, {
		        relatedContext: relatedContext,
		        draggedContext: draggedContext
		      });
		      return onMove(sendEvt, originalEvent);
		    },
		    onDragEnd: function onDragEnd() {
		      this.computeIndexes();
		      draggingElement = null;
		    }
		  }
		};

		if (typeof window !== "undefined" && "Vue" in window) {
		  window.Vue.component("draggable", draggableComponent);
		}

		/* harmony default export */ var vuedraggable = (draggableComponent);
		// CONCATENATED MODULE: ./node_modules/@vue/cli-service/lib/commands/build/entry-lib.js


		/* harmony default export */ __webpack_exports__["default"] = (vuedraggable);



		/***/ })

		/******/ })["default"];
		});
		
	} (vuedraggable_umd$1));
	return vuedraggable_umd$1.exports;
}

requireVuedraggable_umd();

const {createElementVNode:_createElementVNode$3,renderList:_renderList$1,Fragment:_Fragment$1,openBlock:_openBlock$3,createElementBlock:_createElementBlock$3,toDisplayString:_toDisplayString$2,createTextVNode:_createTextVNode$2,unref:_unref$2} = await importShared('vue');

const {onMounted: onMounted$1} = await importShared('vue');
const {computed: computed$1} = await importShared('vue');

const {unref:_unref$1,createElementVNode:_createElementVNode$2,openBlock:_openBlock$2,createElementBlock:_createElementBlock$2,createCommentVNode:_createCommentVNode$1,toDisplayString:_toDisplayString$1,renderList:_renderList,Fragment:_Fragment,createVNode:_createVNode$2,createTextVNode:_createTextVNode$1,normalizeClass:_normalizeClass,normalizeStyle:_normalizeStyle,createBlock:_createBlock} = await importShared('vue');


const _hoisted_1$1 = {
  key: 0,
  class: "back-arrow-box"
};
const _hoisted_2$1 = ["src"];
const _hoisted_3$1 = { class: "list-results-title" };
const _hoisted_4$1 = {
  key: 1,
  class: "hide-category"
};
const _hoisted_5$1 = ["id", "checked", "onChange"];
const _hoisted_6 = ["for"];
const _hoisted_7 = { class: "content-list" };
const _hoisted_8 = { key: 0 };
const _hoisted_9 = {
  key: 1,
  class: "list-items-container"
};
const _hoisted_10 = { class: "menu-results-box" };
const _hoisted_11 = ["onClick"];
const _hoisted_12 = { class: "title-week" };
const _hoisted_13 = ["src"];
const _hoisted_14 = { class: "day-box" };
const _hoisted_15 = { class: "day-results-box" };
const _hoisted_16 = { style: {"background-color":"transparent"} };
const _hoisted_17 = {
  key: 0,
  class: "select-item-form"
};

const {computed,onMounted,ref} = await importShared('vue');



const _sfc_main$1 = {
  __name: 'MenuResults',
  setup(__props) {

const menuStore = useMenuStore(); // Utilisation du store pour accéder aux données et fonctions
const { isMobile } = useIsMobile(); 

//------ Gère le style d'affichage des résultats de la liste en fonction du type de vue sélectionné (single, multi, titleOnly)
computed(() => {
    switch (menuStore.selectedMenusList.typeView) {
        case 'single':
            return 'list-items'
        case 'titleOnly':
            return 'list-items-title-only'
        case 'multi':
            return 'list-items'
    }
});//penser ICIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII


//-------- tableau des catégories de menus à partir des catégories dans les objet menus
const categoriesInMenus = ref(['Petits-déjeuners', 'Entrées', 'Plats', 'Desserts', 'Collations']);

const hideThisCategory = (category) => {
  console.log('test', menuStore.hiddenCategories);
  if(menuStore.hiddenCategories.includes(category)){
    menuStore.hiddenCategories = menuStore.hiddenCategories.filter(c => c !== category);
    menuStore.selectedMenusList.displayCategory = menuStore.hiddenCategories;
    menuStore.updateMenusListcategorySelected(menuStore.selectedMenusList._id, menuStore.selectedMenusList.displayCategory);
  } else {
    
  menuStore.hiddenCategories.push(category);
  menuStore.hiddenCategories = menuStore.hiddenCategories.sort((a, b) => categoriesInMenus.value.indexOf(a) - categoriesInMenus.value.indexOf(b));
  menuStore.selectedMenusList.displayCategory = menuStore.hiddenCategories;
  menuStore.updateMenusListcategorySelected(menuStore.selectedMenusList._id, menuStore.selectedMenusList.displayCategory);
  }
};
//------ Gère l'affichage de la zone de résultats 
//je crée un tableau qui liste les categories et les jours du mois pour afficher les résultats de chaque jour dans la bonne catégorie
const categoriesAndDays = computed(() => {
  const categories = menuStore.hiddenCategories;
  const days = menuStore.weeksInMonth.flat(); // Aplatit le tableau de semaines pour obtenir un tableau de jours
  const categoriesAndDays = [];
  categories.forEach(category => {
    days.forEach((day, index) => {
      categoriesAndDays.push({ category, dayIndex: day.getDate() }); // Associe chaque catégorie à chaque jour en utilisant l'index du jour
    });
  });
  return categoriesAndDays
});

const menuToDisplay = computed(() => {

//je crée un tableau qui liste les menus choisi par le user
  const existingMenus = menuStore.selectedMenusList.items.map(item => {
    // Trouve le menu correspondant dans la liste des menus suggérés en fonction de l'ID
    const menu = menuStore.suggestedMenus.find(menu => menu.name === item.name);//penser à changer par item._id
    return {
      ...item,
      name: menu ? menu.name : 'Menu inconnu' // Ajoute le nom du menu à l'objet, ou 'Menu inconnu' si le menu n'est pas trouvé
    }
  });

//je fusionne les deux tableaux pour associer les menus choisis par le user à leur catégorie et leur jour correspondant et j'ajoute le menu aucune selection pour les catégories et les jours qui n'ont pas de menu choisi par le user
  const mergedMenus = categoriesAndDays.value.map(({ category, dayIndex }) => {
    const menu = existingMenus.find(menu => menu.category === category && menu.dayIndex === dayIndex);
    return menu ? menu : { name: 'Aucune-selection', category, dayIndex } // Si aucun menu n'est trouvé pour cette catégorie et ce jour, retourne un objet avec le nom 'Aucune-selection'
  });

  return mergedMenus



});

//------ Gère l'affichage des semaines selon les semaines du mois en cours en faisant des semaine du lundi au dimanche


//j'initialise selectedWeek à la semaine actuelle du mois
const selectedWeek = ref(`week-${menuStore.weeksInMonth.findIndex(week => week.some(day => day.getDate() === new Date().getDate()))}`);



const categoryColors = {
  'Petits-déjeuners': 'rgb(238,243,253)',
  'Entrées': 'rgb(248,249,238)',
  'Plats': 'rgb(255,243,228)',
  'Desserts': 'rgb(255,240,246)',
  'Collations': 'rgb(241,233,253)'
};


return (_ctx, _cache) => {
  return (_unref$1(isMobile) ? _unref$1(menuStore).showMenusListResults  : _unref$1(isMobile) === false)
    ? (_openBlock$2(), _createElementBlock$2("div", {
        key: 0,
        class: _normalizeClass(_unref$1(isMobile) ? 'list-results-mobile' : 'list-results')
      }, [
        (_unref$1(isMobile))
          ? (_openBlock$2(), _createElementBlock$2("div", _hoisted_1$1, [
              _createElementVNode$2("img", {
                src: _unref$1(arrowLeft),
                alt: "Retour",
                class: "back-arrow",
                onClick: _cache[0] || (_cache[0] = $event => (_unref$1(menuStore).showMenusListResults = false))
              }, null, 8, _hoisted_2$1)
            ]))
          : _createCommentVNode$1("", true),
        _createElementVNode$2("h3", _hoisted_3$1, _toDisplayString$1(_unref$1(menuStore).selectedMenusList?.title || 'Aucune Liste de menus sélectionnée'), 1),
        (_unref$1(menuStore).selectedMenusList)
          ? (_openBlock$2(), _createElementBlock$2("div", _hoisted_4$1, [
              (_openBlock$2(true), _createElementBlock$2(_Fragment, null, _renderList(categoriesInMenus.value, (category, index) => {
                return (_openBlock$2(), _createElementBlock$2("div", {
                  key: index,
                  class: "hide-category-option"
                }, [
                  _createElementVNode$2("input", {
                    type: "checkbox",
                    id: `hide-${category}`,
                    checked: _unref$1(menuStore).hiddenCategories.includes(category),
                    onChange: $event => (hideThisCategory(category))
                  }, null, 40, _hoisted_5$1),
                  _createElementVNode$2("label", {
                    class: "hide-category-label",
                    for: `hide-${category}`
                  }, _toDisplayString$1(category), 9, _hoisted_6)
                ]))
              }), 128))
            ]))
          : _createCommentVNode$1("", true),
        _createVNode$2(StarBar, { class: "mobile-star-bar" }),
        _createElementVNode$2("div", _hoisted_7, [
          (!_unref$1(menuStore).selectedMenusList)
            ? (_openBlock$2(), _createElementBlock$2("p", _hoisted_8, _cache[2] || (_cache[2] = [
                _createTextVNode$1("Choississez une liste dans la barre latérale pour afficher ses résultats. "),
                _createElementVNode$2("br", null, null, -1),
                _createTextVNode$1("Ou créez une nouvelle liste pour commencer à ajouter des éléments. "),
                _createElementVNode$2("br", null, null, -1),
                _createElementVNode$2("span", { class: "yellow-heart" }, "💛", -1)
              ])))
            : (_openBlock$2(), _createElementBlock$2("div", _hoisted_9, [
                _createElementVNode$2("div", _hoisted_10, [
                  (_openBlock$2(true), _createElementBlock$2(_Fragment, null, _renderList(_unref$1(menuStore).weeksInMonth, (week, index) => {
                    return (_openBlock$2(), _createElementBlock$2("div", {
                      key: index,
                      class: "week-box"
                    }, [
                      _createElementVNode$2("div", {
                        class: "title-week-box",
                        onClick: $event => (selectedWeek.value = selectedWeek.value === `week-${index}` ? null : `week-${index}`)
                      }, [
                        _createElementVNode$2("h3", _hoisted_12, "Semaine " + _toDisplayString$1(index + 1), 1),
                        _createElementVNode$2("img", {
                          src: _unref$1(AngleDown),
                          alt: "Voir plus",
                          class: _normalizeClass(["angle-down", { rotated: selectedWeek.value === `week-${index}` }])
                        }, null, 10, _hoisted_13)
                      ], 8, _hoisted_11),
                      _createElementVNode$2("div", _hoisted_14, [
                        _cache[3] || (_cache[3] = _createElementVNode$2("div", { class: "empty-day-box" }, null, -1)),
                        (_openBlock$2(true), _createElementBlock$2(_Fragment, null, _renderList(week, (day, dayIndex) => {
                          return (_openBlock$2(), _createElementBlock$2("p", {
                            class: "day-title",
                            key: dayIndex
                          }, _toDisplayString$1(day.toLocaleDateString('fr-FR', {weekday: 'long',day: 'numeric',month: 'long'})), 1))
                        }), 128))
                      ]),
                      (selectedWeek.value === `week-${index}`)
                        ? (_openBlock$2(true), _createElementBlock$2(_Fragment, { key: 0 }, _renderList(_unref$1(menuStore).hiddenCategories, (category, index) => {
                            return (_openBlock$2(), _createElementBlock$2("div", {
                              class: "categories-result",
                              key: index
                            }, [
                              _createElementVNode$2("h4", {
                                class: "category-title",
                                style: _normalizeStyle({ backgroundColor: categoryColors[category] })
                              }, _toDisplayString$1(category), 5),
                              _createElementVNode$2("div", _hoisted_15, [
                                (_openBlock$2(true), _createElementBlock$2(_Fragment, null, _renderList(week, (day, dayIndex) => {
                                  return (_openBlock$2(), _createElementBlock$2("div", _hoisted_16, [
                                    (_openBlock$2(true), _createElementBlock$2(_Fragment, null, _renderList(menuToDisplay.value.filter((item) => item.category === category && item.dayIndex === day.getDate()), (item, itemIndex) => {
                                      return (_openBlock$2(), _createBlock(Result, {
                                        key: itemIndex + item.name,
                                        item: item,
                                        categoryColor: categoryColors[category]
                                      }, null, 8, ["item", "categoryColor"]))
                                    }), 128))
                                  ]))
                                }), 256))
                              ])
                            ]))
                          }), 128))
                        : _createCommentVNode$1("", true)
                    ]))
                  }), 128))
                ]),
                (_unref$1(menuStore).showMenusListSelection)
                  ? (_openBlock$2(), _createElementBlock$2("div", _hoisted_17, [
                      _createVNode$2(CategoryMenus, {
                        category: _unref$1(menuStore).selectedCategory,
                        class: "select-item-box"
                      }, null, 8, ["category"]),
                      _createElementVNode$2("button", {
                        class: "primary-button ending-button",
                        onClick: _cache[1] || (_cache[1] = $event => (_unref$1(menuStore).showMenusListSelection = false))
                      }, "J'ai fini !")
                    ]))
                  : _createCommentVNode$1("", true)
              ]))
        ])
      ], 2))
    : _createCommentVNode$1("", true)
}
}

};
const MenuResults = /*#__PURE__*/_export_sfc(_sfc_main$1, [['__scopeId',"data-v-f5832fdd"]]);

const {createVNode:_createVNode$1,unref:_unref,createElementVNode:_createElementVNode$1,toDisplayString:_toDisplayString,createTextVNode:_createTextVNode,withModifiers:_withModifiers,openBlock:_openBlock$1,createElementBlock:_createElementBlock$1,createCommentVNode:_createCommentVNode} = await importShared('vue');


const _hoisted_1 = { class: "menu-view" };
const _hoisted_2 = { class: "to-delete-name" };
const _hoisted_3 = { class: "buttons" };
const _hoisted_4 = { class: "to-delete-name" };
const _hoisted_5 = { class: "buttons" };

const _sfc_main = {
  __name: 'MenuView',
  setup(__props) {

const menuStore = useMenuStore();


return (_ctx, _cache) => {
  return (_openBlock$1(), _createElementBlock$1("div", _hoisted_1, [
    _createVNode$1(Sidebar),
    _createVNode$1(MenuResults),
    (_unref(menuStore).showDeleteConfirmationMenusList || _unref(menuStore).showDeleteConfirmationMenu)
      ? (_openBlock$1(), _createElementBlock$1("div", {
          key: 0,
          class: "background-deletion",
          onClick: _cache[6] || (_cache[6] = $event => (_ctx.goback($event)))
        }, [
          (_unref(menuStore).showDeleteConfirmationMenusList)
            ? (_openBlock$1(), _createElementBlock$1("div", {
                key: 0,
                class: "pop-up-delete",
                onClick: _cache[2] || (_cache[2] = _withModifiers(() => {}, ["stop"]))
              }, [
                _createElementVNode$1("p", null, [
                  _cache[7] || (_cache[7] = _createTextVNode("Supprimer ")),
                  _cache[8] || (_cache[8] = _createElementVNode$1("br", null, null, -1)),
                  _cache[9] || (_cache[9] = _createTextVNode()),
                  _createElementVNode$1("span", _hoisted_2, _toDisplayString(_unref(menuStore).toDeleteName), 1),
                  _cache[10] || (_cache[10] = _createTextVNode(" ?"))
                ]),
                _createElementVNode$1("div", _hoisted_3, [
                  _createElementVNode$1("button", {
                    class: "danger-button",
                    onClick: _cache[0] || (_cache[0] = $event => ( _unref(menuStore).deleteMenusList(_unref(menuStore).toDeleteId)))
                  }, "Oui"),
                  _createElementVNode$1("button", {
                    class: "cancel",
                    onClick: _cache[1] || (_cache[1] = $event => (_unref(menuStore).showDeleteConfirmationMenusList = false))
                  }, "Non")
                ])
              ]))
            : _createCommentVNode("", true),
          (_unref(menuStore).showDeleteConfirmationMenu)
            ? (_openBlock$1(), _createElementBlock$1("div", {
                key: 1,
                class: "pop-up-delete",
                onClick: _cache[5] || (_cache[5] = _withModifiers(() => {}, ["stop"]))
              }, [
                _createElementVNode$1("p", null, [
                  _cache[11] || (_cache[11] = _createTextVNode("Supprimer ")),
                  _cache[12] || (_cache[12] = _createElementVNode$1("br", null, null, -1)),
                  _cache[13] || (_cache[13] = _createTextVNode()),
                  _createElementVNode$1("span", _hoisted_4, _toDisplayString(_unref(menuStore).toDeleteName), 1),
                  _cache[14] || (_cache[14] = _createTextVNode(" ?"))
                ]),
                _createElementVNode$1("div", _hoisted_5, [
                  _createElementVNode$1("button", {
                    class: "danger-button",
                    onClick: _cache[3] || (_cache[3] = $event => ( _unref(menuStore).deleteMenu(_unref(menuStore).selectedDay, _unref(menuStore).selectedCategory)))
                  }, "Oui"),
                  _createElementVNode$1("button", {
                    class: "cancel",
                    onClick: _cache[4] || (_cache[4] = $event => (_unref(menuStore).showDeleteConfirmationMenu = false))
                  }, "Non")
                ])
              ]))
            : _createCommentVNode("", true)
        ]))
      : _createCommentVNode("", true)
  ]))
}
}

};
const MenuView = /*#__PURE__*/_export_sfc(_sfc_main, [['__scopeId',"data-v-853dfc60"]]);

const {createElementVNode:_createElementVNode,createVNode:_createVNode,openBlock:_openBlock,createElementBlock:_createElementBlock} = await importShared('vue');

const {createApp} = await importShared('vue');

const {createPinia} = await importShared('pinia');
function createMyApp(Component, el) {
  const app = createApp(Component);
  const pinia = createPinia();
  app.use(pinia);
  app.mount(el);
}
function mount(el) {
  if (!el) {
    console.error("❌ container manquant");
    return;
  }
  el.innerHTML = "";
  const container = document.createElement("div");
  container.style.width = "100%";
  container.style.minHeight = "0";
  container.style.flex = "1";
  el.appendChild(container);
  createMyApp(MenuView, container);
}
window.addEventListener("error", function(event) {
  console.error("Erreur JavaScript capturée :", event.message);
  console.log("Fichier :", event.filename);
  console.log("Ligne :", event.lineno);
  console.log("Colonne :", event.colno);
  console.log("Objet erreur complet :", event.error);
});

export { mount };
